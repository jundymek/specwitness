---
topic: Proofgate — independent verification gate for agentic development (working repo name: specwitness)
updated: 2026-08-30T15:05
---

- (event) Headless PRD run: founder brief (docs/proofgate-input-brief.md, truncated at its s58) used as brain dump; Fast path; no interactive user
- (decision) Criterion Kind = 7 values (behavioral, integration, invariant, security, structural, performance, human); 'deterministic' dropped — it names a verification property, not a requirement class
- (decision) Severity = critical|normal only for V0; recorded in results but does not soften aggregation (any fail => FAIL)
- (decision) Verdict aggregation is a pure function: any fail=>FAIL, else any needs_human=>NEEDS_HUMAN, else PASS; criterion error => run exit 3 unless a fail already occurred
- (decision) Exit codes 0/1/2/3 kept per founder brief; usage errors moved to 64 (EX_USAGE) so automations never confuse bad args with NEEDS_HUMAN — concern documented as ADR-002
- (decision) plan is an explicit pipeline stage (contract->plan->verify), auto-invoked by verify when missing; keeps AI-free re-execution inspectable
- (decision) Branch naming/base/root never assumed after terminal-agents survey: first client uses epic/<n>-<slug>, base varies (master), invocation has no TTY; config+flags only, epic id normalized (7==epic-7==epic-07)
- (decision) BMAD ingestion targets v6 layout with configurable artifact roots (first client redirects to docs/planning-artifacts); EpicSpec is the plugin seam for other sources
- (assumption) Amendment confirmation V0 = procedural (explicit flag/prompt), not cryptographic; supervisor-should-not-amend flagged as open question 3
- (assumption) Billing safety = withhold ANTHROPIC_API_KEY/OPENAI_API_KEY from provider child env in subscription mode; parent env never mutated; CLI behavior re-verified at implementation
- (assumption) Gate failure maps to Verdict FAIL with gate_failed marker (branch demonstrably unmergeable); flagged for author review as open question 2
- (assumption) SM targets (false-positive <10%, duration <=15min, needs-human <30%, AI-free runs >80%) are initial calibrations, tunable during dogfooding
- (decision) MVP challenge (s56): differential BASE/HEAD execution, challenge/mutation, historical-contract regression, MCP/CI all deferred; revisions modeled per-run so differential bolts on
- (event) PRD finalized (status: final); reviewer gate deferred to architecture-phase review due to autonomous session — recorded as process deviation
- (override) Product renamed Proofgate -> SpecWitness by author (mid-session user message); PRD/addendum renamed accordingly
