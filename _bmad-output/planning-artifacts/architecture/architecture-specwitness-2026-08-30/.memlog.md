---
scope: Proofgate V0 npm CLI (whole system)
purpose: build-substrate
altitude: feature
updated: 2026-08-30T15:19
---

- (decision) Paradigm: hexagonal (ports & adapters) around a pure domain core, pipeline as explicit staged state machine; prevents domain/verdict logic tangling with subprocess+browser I/O so 7 parallel story agents cannot diverge on where side effects live
- (constraint) Founder constraints inherited from PRD/brief: no direct LLM APIs, no credential reading, local-first, no SaaS/UI, TypeScript+Node, npm package, standard Playwright, exit codes 0/1/2/3
- (version) Verified 2026-08-30: node 22.20 LTS, claude CLI 2.1.251 (-p, --output-format json), codex CLI 0.144.4 (exec --output-schema/--json), commander 15.0.0, zod 4.5.4, execa 10.0.1, vitest 4.1.11, yaml 2.9.0, @playwright/test 1.62.1, tsup 8.5.1, typescript latest=7.0.2 (Go compiler line) — pin 5.9.x for V0, evaluate 7.x later
- (decision) Security invariant: only ProjectConfig-declared commands may reach a shell; plan probes are typed ops (http/browser/observation/shell-by-config-id); provider output can never introduce a new shell string
- (decision) Fingerprint = SHA-256 over canonical JSON (sorted keys, LF, trimmed) of contract content excluding volatile metadata; stored in contract file; checked before every run
- (decision) Isolation: git worktree from resolved SHA under OS tempdir; run manifest records worktree paths + process-group ids; teardown kills pgids; proofgate clean replays manifests; containers deferred (ADR-004)
- (assumption) Playwright: use project's installation when present, else Proofgate-provisioned; generated scenarios ephemeral in run dir
- (event) Reviewer gate run headless: lint_spine 0 findings; version-reality lens CONCERNS (Node20 EOL blocker, TS 5.9-vs-7 false binary, pnpm 9 stale, execa10 pgid semantics unverified, dep-cruiser missing from stack); adversarial lens CONCERNS (6 divergence pairs + missing authoring module)
- (change) Applied all reviewer fixes: Node floor >=22.12, TS 6.0.x, pnpm 11, dep-cruiser 18 in stack; AD-5 spec/meta partition + Criterion single owner + plans reference by id; AD-6/7 GateFailure as stage result with single aggregate converter; AD-2 shared invoke.ts envelope; AD-8 RunStore sole writer w/ fsync manifests; new AD-13 probe execution contract; authoring/ module + cli->authoring->providers edges; ids normalization owned by domain/ids.ts; criterion id canonical E<n>-<NN>
- (decision) Playwright consumed as test runner (@playwright/test) over generated ephemeral spec files; project installation preferred, provisioned fallback in specwitness-owned cache
- (override) Product renamed Proofgate -> SpecWitness by author mid-session (user message); all artifacts renamed; npm/CLI name specwitness; dot-dir .specwitness/; input brief kept verbatim with naming note
- (event) spine finalized
- (event) Brief tail s59-75 re-supplied by author; reconciled: architecture-questions.md answers all 70 s60 questions (new decisions: explicit config ports + collision=InfraError, keep-all run retention, contracts/plans in Git vs runs local, provenance in meta, unplannable criterion => needs_human); AD-5 + conventions updated accordingly
- (decision) ADR-006 founding decisions (TS/Node npm CLI, standalone product, epic-level boundary, contract-before-implementation, AI-plans/deterministic-execution) + ADR-INDEX mapping all s71 required topics
