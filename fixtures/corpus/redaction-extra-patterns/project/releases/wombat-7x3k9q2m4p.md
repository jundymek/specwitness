# Release notes

Channel: stable

This file's NAME is the project-shaped secret. A passing file probe persists the
paths it matched, not the content it read, so the name is what the file surface
captures — and what the declared pattern must keep out of the run directory.
