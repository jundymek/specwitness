# Hand-written Verification Contract for the corpus fixture `redaction-extra-patterns`.
#
# The `spec:` half was written by hand. `meta.frozen`, `meta.fingerprint` and
# `meta.frozenAt` were produced ONCE, at authoring time, by the shipped
# `specwitness contract epic-1 --freeze` — see `fixtures/corpus/README.md`. The
# OUTCOME, in `expected.json`, is hand-written and never generated.
#
# No criterion mentions the release handle. The contract is about what the
# application does; the secret is what its commands happen to print, and this
# fixture pins where that text ends up.
spec:
  epic: epic-1
  version: 1
  criteria:
    - id: E1-01
      statement: The release endpoint answers 200 and reports the release as published.
      kind: behavioral
      severity: critical
      verifiability: automated
    - id: E1-02
      statement: The packaged command reports its version.
      kind: structural
      severity: normal
      verifiability: automated
    - id: E1-03
      statement: Exactly one release has published release notes.
      kind: structural
      severity: normal
      verifiability: automated
meta:
  schemaVersion: 1
  frozen: true
  fingerprint: 27a8bd1faf3039789ae10a82f8b02cebdfd4daee12ffb3a3ca939f267e42eb5a
  createdAt: 2026-09-10T09:00:00.000Z
  frozenAt: 2026-09-10T09:15:00.000Z
  provenance:
    provider: fake
    model: null
    providerCliVersion: null
    generatedAt: 2026-09-10T09:00:00.000Z
  history: []
