// The declared `data.reset` command. It resets nothing; it prints the
// project-shaped secret and exits 0. No network, no filesystem, no clock.

process.stdout.write('reset ok for handle wombat-7x3k9q2m4p\n');
process.exit(0);
