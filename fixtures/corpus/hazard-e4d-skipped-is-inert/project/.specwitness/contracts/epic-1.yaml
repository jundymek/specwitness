# Hand-written Verification Contract for the corpus fixture
# `hazard-e4d-skipped-is-inert`.
#
# The `spec:` half was written by hand. `meta.frozen`, `meta.fingerprint` and
# `meta.frozenAt` were produced ONCE, at authoring time, by the shipped
# `specwitness contract epic-1 --freeze` — see `fixtures/corpus/README.md`
# section "Adding a fixture". A contract is an INPUT to the run and its
# fingerprint is a hash the product must recompute from these bytes or refuse;
# `expected.json` — the OUTCOME — is hand-written and never generated.
#
# ⚠️ BOTH CRITERIA ARE `verifiability: automated`, AND THAT IS THE POINT. This
# contract's author asked for two things to be checked by machine, and neither
# is. Had either been marked `human`, the run would have stopped at NEEDS_HUMAN
# (exit 2) — the human clause fires unconditionally, before attempts are even
# looked at — and the hazard would be hidden behind a correct-looking stop.
#
# The statements are deliberately CONCRETE and clearly checkable. A reader
# meeting this fixture should feel the gap immediately: these are ordinary
# criteria that any probe could have adjudicated, and the run reported PASS
# without adjudicating either of them.
spec:
  epic: epic-1
  version: 1
  criteria:
    - id: E1-01
      statement: The checkout endpoint rejects a negative quantity with HTTP 400.
      kind: behavioral
      severity: critical
      verifiability: automated
    - id: E1-02
      statement: A completed checkout writes exactly one order row.
      kind: integration
      severity: critical
      verifiability: automated
meta:
  schemaVersion: 1
  frozen: true
  fingerprint: 0c2de3c93e69e5d888e401e2e8870334aa3bf26e20aed7c7172be807dd32b8ed
  createdAt: 2026-09-04T09:00:00.000Z
  frozenAt: 2026-09-04T09:15:00.000Z
  provenance:
    provider: fake
    model: null
    providerCliVersion: null
    generatedAt: 2026-09-04T09:00:00.000Z
  history: []
