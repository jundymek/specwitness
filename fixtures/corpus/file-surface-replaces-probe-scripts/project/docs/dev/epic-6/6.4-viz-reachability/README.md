# 6.4 — Every control reachable at every viewport

## Layout report

Content length grows with the number of co-changes, so the side panel was tested
at three viewport heights: 600, 768 and 1080 pixels.

| viewport height | was any control unreachable before scrolling? |
| --- | --- |
| 600 | yes — both the filter and the export button |
| 768 | no |
| 1080 | no |
