# Hand-written Verification Contract for the corpus fixture
# `browser-probe-provisions`.
#
# The `spec:` half was written by hand. `meta.frozen`, `meta.fingerprint` and
# `meta.frozenAt` were produced ONCE, at authoring time, by the shipped
# `specwitness contract epic-7 --freeze`; see `fixtures/corpus/README.md`.
#
# ONE CRITERION, AND IT IS A UI ONE ON PURPOSE. This fixture is not about what
# the page says — no probe of it ever runs. It is about what the product does on
# the way there: a criterion that only a browser can adjudicate is the shortest
# contract that forces `verify` to need a browser at all.
spec:
  epic: epic-7
  version: 1
  criteria:
    - id: E7-01
      statement: The orders page renders under the heading Orders.
      kind: behavioral
      severity: normal
      verifiability: automated
meta:
  schemaVersion: 1
  frozen: true
  fingerprint: 07f7ef336c67527579ec94ef99b3a59eadcf3038b96de90a1a3f301054b7bb9b
  createdAt: 2026-09-07T09:00:00.000Z
  frozenAt: 2026-09-07T09:15:00.000Z
  provenance:
    provider: fake
    model: null
    providerCliVersion: null
    generatedAt: 2026-09-07T09:00:00.000Z
  history: []
