# Hand-written Verification Contract for the corpus fixture
# `08-unstartable-service`.
#
# The `spec:` half was written by hand. `meta.frozen`, `meta.fingerprint` and
# `meta.frozenAt` were produced ONCE, at authoring time, by the shipped
# `specwitness contract epic-1 --freeze` — see `fixtures/corpus/README.md`
# section "Adding a fixture". A contract is an INPUT to the run and its
# fingerprint is a hash the product must recompute from these bytes or refuse;
# `expected.json` — the OUTCOME — is hand-written and never generated.
#
# ONE criterion, `automated`, and that minimalism is the point. This fixture
# never adjudicates anything: the run stops at the services stage before a
# single probe executes. The criterion exists so the plan beside it is a real
# plan against a real service, which is what gives the project something to
# adjudicate and keeps the run from being refused before it starts.
spec:
  epic: epic-1
  version: 1
  criteria:
    - id: E1-01
      statement: The orders endpoint answers 200 and reports the order state as approved.
      kind: behavioral
      severity: critical
      verifiability: automated
meta:
  schemaVersion: 1
  frozen: true
  fingerprint: 8ccc58d6fdc7124a50affe1bb8e1f4eb4e9f4f88781ea14894624765bfaa053b
  createdAt: 2026-09-04T09:00:00.000Z
  frozenAt: 2026-09-04T09:15:00.000Z
  provenance:
    provider: fake
    model: null
    providerCliVersion: null
    generatedAt: 2026-09-04T09:00:00.000Z
  history: []
