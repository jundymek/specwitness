# Hand-written Verification Contract for the corpus fixture
# `06-subjective-criterion`.
#
# The `spec:` half was written by hand. `meta.frozen`, `meta.fingerprint` and
# `meta.frozenAt` were produced ONCE, at authoring time, by the shipped
# `specwitness contract epic-1 --freeze` — see `fixtures/corpus/README.md`
# section "Adding a fixture". A contract is an INPUT to the run and its
# fingerprint is a hash the product must recompute from these bytes or refuse;
# `expected.json` — the OUTCOME — is hand-written and never generated.
#
# ⚠️ `verifiability: human` ON E1-02 IS THE ENTIRE FIXTURE. It is one of exactly
# two NEEDS_HUMAN triggers in the whole product (Q39), and it is a property of
# the CONTRACT rather than a judgement made later at run time — which is why no
# probe result and no later stage may overturn it. Dropping this field at the
# integrity stage is what once made a contract whose author had written "no
# machine may answer this" verify PASS at exit 0: the machine answering the one
# question its author had written down that no machine may answer.
#
# E1-01 is `automated` and is satisfied, so the outcome is MIXED rather than
# uniformly unadjudicable. See `expected.json` for why that matters.
spec:
  epic: epic-1
  version: 1
  criteria:
    - id: E1-01
      statement: The printed checkout receipt states the order total.
      kind: structural
      severity: normal
      verifiability: automated
    - id: E1-02
      statement: The printed checkout receipt reads as courteous rather than merely correct.
      kind: behavioral
      severity: normal
      verifiability: human
meta:
  schemaVersion: 1
  frozen: true
  fingerprint: 138252eb59694b483cf8af8fd9e920be6addbc2346658bf1c129abff27528332
  createdAt: 2026-09-04T09:00:00.000Z
  frozenAt: 2026-09-04T09:15:00.000Z
  provenance:
    provider: fake
    model: null
    providerCliVersion: null
    generatedAt: 2026-09-04T09:00:00.000Z
  history: []
