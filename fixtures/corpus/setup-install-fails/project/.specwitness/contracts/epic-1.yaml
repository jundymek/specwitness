# Hand-written Verification Contract for the corpus fixture `setup-install-fails`.
#
# The `spec:` half was written by hand. `meta.frozen`, `meta.fingerprint` and
# `meta.frozenAt` were produced ONCE, at authoring time, by the shipped
# `specwitness contract epic-1 --freeze` — see `fixtures/corpus/README.md`
# section "Adding a fixture", step 3. That is deliberate and it is NOT what
# AD-12 forbids: a contract is an INPUT to the run, and its fingerprint is a
# hash the product must be able to recompute from these bytes or refuse the run.
# `expected.json` — the OUTCOME — is hand-written and never generated.
#
# ⚠️ THIS CRITERION IS NEVER ADJUDICATED, AND THE CONTRACT IS STILL REQUIRED.
# An infrastructure error at `setup` stops the pipeline before `aggregate`, so
# no criterion result is materialised at all and `expected.json` pins an empty
# criteria set. The contract must still be present, frozen and covered by the
# plan, or the run would be refused for a different reason — and a fixture that
# reaches its expected outcome two ways proves neither.
spec:
  epic: epic-1
  version: 1
  criteria:
    - id: E1-01
      statement: The packaged manifest reports the application name and version.
      kind: structural
      severity: normal
      verifiability: automated
meta:
  schemaVersion: 1
  frozen: true
  fingerprint: 7ec129181de781af166f883392da800ae741c6943bc1138e056672f5edcddee8
  createdAt: 2026-09-05T09:00:00.000Z
  frozenAt: 2026-09-05T09:15:00.000Z
  provenance:
    provider: fake
    model: null
    providerCliVersion: null
    generatedAt: 2026-09-05T09:00:00.000Z
  history: []
