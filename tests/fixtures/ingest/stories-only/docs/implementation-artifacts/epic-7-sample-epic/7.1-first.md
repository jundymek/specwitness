# Story 7.1: First story from a per-story file

Status: ready-for-dev

## Story

As a fixture author,
I want a per-story file with a narrative,
so that the story-file reader has something to read.

## Acceptance Criteria

From `docs/planning-artifacts/epics.md` (authoritative — expand, never weaken):

1. **Given** a per-story file
   **When** it is ingested
   **Then** its criteria are numbered from one.
2. **Given** a criterion following the first
   **When** it is read
   **Then** it keeps its own ordinal.

Clarifications (detail only, no weakening):

- This bullet is NOT an acceptance criterion. It follows the first contiguous
  list block and must be excluded.
- Neither is this one.

## Tasks / Subtasks

- [ ] Task 1 — this section must not be read at all.

## Dev Notes

Nothing here is ingested either.
