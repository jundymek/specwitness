# Prompt: Create Epic 3 story files (paste into a fresh Claude Code session in this repo)

---

You are working in the SpecWitness repo (`/Users/jundymek/dev/specwitness`, default branch `master`). **Epics 1 and 2 are implemented and merged** — Epic 1 at merge commit `b5377b0` (6 stories, 540 tests), Epic 2 at merge commit `fbdd359` (7 stories + follow-up 2.8, ~1336 tests on the epic tip); their retrospectives are `docs/findings/epic-1-retro-2026-08-31.md` and `docs/findings/epic-2-retro-2026-08-31.md`, and the harness-side defects Epic 2 produced are in `docs/findings/epic-2-harness-defects-2026-08-31.md`. Your ONLY task in this session: produce implementation-ready story files for **Epic 3 — Isolated Deterministic Verification** (stories 3.1–3.7), ready to be executed by the terminal-agents harness as one supervised cohort. Do NOT implement any production code, and do NOT modify the finalized planning artifacts (PRD, spine, ADRs, epics.md, roadmap.md) — except the one roadmap line explicitly assigned to you in Step 4.

Epic 3 is the epic where SpecWitness first *does its job*: `specwitness verify` becomes a real (gates-only) merge gate. Everything before it authored artifacts; this one executes against a branch and returns a verdict a harness will act on. Two consequences for how you write these stories: the exit-code contract stops being theoretical (a wrong classification here silently blocks or waves through a merge), and **this is the first epic whose code spawns arbitrary project commands** — the process-lifecycle discipline in AD-8 is the thing standing between a crashed run and a machine full of orphaned services.

## Step 0 — Ground truth first (two epics of merged code now exist)

Every story you write must reference the **real, merged** modules by path and exported name — never re-specify something that already exists, never invent a signature. Epic 2's lesson 5 is binding on you as an author too: *verify shapes against merged source, not against a summary* — including the summaries in this prompt. Read, in full:

1. `CLAUDE.md` (its "Current phase" line will be stale again — see Step 4).
2. `docs/findings/epic-2-retro-2026-08-31.md` — all of it, but especially:
   - **§5 debt item 1** — orphaned descendants are not reaped when a spawned child forks; detection is fixed, `kill(-pgid)` is **story 3.2's**, and nine orphaned `sleep 3600` processes on this machine are the evidence. This is the single most important inherited fact in the epic.
   - **§5 debt item 2** — the `roadmap.md` EPIC 3 wave-A line says "3.2 (infra/process-runner, …)" unqualified while 2.3 already created that file; 3.2 owns the **lifecycle extension**, not a new file. Owner action item B2.
   - **§5a** — two P2 defects in merged code that Epic 3 inherits and that touch its path: (i) `fenceMask` closing on a tagged inner fence in `src/ingest/bmad-v6/markdown.ts`, (ii) `src/authoring/contract-file.ts` reporting a failed write after a successful rename. Decide explicitly, in a story or as a question, whether Epic 3 fixes them (see Step 3's "Inherited defects" note) — do not let them become folklore.
   - **§3 learning 3** — "reusing a probe is not enough; the judgement over the probe has to be shared too." The retro names Epic 3's verify path as the next place this shape appears.
   - **§3 learning 1** — the unasserted half of a decision is where the bug lives.
   - **§6** — the seams Epic 3 builds on, listed by the supervisor as merged and test-pinned.
   - **§7 action items** (B1–B7, A1/A3 carried) and **§9** gates no agent could run — CI has never executed a step, Linux and Node exactly 22.12 remain unverified, and `--json` snapshot testing in CI is an Epic 3 exit criterion that no agent can run. Say so in the stories rather than letting an agent claim CI green.
3. `docs/findings/epic-2-harness-defects-2026-08-31.md` — H-1..H-16. Several change what you write into the Cohort & Dependencies block: **H-10** (specs told agents to call `set-depends-on.sh` at a point the intent gate forbids — five agents blocked; fix the wording in YOUR template), **H-3/H-4** (the rebase nudge reaches merged agents, and plain `git rebase` is wrong after a squash merge — put `git rebase --onto origin/epic/3-isolated-deterministic-verification <last-merged-sha>` in the story text), **H-8** (auto-review runs `pnpm test` concurrently in the agent's worktree — 2.8 fixed the project half; new Epic 3 test files must stay concurrency-safe), **H-13** (the secret-path matcher rejects filenames containing `.env` — do not name a file `*.env.test.ts`).
4. The Epic 1 and Epic 2 story files under `docs/implementation-artifacts/epic-1-install-configure-diagnose/` and `docs/implementation-artifacts/epic-2-verification-contracts/` — their **Dev Agent Record / Completion Notes** carry decisions the code embodies. Match their shape and depth (each ~350–480 lines).
5. The merged code — quote it, don't paraphrase:
   - **Domain core (frozen, do not redefine):** `src/domain/errors.ts` (UsageError, ConfigError, IngestError, IntegrityError, ProviderError, InfraError), `src/domain/result.ts` (`CriterionStatus`, `GateStatus`, `CriterionResult`, `GateResult` — note `CriterionResult` is documented as extended **additively** by Epic 3's `domain/criterion-result.ts`), `src/domain/run-outcome.ts` (`Verdict`, `RunOutcome`, `INFRA_ERROR_CLASSIFICATIONS`, `isVerdictOutcome`/`isInfraErrorOutcome`), `src/domain/verdict.ts` (the aggregation function 3.4/3.3 must call, not re-implement), `src/domain/ids.ts`, `src/domain/run-id.ts`, `src/domain/ports.ts` (`Clock`, `Ids` — its header says "Epic 3 brings the process runner and the git/worktree seam" **here**).
   - **The process seam:** `src/domain/process-runner.ts` (port: `ChildEnvironment`, `ProcessOutcome`, `ProcessResult`, `ProcessRunOptions`, `ProcessRunner` — read its OWNERSHIP block, which names 3.2 explicitly and says every method added now is one 3.2 must preserve) and `src/infra/process-runner.ts` (the execa 10 implementation, including the forking-timeout reproduction written into the file). 3.2 **extends** these; it does not replace them, and it must not break 2.4/2.5's call sites.
   - **Run storage:** `src/infra/run-store.ts` (`RunStore` — sole writer under `.specwitness/runs/<run-id>/` per AD-8; crash-durable manifest append implemented, **atomic stage-and-rename finalize for `result.json` deliberately NOT implemented — its consumer is story 3.5**), `src/schemas/manifest.ts` (`RunManifestSchema` with the reserved-empty `worktrees`/`processGroups`/`reaped` fields 3.2 populates, and the documented "populating them does NOT bump `schemaVersion`" policy), `src/schemas/versions.ts` (`SCHEMA_VERSIONS` — its comment reserves `jsonReport` for Epic 3; 3.5 registers it).
   - **The CLI edge:** `src/cli/main.ts` (command registration + the single broad catch), `src/cli/exit.ts` (the ONLY exit table), `src/cli/print-error.ts`, `src/cli/commands/report.ts` — read this one closely: it is a story-1.6 stub that explicitly defers `--json`, `report <epic>`, and full rendering to Epic 3 story 3.5, and its `renderRun` returns a string precisely so 3.5 can reuse the shape.
   - **Contract reading, for the integrity stage:** `src/authoring/contract-file.ts`, `src/authoring/contract.ts`, `src/authoring/verifiable.ts` (`assertVerifiableContract` — the guard 2.6 built for "verify refuses an unfrozen contract", which 3.7 finally wires), `src/schemas/contract.ts` (`parseContract`, integrity/fingerprint verification), `src/schemas/canonical.ts`.
   - **Config:** `src/config/schema.ts` + `src/config/types.ts` (`GateConfig`, `ServiceConfig`, `PlanningConfig`; gates are an ordered array with unique ids, `project.baseBranch` required, services carry readiness with `timeoutSec`), `src/config/declared-command.ts` (`DeclaredCommand`, `commandText()` — the AD-3 brand that gate commands MUST carry), `src/config/index.ts`.
   - **Guards Epic 3 must extend rather than duplicate:** `.dependency-cruiser.cjs` (there is no rule yet for `src/pipeline/**` or `src/report/**` — 3.3 and 3.6 must add them, in the spirit of `ingest-core-only`), `tests/unit/dependency-rules.test.ts` (now concurrency-safe after 2.8 — keep it that way), `tests/unit/doctor/credential-boundary.test.ts`, `tests/unit/exit-location.test.ts`, `tests/integration/cli.test.ts`, `tests/setup/build-cli.ts`, `tests/fakes/ports.ts` (`FixedClock`, `SequenceIds`), `tests/fakes/agent-provider.ts`, `package.json`, `.github/workflows/ci.yml` (triggers still disabled — agents run the steps locally and say so).
6. Run `git --version` and record it; note the git features 3.1/3.2 depend on (`git worktree add --detach`, `git worktree list --porcelain`, `git worktree remove --force`, `git rev-parse --verify <ref>^{commit}`) and require the stories to **probe or fail with a named error** rather than assume. Also check whether `.specwitness/` exists in this repo and what `init` writes to `.gitignore` (Q11: `runs/` and `scorecard.jsonl` are local-only) — 3.5's persistence must land inside that boundary.

## Step 1 — Load planning context (in this order)

1. `docs/planning-artifacts/epics.md` — Epic 3 section: 7 stories with Given/When/Then (authoritative; expand, never weaken).
2. `docs/planning-artifacts/roadmap.md` — EPIC 3 block: waves (A: 3.1 + 3.2 + 3.3 → B: 3.4 + 3.5 + 3.6 → 3.7), shared contracts ("AD-6 aggregation incl. gateFailed, AD-7 hierarchy, AD-8 manifest discipline, RunResult shape (3.3's, merged before wave B), result.json schemaVersion"), exit criteria. **Note the wave-A line's known inaccuracy** (retro §5 debt 2 / action B2) and treat 3.2 as a lifecycle *extension* of a file that already exists.
3. `docs/planning-artifacts/architecture/architecture-specwitness-2026-08-30/ARCHITECTURE-SPINE.md` — **AD-6** (closed enums; aggregation as one pure function over gate + criterion results; `GateFailure` is a **stage result, not a thrown exception**, and the aggregate stage is the only converter; exit mapping in exactly one table), **AD-7** (hierarchy; gate failure deliberately outside it; unclassified exceptions escalate to `InfraError`, fail closed), **AD-8** (detached worktree under OS temp dir, source repo read-only, per-process-group spawning, manifest written+fsynced *before* resources exist-in-use, teardown then `clean`, RunStore sole writer, SpecWitness files never in the project working tree), **AD-9** (determinism ports; no direct clock/random reads in `domain`/`pipeline`), **AD-10** (typed evidence union incl. `command` and `gate`; redaction at capture in `domain/evidence.ts` constructors; renderers never re-redact), **AD-11** (one `RunResult`, many renderers; no renderer computes its own facts; bounded terminal output with a pointer to the full file; JSON snapshot-tested), **AD-13** (`SurfaceExecutor` / `ProbeAttempt` and the single `domain/criterion-result.ts` — 3.3 lands these types even though Epic 4/5 fill them), AD-1 layer map and the Stack table.
4. `docs/adr/ADR-002-exit-codes.md`, `docs/adr/ADR-003-gate-failure-verdict.md` (the `gateFailed` marker; note carried action A3 — the wording should read `gateFailed?: string`, and `src/domain/run-outcome.ts` already implements it that way), `docs/adr/ADR-004-worktree-isolation.md`, `docs/adr/ADR-INDEX.md`.
5. `docs/planning-artifacts/prds/prd-specwitness-2026-08-30/prd.md` — FR-19, FR-20, FR-21 (partial — only the process-lifecycle half is Epic 3's; service readiness is Epic 4 story 4.1), FR-22, FR-27, FR-28, FR-29, FR-30, FR-31, FR-32 (semantics only — Epic 3 defines `flaky` in the model; retry orchestration arrives with probes), NFR-7, NFR-8, NFR-9, UJ-2; `addendum.md` §A (how the first client consumes exit codes and `--json`) and §D (config sketch).
6. `docs/planning-artifacts/architecture/architecture-specwitness-2026-08-30/architecture-questions.md` — **Q11/Q12** (what is committed vs local-only), **Q14/Q15** (only config-declared commands reach a shell; ProcessRunner only), **Q18–Q22** (worktree creation, cleanup guarantee, `clean`, temp files, read-only-toward-source incl. the "kill verify mid-run and assert `git status` clean" corpus test), **Q25** (per-process-group kill covers grandchildren; macOS/Linux semantics verified **by story 3.2's tests** — this is the execa 10 concern the spine review flagged), **Q40–Q46** (criterion status semantics and aggregation), **Q47–Q52** (evidence union, relative evidence paths, size caps, run storage, keep-all retention, `report` re-renders and never re-executes), **Q53–Q55** (versioned `result.json`, same document on stdout with `--json`, exit code + JSON as the harness contract). Look these up before inventing any "how exactly".

## Step 2 — Generate the seven story files

Invoke the BMAD skill **`bmad-create-story`** explicitly by name (deprecated in favor of bmad-build, but correct here: stories are executed by the external harness, not bmad-build). The skill's default output path is `docs/implementation-artifacts/<dashed-key>.md` — **override it**: the harness reads specs by dot-style task id from an epic directory. Write each file to:

```
docs/implementation-artifacts/epic-3-isolated-deterministic-verification/<task-id>.md
```

with these exact task ids (they map to the existing `sprint-status.yaml` keys via the harness's `first-dot-dash` rule, satisfy `TASK_ID_REGEX='^[0-9]+\.[0-9]+-[a-z0-9-]+$'`, and are ≤ 64 chars):

```
3.1-revision-resolution-isolated-worktree
3.2-process-groups-run-manifest-specwitness-clean
3.3-staged-pipeline-state-machine
3.4-deterministic-gates-execution
3.5-runresult-persistence-specwitness-report
3.6-terminal-json-renderers
3.7-end-to-end-gates-only-verify
```

Run the skill workflow once per story, in that order. Epic integration branch for this cohort: **`epic/3-isolated-deterministic-verification`** (story PRs target it, never `master`).

## Step 3 — Quality bar for every story (founder brief §63 + Epic 1 and 2 lessons)

Each file, in the shape the Epic 1/2 specs established (Status → Cohort & Dependencies → Story → Acceptance Criteria verbatim + "Clarifications (detail only, no weakening)" → Tasks/Subtasks (TDD order) → Dev Notes: Goal & Context / Exact scope / Out of scope / Dependencies & upstream contracts by ID / Architecture compliance / Testing requirements / Integration expectations / Failure modes / Security / Project Structure Notes / References → empty Dev Agent Record), must give one agent everything WITHOUT redesigning the product. Stories in the same wave must not share files; wave B and 3.7 build on merged predecessors only; every PR leaves the repo coherent (§64).

Hard constraints to carry into every Epic 3 story:

- **The RunResult shape is the epic's keystone, and 3.3 owns it.** Four stories (3.4, 3.5, 3.6, 3.7) read it and two of them persist or render it. 3.3 must publish the complete shape — run id, epic, base/head SHAs, `RunOutcome`, stage timeline (per-stage status + durationMs), `GateResult[]`, `CriterionResult[]`, evidence references, provider usage, ISO-8601 UTC timestamps — **in writing during intent-sync, before wave B writes any code**, and land it in `src/domain/` where AD-1 keeps it pure. Wave B consumers must re-check the shape against merged source before building on it (Epic 2 lesson 5). No consumer may widen it in its own branch: an additive field is a message to 3.3's owner, or a follow-up.
- **AD-6 / AD-7 classification is the product promise, and it is testable.** `GateFailure` is a stage result, never a thrown exception; the aggregate stage is the only place stage results become a `RunOutcome`; `domain/verdict.ts` already implements the aggregation and must be **called, not re-implemented**; exit codes come only from `src/cli/exit.ts`. The three-way distinction (PASS 0 / FAIL 1 + `gateFailed` / InfraError 3) must be pinned by a test per story that can produce each, not asserted in prose. A gate command that cannot start is InfraError, not FAIL — that specific test belongs to 3.4.
- **AD-8 lifecycle (this epic's highest-risk area — tested, not asserted):** 3.2 extends the *existing* `ProcessRunner` port and its execa implementation additively (`ProcessRunOptions` is a single options object precisely so this is possible; 2.4/2.5's call sites must keep compiling and their tests keep passing). Every spawned child gets its own process group; the pgid and worktree path are written **and fsynced into `manifest.json` before the resource is used**; teardown is SIGTERM → grace → SIGKILL on the **group**; `specwitness clean` (and `--all`) replays unreaped manifests. The **forking-grandchild reaping gap is explicitly this story's to close** (retro §5 debt 1) — its integration test must spawn a shell that forks, kill at teardown, and assert the grandchild is gone, on macOS, with the Linux half declared pending-owner if no Linux host exists. Add a test that leaves **no orphaned processes behind**: Epic 2's own timeout test leaked nine, and a suite that leaks is a suite nobody will run twice.
- **The source repo is read-only.** Worktree add/remove are the only git writes; nothing is written into the project working tree; `git status` in the source repo is byte-identical before and after every run, including a run killed with -9 (3.1 owns that integration test; 3.7 owns the end-to-end version). A missing `--head` ref is a named error with `HINT: git fetch` — SpecWitness never fetches implicitly. An ambiguous or unresolvable root refuses rather than guessing.
- **AD-3 stays intact under execution.** Gate commands are `DeclaredCommand`s from Project Config and nothing else; provider binaries and `git` remain trusted-tool spawns with fixed argv (never `DeclaredCommand`s); no string from a contract, a provider, or a plan is ever interpolated into a shell. `tests/unit/config/boundary-scan.test.ts` already enforces the brand boundary — say which story extends its scope to `src/pipeline/**` (recommend 3.4, the first module to execute a declared command).
- **AD-11 renderers are pure over the model.** 3.6's terminal and JSON renderers compute nothing that is not already in `RunResult`; `--json` puts the JSON document and nothing else on stdout (everything human goes to stderr) and its content is **byte-equal to the persisted `result.json`** — make that an assertion in a test, not a claim, and name which story owns it (recommend 3.5 persists, 3.6 renders, and the byte-equality test lives with 3.6 while 3.7 proves it end-to-end through the built binary). The JSON snapshot test must exist even though CI cannot run it (retro §9).
- **AD-10 evidence: capture-time redaction, decided explicitly.** Epic 3 introduces the first real evidence — gate stdout/stderr. `domain/evidence.ts` and its redacting constructors are listed in the spine but do not exist yet. **Assign them to 3.3** (wave A, alongside the RunResult shape) so 3.4 has typed `gate`/`command` evidence to produce and 3.6 has a bounded thing to render, and say so in 3.3's Project Structure Notes and in 3.4/3.6's dependencies. Bounded inline output plus the full file in the run directory, with a truncation marker pointing at it (Q49).
- **`report` is an existing stub with a documented handover.** 3.5 grows `src/cli/commands/report.ts` into full rendering (`report <epic>` = latest run, `report <run-id>`, `--json`), reusing the string-returning `renderRun` seam that story 1.6 left for exactly this. It must not re-execute anything (Q52), and it must not become a second renderer — it calls 3.6's. Sequence that honestly: 3.5 and 3.6 are in the same wave, so specify the seam (3.6 exports the renderers; 3.5 imports them) in intent-sync and keep the two stories' test files disjoint.
- **RunStore stays the sole writer.** 3.5 adds the atomic stage-and-rename finalize for `result.json` **inside `src/infra/run-store.ts`**, which the file's own header reserves for it; 3.2 adds manifest lifecycle updates in the same file. Two stories touching one file in the same-ish window is exactly the coordination Epic 2 handled well by making the two suites share zero `expect()` — 3.2 is wave A and 3.5 is wave B, so the sequence is safe, but say it in both stories.
- **Schema registration:** 3.5 registers `jsonReport` in `SCHEMA_VERSIONS` (the registry's comment already reserves it) and owns the `result.json` zod schema in `src/schemas/`. Populating the manifest's reserved arrays does **not** bump `runManifest` — the file says so; do not let 3.2 bump it.
- **Layering:** `src/pipeline/**` (3.3) and `src/report/**` (3.6) are new application-layer directories with **no dependency-cruiser rule yet**. Each story adds its own rule modelled on `ingest-core-only` and pins it in `tests/unit/dependency-rules.test.ts` — which 2.8 made concurrency-safe, so extend it without reintroducing fixed-name scratch modules under `src/`.
- **Testing without a real project:** gates execute against **fixture repos built in a tmp dir by the test** (a git repo with real commits, a `package.json` whose scripts pass or fail deterministically), never against this repository and never against the network. Domain and pipeline suites spawn zero subprocesses (faked stages, `FixedClock`/`SequenceIds` from `tests/fakes/ports.ts`). The Golden Corpus fixtures proper are Epic 6 — 3.7 uses inline fixtures and says so.
- **Inherited defects (decide, don't inherit silently):** retro §5a lists two P2 defects in merged code, both of which touch this epic's path. Recommended: fix (ii) `src/authoring/contract-file.ts` post-rename durability reporting inside **3.7** (whose integrity stage reads the file that writer produces) or as a named follow-up, and leave (i) `fenceMask` to a follow-up story since no Epic 3 story ingests markdown. Whatever you choose, state it in the story text or raise it as a question — do not leave it unaddressed and unmentioned.
- Stack pinned exactly per the spine (Node ≥22.12, TS 6.0.x, pnpm 11, commander 15, zod 4.5, execa 10, yaml 2.9, vitest 4.1, tsup 8.5, dependency-cruiser 18). TDD per story; Conventional Commits; ISO-8601 UTC everywhere; `ERROR:` + `HINT:` on stderr; agent-callable commands prompt-free (`verify` and `report` assume no TTY; `clean` is prompt-free too — it is exactly the command an operator scripts).

### Cohort & Dependencies section (harness self-configuration) — required in every story

The whole cohort launches at once and serializes via the harness's `--depends-on` mechanism. Facts to design around — three of them are corrections earned in Epic 2:

- Agents self-configure after reading the spec: `~/.terminal-agents/scripts/set-cohort.sh <peers>` and `~/.terminal-agents/scripts/set-depends-on.sh <agent>:done` (`done` = PR **merged**, detected by the poller; never use `pr-opened`).
- **Put the `set-depends-on.sh` instruction AFTER the intent-sync step, not before it** (harness defect H-10): the intent gate blocks `set-depends-on`/`agent-wait` until cohort intent-sync completes, and five Epic 2 agents took block loops in their first minutes because the spec told them to run it immediately. Word it as: complete intent-sync first, then set the marker.
- **One `waiting_for` marker per agent** — successive `set-depends-on.sh` calls REPLACE, they do not accumulate. Multi-predecessor stories must wait **sequentially**: set the first marker; when unblocked, set the next; and **before setting any marker, check whether that predecessor's PR is already merged** (`gh pr list --base epic/3-isolated-deterministic-verification --state merged --search "<task-id>"`, or the peer's `status.json` `phase: done`) and skip already-satisfied ones.
- **Rebase after an unblock, with the right command.** Immediately after each unblock: `git fetch origin`, then — because this project **squash-merges** — `git rebase --onto origin/epic/3-isolated-deterministic-verification <sha-of-your-last-merged-commit>`, not a plain `git rebase` (H-4: a plain rebase conflicts on every already-merged commit). And: **if your own PR is already merged, a rebase nudge is not for you — ignore it** (H-3; three Epic 2 agents were reopened from `done` and one did 45 minutes of work that had nowhere to land).
- Seven stories exceed the harness's six default names, so the launch will pass `--names`; assume this mapping and tell agents to depend on the *story-id holder* if `tmux-overview.sh` shows otherwise:
  `alice=3.1, bob=3.2, pamela=3.3, arnold=3.4, rambo=3.5, chuck=3.6, dolph=3.7`.
- Dependency graph to encode (derive from the actual imports you specify; adjust if your file ownership changes it):
  - 3.1, 3.2, 3.3 — no depends-on (wave A). **3.3 is the critical path for the entire epic** — every wave-B story imports its RunResult, evidence and stage types. Keep 3.3's scope tight and tell its agent to PR early; nothing in wave B can start before it merges.
  - 3.4 → `pamela:done` (3.3: pipeline, stage results, RunResult, evidence types), then `bob:done` (3.2: process-group spawning for gate commands).
  - 3.5 → sequentially `pamela:done` (RunResult + schema), then `bob:done` (3.2's RunStore manifest changes, same file).
  - 3.6 → `pamela:done` (renderers are pure over 3.3's RunResult; nothing else).
  - 3.7 → sequentially `arnold:done` (3.4 gates), then `rambo:done` (3.5 persistence), then `chuck:done` (3.6 renderers), then `alice:done` (3.1 worktree) — 3.7 is the integration story and needs all of wave B plus the worktree.
- **File ownership must be disjoint within a wave.** The two known collisions to design around: `src/infra/run-store.ts` (3.2 manifest lifecycle, 3.5 result finalize — different waves, state it in both) and `src/cli/commands/report.ts` (3.5 only). If your scope decisions create another, resolve it by ownership in the spec, not by hoping intent-sync catches it.

## Step 4 — Close out

1. Update `docs/implementation-artifacts/sprint-status.yaml`: the seven Epic 3 stories → `ready-for-dev`, `epic-3` → `in-progress` (the create-story workflow's own status step); then run `uv run .claude/skills/bmad-sprint-planning/scripts/sprint_plan.py validate --status-file docs/implementation-artifacts/sprint-status.yaml` and require `"valid": true`.
2. **Amend the one roadmap line that is now wrong** (Epic 2 retro action item B2, and the only planning-artifact edit you are authorized to make): `docs/planning-artifacts/roadmap.md`'s EPIC 3 wave-A entry should read `infra/process-runner` **lifecycle extension** rather than implying 3.2 creates the file. Change nothing else in roadmap.md; note the edit in your final report.
3. Update the stale "Current phase" paragraph in `CLAUDE.md` to state: Epic 1 merged 2026-08-31 (`b5377b0`), Epic 2 merged 2026-08-31 (`fbdd359`, 8 stories including follow-up 2.8), production code under `src/` now covers CLI, domain core, config, ingest, contract authoring and providers, Epic 3 stories authored; retrospectives and the harness-defect record live in `docs/findings/`. Keep everything else in CLAUDE.md as is.
4. Commit with a Conventional Commits message (e.g. `docs: add Epic 3 implementation-ready story files`) and **push to `origin/master`** — the harness's spec-on-origin guard refuses to launch a cohort whose specs are not on origin.
5. Report: created files; the exact launch command the operator should run, which is expected to be
   ```
   ~/.terminal-agents/scripts/launch-task.sh --project specwitness \
     --supervised --supervisor-stage 3 \
     --base epic/3-isolated-deterministic-verification --max 7 \
     --names alice,bob,pamela,arnold,rambo,chuck,dolph \
     3.1-revision-resolution-isolated-worktree \
     3.2-process-groups-run-manifest-specwitness-clean \
     3.3-staged-pipeline-state-machine \
     3.4-deterministic-gates-execution \
     3.5-runresult-persistence-specwitness-report \
     3.6-terminal-json-renderers \
     3.7-end-to-end-gates-only-verify
   ```
   (flag anything in your stories that would change it); any ambiguities as questions at the end (SAVE QUESTIONS rule) — at minimum:
   - confirm the **evidence-module placement** (recommended: `domain/evidence.ts` lands in 3.3 rather than being deferred to Epic 4);
   - confirm the **renderer/report seam** (3.6 exports, 3.5 imports) and that 3.5 does not become a second renderer;
   - confirm the disposition of the **two inherited §5a defects**;
   - confirm whether **`meta.provenance.model` / `providerCliVersion` wiring** (retro action B6) is an Epic 3 story, a follow-up, or out of scope here — it is a genuine open owner decision and none of the seven stories obviously owns it;
   - state plainly which Epic 3 exit criteria **no agent can satisfy** (Linux process-group semantics, any CI-executed step, the JSON snapshot test running in CI) so they are reported pending-owner rather than passed;
   - and confirmation that no planning artifacts were modified other than the single roadmap line in Step 4.2, and that no production code was written.
