# Story 6.10: Evidence-kind expectations in the corpus format

Status: ready-for-dev

**SCHEDULED INTO EPIC 6 WAVE 3 by owner decision, 2026-09-05 — WITH TASK 5 REMOVED FROM SCOPE.**

This spec was authored unassigned and unscheduled, and said that which wave it lands in was the owner's call. The call is made: **you are the third agent of wave 3, the closing wave**, alongside 6.6 (defect attribution & summary) and 6.7 (docs & packaging).

**⚠️ TASK 5 IS OUT OF SCOPE. Do not retro-fit `evidence` expectations onto story 6.2's merged fixtures.** The severability line this spec itself proposes — *"drop Task 5, keep ACs 1-5"* — is the scope you are given, and the reason is specific rather than cautious:

`fixtures/corpus/` is **not** in `REVIEW_IGNORE_PATHS`, so editing a merged `expected.json` re-stales your `codex_review` and `security_check`. Wave 2 hit exactly that mechanism twice, both times on story 6.9: a verdict recorded seconds after a push belonged to the **previous head**, the harness then discarded the new auto state because a recorded verdict already existed, and the result was a `codex_review` field reading `addressed` that was confidently wrong **while satisfying all three Stage-4 conditions**. Nothing in the merge gate can see that. In a wave that also writes the epic retrospective and opens the integration PR, the owner chose not to run that risk for a retro-fit that can be done at any time afterwards.

**So: deliver ACs 1-5 in full.** The format capability, both comparison modes, the version decision, and the proving fixture that is seen red. **Touch no fixture that story 6.2, 6.3 or 6.4 merged.** Your own proving fixture is new and yours.

**Say in your PR body that Task 5 was removed by owner decision and remains available as follow-up work** — it is a genuine improvement to story 6.2's AC1 pinning, and it should not be lost just because this story shipped without it.

**Its blocker has cleared.** This spec was authored `backlog` and BLOCKED ON STORY 6.2, because Task 0's last item and the whole of Task 5 read and modify story 6.2's *merged* fixtures. **Story 6.2 merged on 2026-09-05 as `dfb9995` (PR #68)**, so both dependent tasks are now satisfiable and the status moves to `ready-for-dev` as this header said it would. The `[BLOCKED ON 6.2]` markers below are left in place as a record of why the tasks are shaped that way; they no longer block anything.

Everything else — the schema key, the comparator branch, and the proving fixture that satisfies AC5 — depends only on story 6.1, which is merged. If the owner ever wants the format capability sooner than 6.2's fixtures can use it, the story is severable at exactly that line: drop Task 5, keep ACs 1-5, and say so in the PR body.

## Cohort & Dependencies (harness self-configuration)

```
master
  └── epic/6-golden-corpus-and-scorecard
        ├── wave 1 (merged):  6.1                          the corpus runner and expected.json format
        ├── wave 2 (merged):  6.2, 6.3, 6.4, 6.5, 6.8, 6.9 fixtures, scorecard, prompt helper, browser CI
        ├── wave 3 (closing): 6.6, 6.7, 6.10               ← you are here
        └──                   + retrospective + integration PR
                                                          │
        epic branch ──── one PR ──────────────────────────┴──→ master  (OWNER'S CLICK)
```

**Your peers are 6.6 (defect attribution & summary) and 6.7 (docs & packaging).** Run `~/.terminal-agents/scripts/set-cohort.sh` with them. **You joined this wave after it launched**, so intent-sync promptly and tell them what you own: `tests/corpus/expected.ts` and `tests/corpus/runner.ts`. Neither peer touches those — 6.6 creates the `scorecard` command surface, 6.7 owns the README, the integration guide and packaging. **You set no `set-depends-on` marker**: your blocker (6.2) merged in wave 2.

**Eight merged stories are your contract and every author is gone.** Read the epic journal (`~/.local/state/terminal-agents/epics/epic/6-golden-corpus-and-scorecard/journal.md`) — its wave-2 entry is the only account of what those six stories delivered, and under Stage 4 the owner never read one of those PRs.

Your PR targets **`epic/6-golden-corpus-and-scorecard`**, never `master`. **Your PR title becomes the squash subject and must be a valid Conventional Commit**; put the story id in a `Refs:` trailer, not the title.

**After any unblock or `epic-updated` nudge:** `git fetch origin`, then — this project **squash-merges** — `git rebase --onto origin/epic/6-golden-corpus-and-scorecard <sha-of-your-last-merged-commit>` (H-4). **If your own PR is already merged, ignore a rebase nudge** (H-3).

### ⚠️ Stage 4: your own sign-offs may be the last thing between your code and the epic branch

`SUPERVISOR_MERGE=1` and `SECURITY_GATE=1` are set for this project. The gate is **three factual conditions**: `codex_review` clean/addressed **and fresh**; `security_check` clean/addressed **and fresh**; the **rebase verdict** `clean` (or absent). The supervisor's judgement is **not among them**.

**Sign off from the actual review report, never from a status line.** Wave 2 recorded the auto-review classifier reporting *"no findings"* over **real findings twelve times across five agents** (harness item e3-A). Count `Review comment:` blocks and read the final conclusion; do not grep for `[Pn]` severity tags, because the reviewer's own prompt forbids them and the day it obeys, that grep passes everything silently.

**"Fresh" is a CONTENT rule, not a clock rule.** `REVIEW_IGNORE_PATHS` covers `docs/implementation-artifacts/*.md`, `docs/findings/*.md` and `sprint-status.yaml`. **`tests/` is NOT covered, and neither is `fixtures/corpus/`** — which is exactly why Task 5 was removed from your scope. **Sign off after your last code commit, not before.**

**⚠️ And the failure that bit wave 2 twice, both times on story 6.9:** a verdict recorded seconds after a push belonged to the **previous head**, the harness discarded the new auto state because a recorded verdict already existed, and a `codex_review` reading `addressed` was confidently wrong **while satisfying all three Stage-4 conditions**. Nothing in the gate can see this. **Check that the SHA your verdict was recorded against is your current head.**

**`gh pr merge --repo` and `gh pr edit --base` are blocked outright, at every stage, for every agent.**

## Provenance — why this story exists and who wrote it

**Written by the supervisor of `epic/6-golden-corpus-and-scorecard` (agent `superman`) on 2026-09-04, during wave 2**, with the owner's explicit agreement, under the follow-up-spec permission. It is not the output of a planning session and it does not appear in `docs/planning-artifacts/epics.md`.

**The evidence.** Story 6.2's agent (`alice`) reported the gap to the supervisor at 2026-09-04T15:39:50Z while reading her dependencies, before writing any fixture. The supervisor verified every claim independently, in source, in the same session:

- `tests/corpus/expected.ts:117-193` — `ExpectedOutcomeFileSchema` is `.strict()`, and its complete key set is `expectedVersion`, `fixture`, `why`, `proves`, `command`, `exitCode`, `outcome`, `criteria`, `stderrContains`, `stderrAbsent`. There is **no** field for evidence, and because the schema is strict, a fixture that adds one fails to load rather than being silently ignored.
- `compareOutcome` in `tests/corpus/runner.ts` compares the process exit code, the outcome object, criterion ids and statuses, and stderr substrings. **It does not read evidence at all.**
- The runner's only read of `document.evidence` is `nonLoopbackEvidenceTargets` (`tests/corpus/runner.ts:~1159-1180`), which extracts `.request.url` / `.url` to check hosts. It inspects *targets*, never *kinds*.
- The closed evidence union is in `src/schemas/result.ts:~257-263`: `http | browser | observation | command | gate | provider`.

Both files arrived on the epic branch in **PR #64** (story 6.1, merged as `d428664`).

**Why this is a new story and not a correction to 6.2.** Nobody owns `tests/corpus/expected.ts` in Epic 6 wave 2. Story 6.1 merged it and its agent is gone; 6.2, 6.3 and 6.4 own only their own fixture directories; 6.5 owns the scorecard; 6.8 owns `src/authoring/**`; 6.9 owns one CI job. Wave 3 is 6.6 (defect attribution) and 6.7 (docs & packaging), neither of which owns the corpus format either. Story 6.2's agent identified the gap, correctly declined to widen a merged contract from inside a fixture story, and is delivering her five fixtures against the format as merged with the limitation stated plainly in her PR body. **She is right to do so, and instructing her otherwise would have been the defect** — a fixture story that quietly widens the runner is how a wave of six agents ends up with six formats.

## The consequence today, stated exactly

Story 6.2's AC1 (`docs/planning-artifacts/epics.md:877-879`) reads:

> **Then** the expected verdict, failing criterion ids, and **evidence kinds** match `expected.json` exactly

**⚠️ CORRECTED 2026-09-04T19:38Z, before this spec was merged. The original version of this section said the third clause "is not, and cannot be" pinned without this story. That was wrong, and the correction is recorded rather than quietly edited away, because the wrong version is what the owner agreed to when approving this spec.**

Story 6.2 satisfied AC1's evidence-kind clause **without** this story and without widening the format. `verify --json` writes the terminal report to stderr, and its Evidence section names every member by kind, so the merged `stderrContains` field pins the kinds mechanically:

```
"stderrContains": [
  "  gate tests (pass, exit 0): node gates/tests.cjs",
  "  http POST http://127.0.0.1:<PORT:app>/orders -> 201",
  "  observation orders",
  "  command report (exit 0): node commands/report.cjs"
]
```

That is real mechanical pinning, it was seen red, and it shipped in PR #68. **The corpus is therefore not silent about evidence kinds, and this story is no longer needed to satisfy any acceptance criterion.**

### What still justifies this story, stated narrowly

The `stderrContains` route pins the **rendered text** of the evidence lines. That has two properties this story would not share:

- It is a **presence/subset** assertion. It cannot express "exactly these kinds and no others", so a run that starts producing an *additional* kind still passes.
- It is **coupled to the renderer**. It asserts what the terminal report printed, not what `document.evidence` contains. A kind that disappeared from the document while the renderer still printed a line — or a renderer format change — moves the assertion off its target.

This story proposes an `exact`-or-`subset` set comparison over the **structured** `document.evidence`, decoupled from formatting. That is a genuine improvement and it is why the story is still worth doing. **It is no longer urgent, it blocks nothing, and it must never be described as making an unsatisfiable AC satisfiable.**

Whoever picks this up: read story 6.2's merged fixtures first and treat the `stderrContains` assertions as the baseline you are strengthening, not replacing.

## Story

As the reader of a corpus fixture six months from now,
I want an `expected.json` to be able to pin the **kinds** of evidence a run produced, in the same hand-written, mechanically-compared way it already pins the verdict and the criterion statuses,
So that a change which quietly stops a verification surface from producing evidence turns a fixture red instead of passing unnoticed (FR-15, AD-6, story 6.2 AC1).

## Acceptance Criteria

**Not from `epics.md`** — this story was written by the supervisor during Epic 6 wave 2 (see Provenance). Its ACs are stated here in full and are authoritative.

1. **Given** an `expected.json` carrying an optional `evidence` expectation
   **When** the corpus runner compares a fixture's run against it
   **Then** the distinct `kind` values in the run's `document.evidence` are compared against the expectation, and a mismatch is a **fixture failure naming the kinds expected and the kinds observed** — not a warning, not a log line.
2. **Given** an `expected.json` that omits the `evidence` key entirely
   **When** it is loaded and compared
   **Then** it loads and passes exactly as it does today, asserting nothing about evidence — the key is **additive and optional**, so no merged fixture is *required* to change and none changes meaning by virtue of this story landing.

   This is a statement about the **format's backward compatibility**, not a prohibition on editing fixtures. Deliberately strengthening a selected fixture by *adding* an `evidence` expectation to it is exactly what Task 5 asks for and is permitted. What AC2 forbids is a fixture's meaning changing **without its author touching it** — the silent reinterpretation `tests/corpus/expected.ts:39-46` exists to prevent.
3. **Given** the expectation's `assertion` discriminator
   **When** it is `exact`, the observed set of kinds must equal the expected set; **when** it is `subset`, every expected kind must be present and additional kinds are permitted
   **And** `assertion` is **required whenever the `evidence` key is present**, so that a fixture can never be read as making the stronger claim when its author meant the weaker one.
4. **Given** the change to `ExpectedOutcomeFileSchema`
   **When** an author decides whether `EXPECTED_VERSION` must be bumped
   **Then** the decision is **recorded in the code with its reasoning**, consistent with `tests/corpus/expected.ts:39-46` (*"a change to the format is a visible, dated event rather than a silent reinterpretation of files nobody re-read"*) and with the additive-evolution posture of **ADR-008** — and whichever way it goes, a fixture written against the old version still loads or fails loudly, never silently reinterpreted.
5. **Given** the new comparison path
   **When** the story is delivered
   **Then** at least one hand-written fixture **proves it goes red on a kind mismatch** — planted, seen red, and recorded in the Verification log — because a comparison nobody has watched fail is exactly the green-for-nothing this corpus exists to catch.

## Tasks / Subtasks

- [ ] Task 0 (first, before any code): read the merged contract (AC: all)
  - [ ] `tests/corpus/expected.ts` **in full**, especially `EXPECTED_VERSION` (line 38-46) and the `.strict()` close at line 193.
  - [ ] `compareOutcome` in `tests/corpus/runner.ts` in full — how a problem is phrased, and how the `exact` / `subset` idiom already works for `criteria.assertion`. **Follow that idiom; do not invent a second one.**
  - [ ] `nonLoopbackEvidenceTargets` (`tests/corpus/runner.ts:~1159`) — the one existing reader of `document.evidence`, and the reason it reads targets rather than kinds.
  - [ ] `src/schemas/result.ts:~257-263` — the closed union, and every one of the six kinds.
  - [ ] `fixtures/corpus/README.md` and the merged fixtures — **there is no regeneration affordance for `expected.json` and there must never be one.**
  - [ ] Story 6.2's merged fixtures and PR body (**merged as `dfb9995`, PR #68** — the blocker has cleared), which state the limitation this story removes, and whose `stderrContains` assertions are **the baseline you are strengthening, not replacing**. **Read them; do not edit them** — Task 5 is out of scope.
- [ ] Task 1 (TDD — write the failing test first, watch it fail): the schema (AC: 1, 2, 3)
  - [ ] Add the optional `evidence` key to `ExpectedOutcomeFileSchema`, with `assertion` required inside it.
  - [ ] Prove a fixture **without** the key still loads and still means what it meant (AC2).
  - [ ] Prove an `evidence` object **without** `assertion` is refused at load time, naming the field.
- [ ] Task 2 (TDD): the comparator (AC: 1, 3)
  - [ ] Compare distinct kinds from `document.evidence`; phrase the problem in the existing house style, naming expected and observed.
  - [ ] Both `exact` and `subset` covered, in both directions (missing kind; extra kind).
- [ ] Task 3: the version decision (AC: 4)
  - [ ] Decide, implement, and **write the reasoning into the code** beside `EXPECTED_VERSION`.
  - [ ] Whichever way: prove the old-version behaviour is loud, not silent.
- [ ] Task 4: the proving fixture (AC: 5)
  - [ ] One hand-written fixture whose expectation pins kinds. **`expected.json` hand-written — never generated by the code under test.**
  - [ ] **Plant a mismatch, watch it go red, record the exact failure text** in the Verification log. Then unplant it.
  - [ ] Use a `runner-`-prefixed name or a number agreed with whatever fixture stories are live, so it collides with nobody.
- [ ] ~~Task 5: retro-fit `evidence` onto story 6.2's merged fixtures~~ — **REMOVED FROM SCOPE by owner decision 2026-09-05.** Do not do this. Record in your PR body that it was removed and remains available as follow-up.
- [ ] Task 6: gates and handover
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm depcruise` green; the corpus suite green.
  - [ ] Update your row in `docs/implementation-artifacts/sprint-status.yaml`. **Your row only.**

## Dev Notes

### Goal & Context

The corpus exists to make silent regressions loud. It currently pins what a run *concluded* — verdict, exit code, criterion statuses — and is entirely silent about what a run *observed*. Those are different failures. A change that leaves the verdict correct while a verification surface quietly stops producing evidence is invisible to every fixture merged today, and it is precisely the shape of defect this project has now hit twice at other altitudes: Epic 4's retrospective named it at the criterion level (*"every new way for a criterion to produce no attempts is a new way to reach green-for-nothing"*), and Epic 6's story 6.9 exists because it recurred at the CI level, where five browser suites self-skipped into a green check.

This story closes the same hole one level down, in the artifact that is supposed to be the product's own witness.

### Exact scope

- `tests/corpus/expected.ts` — one optional key, its inner shape, and the version decision.
- `tests/corpus/runner.ts` — one comparison branch inside `compareOutcome`.
- One new proving fixture under `fixtures/corpus/`.
- ~~`evidence` expectations added to already-merged fixtures~~ — **out of scope** (Task 5 removed).
- The unit tests for all of the above.

### Out of scope (do NOT build)

- **Any change to `src/`.** This is a test-side format. The product's `EvidenceSchema` is correct as it stands and is not yours to touch.
- **Pinning evidence *contents*** — URLs, bodies, exit codes of observed commands, screenshots. Kinds only. Contents are a much larger design question about normalisation and secrets, and it is not this story's.
- **A regeneration affordance of any kind.** Not a script, not a flag, not a `--update` mode. It is forbidden by story 6.1's merged tests, stated in three module headers and in `fixtures/corpus/README.md`, and a red fixture is a question to answer by reading the run directory the failure names.
- **Relaxing `.strict()`** anywhere. The key is additive and optional; strictness stays.
- **Renumbering any existing fixture.** Four merged ACs across Epics 4 and 5 name fixtures by number.

### Dependencies & upstream contracts (by ID)

- Story **6.1** (merged, PR #64, `d428664`) — the corpus runner, `ExpectedOutcomeFileSchema`, `EXPECTED_VERSION`, the hermetic e2e harness. **Your contract.**
- Story **6.2** (Epic 6 wave 2) — the fixtures whose AC1 this story lets someone finally satisfy in full. **This is a BLOCKING dependency for Task 0's last item and for the whole of Task 5**, both of which read or modify 6.2's *merged* fixtures. This story stays `backlog` until 6.2 merges.
- **ADR-008** (`docs/adr/ADR-008-persisted-envelope-strictness.md`) — additive optional keys over relaxed strictness. **Note carefully: ADR-008 binds `src/schemas/versions.ts` and persisted product artifacts. `expected.json` is a test-side format and is NOT among them.** Follow the ADR's *posture* because it is the house style; do not claim it as binding, and do not import its version-skew `InfraError` machinery into a test harness where exit codes mean nothing.

### Architecture compliance

- **AD-3** — nothing here reaches a shell. The comparator is pure.
- **AD-6** — evidence belongs to a run document; you read it, you never synthesise it.
- Verdicts stay mechanical. A kind comparison is set arithmetic and must remain so.

### Testing requirements

- **TDD, and the failing test must actually be seen red.** AC5 is not satisfied by a test that has only ever been green.
- Tests spawn no real `claude` / `codex` and touch neither `~/.claude/` nor `~/.codex/`.
- The proving fixture's `expected.json` is **hand-written**.

### Failure modes (what "wrong" looks like here)

- **The comparison passes vacuously** — an empty expected set, or a fixture whose run produces no evidence at all, quietly satisfying `subset`. Guard it and test the guard.
- **`assertion` made optional with a default.** A defaulted discriminator is exactly the silence-read-as-a-claim this format forbids; AC3 requires it present.
- **Kinds compared as an ordered array.** They are a set. Two fixtures differing only in evidence order must not disagree.
- **A stringly-typed kind list.** The six kinds are a closed union in `src/schemas/result.ts`; a typo in a fixture must be caught at load, not silently never-matched.
- **Retro-fitting merged fixtures without saying so** (Task 5) — and then being surprised that `codex_review` and `security_check` went stale, because `fixtures/corpus/` is **not** in `REVIEW_IGNORE_PATHS`.

### Security

Low surface: no subprocess, no network, no credential path. The one thing to watch is that a failure message naming expected-vs-observed kinds must not start quoting evidence *contents* — kinds are safe to print, request bodies and provider payloads are not.

### Project Structure Notes

Everything lands under `tests/corpus/` and `fixtures/corpus/`. No `src/` file is touched, so no dependency-cruiser boundary is engaged.

### References

- Epics: **not in `epics.md`** — this story's ACs are in this file (see Provenance).
- PRD: FR-15, NFR-6
- Spine: AD-3, AD-6
- ADRs: **ADR-008** (posture, not binding — read the caveat above), ADR-002
- Merged code: `tests/corpus/expected.ts`, `tests/corpus/runner.ts`, `src/schemas/result.ts:~257-263`, `fixtures/corpus/README.md`
- Epic 4 retro: `docs/findings/epic-4-retro-2026-09-02.md` §2 observation 2 — *green for nothing*, the defect shape this story addresses
- Epic 6 wave 1 journal entry — the merged-format facts, and the `REVIEW_IGNORE_PATHS` wrinkle

### Gates you cannot satisfy — report as pending-owner, never as passed

- **CI** is **not** on this list. It executes as of `fa2647b`. Run it; report both `ubuntu-latest` and `macos-latest`.
- **Windows.** Out of scope.
- **Real `claude` / `codex` invocations.** Forbidden for the corpus suite by story 6.1's AC1.
- **npm publishing.** Not this story.
- **The merge clicks.**

## Dev Agent Record

### Context Reference

- Story spec: `docs/implementation-artifacts/epic-6-golden-corpus-and-scorecard/6.10-corpus-evidence-kind-expectations.md`
- **Unassigned at authoring time.** Fill in the wave and cohort you actually ran in.

### Agent Model Used

_(fill in)_

### Debug Log References

_(fill in)_

### Verification log — what was planted, and what went red

_(fill in — REQUIRED, and AC5 depends on it: the planted kind mismatch and the exact failure text it produced)_

### The version decision

_(fill in — REQUIRED: bumped or not, and the reasoning, matching what you wrote into the code)_

### Merged fixtures retro-fitted, and those deliberately left alone

_(fill in — REQUIRED, even if the answer is "none, and here is why")_

### Completion Notes List

_(fill in)_

### File List

_(fill in)_
