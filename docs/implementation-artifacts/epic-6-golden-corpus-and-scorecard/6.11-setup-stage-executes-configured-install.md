# Story 6.11: The setup stage executes the configured install command

Status: ready-for-dev

**UNASSIGNED AND UNSCHEDULED.** No wave owns this story. It was written outside the normal planning path — see "Provenance" below — and which wave, or which epic, it lands in is the owner's call. Writing the spec scheduled nothing.

**It is numbered `6.11` because it was found during Epic 6 and the epic-6 directory is where this repository puts supervisor follow-ups (story 6.10 set that precedent on 2026-09-04). The number records where it was found, not where it belongs.** The gap itself is inherited from Epic 4: the placeholder it replaces names Epic 4 as the story that fills it. If the owner would rather this land as an Epic 7 story or a re-opened Epic 4 story, renumber it — nothing in the content depends on the number, and the sprint-status row is the only other place it appears.

**Nothing blocks this story.** Every file it touches is merged and stable, and no story in Epic 6 wave 3 (6.6, 6.7, 6.10) touches any of them.

## Provenance — why this story exists and who wrote it

**Written by the supervisor of `epic/6-golden-corpus-and-scorecard` (agent `superman`) on 2026-09-05, during wave 3**, with the owner's explicit agreement, under the follow-up-spec permission. It is not the output of a planning session and it does not appear in `docs/planning-artifacts/epics.md`.

**The evidence.** Story 6.7's agent (`bob`) reported the gap to the supervisor at 2026-09-05T12:18:13Z while writing the repository's first README — he had to describe what `setup.install` does to a first reader, and found that it does nothing. He called it the most consequential thing he found and noted it is in no epic's findings. **The supervisor verified every claim independently, in source on the epic branch, in the same session, before asking the owner:**

- **The config schema accepts it.** `src/config/schema.ts:73` defines `setupSchema`; `src/config/schema.ts:241` mounts it as `setup: setupSchema.default({})`. `src/config/types.ts:22` exports `SetupConfig` from it.
- **`doctor` validates it and tells the user it is fine.** `src/cli/doctor/checks/commands-resolvable.ts:36-37`:

  ```ts
  if (config.setup.install !== undefined) {
    entries.push({ id: 'setup.install', command: commandText(config.setup.install) });
  ```

  So `doctor` resolves the install command's binary on PATH and reports it as a resolvable command.
- **`verify` never executes it.** `src/pipeline/stages/setup.ts` is eighteen lines and its whole body is:

  ```ts
  export function createSetupStage(): Stage {
    return createPlaceholderStage('setup', 'Epic 4 runs the configured install command');
  }
  ```

  Its header reads *"The `setup` stage — PLACEHOLDER. **Filled by Epic 4.** It runs the Project Config's `setup.install` command inside the worktree, so that gates and probes execute against installed dependencies."*
- **`createPlaceholderStage` returns `stageOk(...)`** (`src/pipeline/stages/placeholder.ts`), so the stage is `ok` in the timeline and the pipeline continues to `gates`.
- **Epic 4 merged on 2026-09-03 (`23873ed`, PR #54) and did not fill it.** The placeholder above is the state of the file on `epic/6-golden-corpus-and-scorecard`, which contains all of Epic 4 and Epic 5.
- **`setup` is stage 4 of 11**, immediately before `gates` (`src/domain/stage.ts`, `STAGE_NAMES`). The position is already correct; only the body is missing.

**Why this is a new story and not a correction to anyone.** Nobody owns it. `src/pipeline/stages/setup.ts` was created by story 3.3 as one of seven deliberate placeholders and explicitly excluded from story 3.4's scope — the file says so itself: *"Not story 3.4's, despite sitting next to the gates stage: the config carries `setup.install` and this stage is named for it, but it is outside 3.4's acceptance criteria and nobody has been assigned it."* Epic 4 was named as the filler and shipped seven stories without it. Epic 6 wave 3 is 6.6 (scorecard command surface), 6.7 (docs & packaging) and 6.10 (corpus expectation format); none of them owns a pipeline stage. **bob was right to report it rather than fix it**: a documentation story that quietly implements a missing pipeline stage is exactly the scope creep this project's spec template warns about, and his README correctly documents what is built rather than what was intended.

## The consequence today, stated exactly — and stated fairly

A user writes this in `.specwitness/config.yaml`:

```yaml
setup:
  install: pnpm install --frozen-lockfile
```

`specwitness doctor` reports the command as resolvable. `specwitness verify` then creates the isolated worktree, records a `setup` stage with status `ok`, and runs the gates **against a worktree in which nothing was installed**. On a fresh worktree of a typical Node project, `pnpm test` in the gates stage then fails because `node_modules` does not exist — and a gate that says no is a **product FAIL, exit 1** (`src/domain/stage.ts`: `failed` — *"ran and produced a PRODUCT-relevant negative outcome"*). **A missing install is an environment problem being reported as the project's code being wrong. That is the exact inversion this product exists to prevent** (`CLAUDE.md`: *"Infra failures are never reported as product FAIL"*).

**Two things keep this from being silent, and the spec states them rather than overselling the defect:**

1. The placeholder records a timeline entry reading `not implemented yet — Epic 4 runs the configured install command`, and that detail is rendered and persisted. A reader of the terminal report or `result.json` **can** see that the setup stage did nothing. `src/pipeline/stages/placeholder.ts` says this is deliberate: *"a run of today's pipeline says plainly which parts of verification are not built yet instead of implying they passed."*
2. Every corpus fixture and every existing test passes today, because none of them declares `setup.install` — the fixtures install nothing and the gates they run need nothing installed.

**So the defect is not "a silent green stage". It is that `doctor` and `verify` disagree about whether a config key is live**, and the user is told by the command whose whole job is pre-flight validation that the key is fine. A user who reads `doctor` and does not also read the setup line of every run report gets gates run against an uninstalled tree. **The first reader of the new README is the author, dogfooding this on a real epic in Epic 7** (`docs/planning-artifacts/roadmap.md`), and a real project that declares an install is the normal case, not the edge case.

## Story

**As a** developer running SpecWitness against a project whose gates need installed dependencies,
**I want** the `setup` stage to actually run my configured `setup.install` command inside the verification worktree,
**so that** my gates and probes execute against a working tree, and a failed install is reported as an infrastructure error rather than as my code failing.

## Acceptance Criteria

Authored for this story; there is no `epics.md` entry to expand. **Expand, never weaken.**

1. **Given** a Project Config declaring `setup.install`
   **When** `specwitness verify` runs
   **Then** that command is executed inside the verification worktree, after the `worktree` stage and before the `gates` stage, and the `setup` timeline entry reports it ran — with the command, its exit code and its duration.
2. **Given** a Project Config declaring **no** `setup.install`
   **When** `specwitness verify` runs
   **Then** the `setup` stage is `ok`, records that nothing was configured, executes no subprocess, and the run is byte-identical in outcome to today's.
3. **Given** a `setup.install` command that **fails** (non-zero exit), is **not resolvable** on PATH, or **times out**
   **When** `specwitness verify` runs
   **Then** the stage status is **`error`**, the run ends with an `{infraError}` outcome and **exit 3 — never exit 1**, the pipeline stops before `gates`, and the run still reaches `aggregate → persist → teardown`. The error carries `ERROR:` + `HINT:` naming the command and the config key.
4. **Given** the same config
   **When** `specwitness doctor` and `specwitness verify` are both run
   **Then** they agree about `setup.install`: a key `doctor` validates is a key `verify` executes. **A test pins this agreement directly** — it is the actual defect this story closes, and it must not be provable only by reading two files.
5. **Given** a corpus fixture declaring `setup.install`
   **When** the corpus suite runs
   **Then** a hand-written `expected.json` pins the install having run, and a second fixture pins the failed-install path producing exit 3 with the gates stage `skipped` rather than `failed`.

Clarifications (detail only, no weakening):

- **AD-3 is the invariant that governs this whole story.** `setup.install` is a Project-Config-declared command and therefore may reach a shell; nothing else may. **Reuse the existing command machinery — do not write a second one.** `src/pipeline/stages/gate-command.ts` already exports `splitCommandLine`, `hasUnterminatedQuote`, `usesUnsupportedEscaping` and `hasGluedExecutableSuffix`, and the gates stage already resolves a bare name on PATH and distinguishes "not installed" from "file not found" (`src/pipeline/stages/gates.ts:293-326`). A second, subtly different command path is a security finding, not a convenience.
- **Read the gates stage before you write anything.** It is the merged, reviewed answer to every question this story raises — timeout handling, process groups, PATH resolution, the `ERROR:`/`HINT:` shape, and how a command's output becomes evidence. `src/pipeline/stages/services.ts` and `src/pipeline/stages/data.ts` are two more precedents for running a configured command; `data.ts:127` records why a data command's timeout is five minutes and reasons about it explicitly. **Pick your install timeout the same way — with a stated reason, not a guessed number.** An install is legitimately slower than a gate; the gates stage chose fifteen minutes (`gates.ts:110`) *because* real gates are `pnpm install` plus a test run, which is itself evidence about the right order of magnitude.
- **The `failed` / `error` split is the point.** `src/domain/stage.ts` states it: *"Collapse them into one 'bad' value and a branch that simply does not build starts reporting as 'the environment is broken, retry' — after which a retry merges it."* This story is the mirror image — an install that did not happen must never surface as the branch being wrong. **There is no case in which a `setup.install` failure yields exit 1.**
- **You add no new exit code and no new stage.** `src/cli/exit.ts` is the single table (0/1/2/3/64, ADR-002) and `STAGE_NAMES` is frozen at eleven. The `setup` name and its position already exist; you are replacing a placeholder body, not extending a sequence.
- **Process groups and teardown already exist and you must use them, not re-invent them.** Story 3.2 shipped process-group handling and `specwitness clean`; an install that spawns children and is then interrupted must not leak them. Read that code before spawning anything.
- **Do not remove the placeholder mechanism.** Six other stages still use `createPlaceholderStage` and its contract — *never fails, records which story fills it* — is load-bearing for them. You are replacing one call site.
- **`doctor` may need no change at all, and that is a legitimate outcome.** AC4 asks for agreement, and today `doctor` is the half that is already right. Verify it, pin it with a test, and say in the PR body that you changed nothing there rather than changing something to look busy.
- **Determinism and evidence.** Whatever the install stage records must satisfy AD-11: `--json` and the terminal report carry the same facts, and timestamps are ISO-8601 UTC. Install output is high-volume and machine-specific — **decide deliberately how much of it is persisted**, and say so, because `result.json` is a durable artifact and an install log can be megabytes. Absolute home paths must not leak into evidence.
- **The corpus fixtures in AC5 carry hand-written `expected.json`** — never generated by the code under test — and `fixtures/corpus/` is **not** in `REVIEW_IGNORE_PATHS`, so a fixture edit re-stales `codex_review` and `security_check` exactly as a `src/` edit does. Land the fixtures, then sign off, in that order.
- **The non-Node fixture (story 6.4) is the interesting AC5 case.** It has no `package.json` at any depth. An install story that only ever proves itself with `pnpm install` has proved something narrower than `setup.install` promises.

## Tasks / Subtasks

- [ ] **Task 0 — read before writing.** `src/pipeline/stages/setup.ts`, `placeholder.ts`, `gates.ts`, `gate-command.ts`, `services.ts`, `data.ts`, `src/domain/stage.ts`, `src/config/schema.ts`, `src/cli/doctor/checks/commands-resolvable.ts`. Record in the Dev Agent Record which precedent you followed for the command path and why. **Every claim about merged code cites `path:line` read this session** (Epic 5 action item e5-B).
- [ ] **Task 1 — the failing test first.** A test that declares `setup.install` and asserts it executed. **Watch it fail against the placeholder and record what red looked like** (TDD is per-story policy here; a test never seen red proves nothing).
- [ ] **Task 2 — implement the stage** over the existing command machinery. No second command path.
- [ ] **Task 3 — the no-config path** (AC2), pinned by its own test: no subprocess, `ok`, outcome unchanged.
- [ ] **Task 4 — the failure paths** (AC3): non-zero exit, unresolvable binary, timeout. Each pinned separately, each asserting **exit 3 and stage `error`**, and each asserting `gates` is `skipped`, not `failed`.
- [ ] **Task 5 — the doctor/verify agreement test** (AC4). This is the regression test for the defect itself.
- [ ] **Task 6 — two corpus fixtures** (AC5) with hand-written `expected.json`: one installing successfully, one failing. Include the non-Node target in your thinking; say in the PR body which you chose and why.
- [ ] **Task 7 — update the README and the harness integration guide** if story 6.7 has merged by then: they document `setup.install` as it behaves, and this story changes that behaviour. If 6.7 has not merged, say so in the PR body and leave the docs alone.
- [ ] **Task 8 — `pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm depcruise`**, all exit 0, summary line quoted verbatim in the PR body.
- [ ] **Task 9 — Dev Agent Record** filled: model, debug log references, the verification log (what was planted, what went red), completion notes, and a File List naming every NEW/UPDATE path.

## Dev Notes

### Goal & Context

Close the gap between what the config accepts and what the pipeline executes, for the one key where the difference turns an environment problem into a product FAIL. This is a small story with a high blast radius: it is the first time the `setup` stage does anything, and it sits directly upstream of the gates stage that produces the product verdict.

### Exact scope

`src/pipeline/stages/setup.ts` (REPLACE the placeholder body), its unit tests, the doctor/verify agreement test, two corpus fixtures, and — conditionally on 6.7 having merged — the two documents that describe `setup.install`.

### Out of scope (do NOT build)

- Any other placeholder stage. Six remain; they are not yours.
- Caching, incremental installs, lockfile inspection, or any judgement about *which* install command is correct. The user declared it; you run it.
- Changing `STAGE_NAMES`, `src/cli/exit.ts`, or the placeholder mechanism.
- Re-opening the AD-10 redaction question or the closing-pipe exit-1 issue — both are separately escalated and neither is this story's.

### Architecture compliance

AD-3 (only config-declared commands reach a shell), AD-7 (typed errors), AD-11 (`--json` and the terminal report carry the same facts), ADR-002 (the exit-code contract; infra never surfaces as product FAIL). The `failed`/`error` split in `src/domain/stage.ts` is the local statement of the last of these and this story is measured against it.

### Testing requirements

TDD, failing test first, seen red and recorded. Tests never spawn the real `claude`/`codex` CLIs and never read `~/.claude/` or `~/.codex/`. Subprocess behaviour is tested against harmless commands and fixtures. Corpus `expected.json` is hand-written. Assert on ISO-8601 UTC, not locale output.

### Failure modes (what "wrong" looks like here)

- **A failed install reported as exit 1.** The single worst outcome; it is the inversion the product exists to prevent.
- **A second command-splitting path** that diverges from `gate-command.ts` under quoting. A command-injection surface is a security finding in this repository, and the security sign-off gate is on for every PR.
- **An install whose children leak** when the run is interrupted — story 3.2's process-group work exists precisely for this.
- **A green `setup` stage that ran nothing** while claiming it did. The current placeholder is honest about doing nothing; a replacement that is less honest is strictly worse than what it replaced.
- **Megabytes of install log in `result.json`**, or an absolute home path inside evidence.

### Security

`setup.install` is user-supplied text that reaches a shell. It is *allowed* to, under AD-3, because it is Project-Config-declared — but it must travel the same audited path as gates, services and data commands, and no provider output may ever reach it. Evidence and logs must not carry secrets or absolute home paths. Run the security sign-off against your own diff before the PR (`SECURITY_GATE=1` on this project).

### Project Structure Notes

`src/pipeline/stages/setup.ts` already exists at the agreed path with the agreed export name (`createSetupStage`, wired in `src/pipeline/stages/index.ts:30`). You are replacing a body, not adding a file to the pipeline.

### References

- `src/pipeline/stages/setup.ts` — the placeholder and its header, naming Epic 4 as the filler.
- `src/pipeline/stages/placeholder.ts` — why the placeholders exist and what contract they keep.
- `src/pipeline/stages/gates.ts:110`, `:293-326` — timeout reasoning and PATH-resolution error shape.
- `src/pipeline/stages/gate-command.ts:283-351` — the command-splitting surface to reuse.
- `src/pipeline/stages/data.ts:127`, `:427-443` — a second precedent for a configured command, with its own timeout reasoning.
- `src/config/schema.ts:73`, `:241`; `src/config/types.ts:22` — where `setup.install` is accepted.
- `src/cli/doctor/checks/commands-resolvable.ts:36-37` — where `doctor` validates it.
- `src/domain/stage.ts` — `STAGE_NAMES` (setup is 4 of 11) and the `failed`/`error` split with its rationale.
- `docs/planning-artifacts/epics.md:499` — the frozen stage sequence, `resolve → integrity → worktree → setup → gates → …`.
- `docs/adr/ADR-002` — the exit-code contract.

### Gates you cannot satisfy — report as pending-owner, never as passed

- **npm publishing** and any real `claude` / `codex` invocation — standing owner-only gates on this project. This story needs neither: it makes no provider call.
- **The merge click.**
- If the owner has not by then decided the closing-pipe exit-1 question (a PASS can exit 1 under a closing pipe, an ADR-002 violation escalated during Epic 6 wave 2 and deliberately left unfixed), **note it and move on** — it is adjacent to your exit-code work and it is not yours to settle.

## Dev Agent Record

### Context Reference

<!-- to be filled by the implementing agent -->

### Agent Model Used

<!-- to be filled by the implementing agent -->

### Debug Log References

<!-- to be filled by the implementing agent -->

### Verification log — what was planted, and what went red

<!-- to be filled by the implementing agent -->

### The timeout decision

<!-- to be filled by the implementing agent: the number, and the reason it is that number -->

### Completion Notes List

<!-- to be filled by the implementing agent -->

### File List

<!-- to be filled by the implementing agent -->
