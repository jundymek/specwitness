# Story 6.7: Dogfooding readiness — docs & packaging

Status: ready-for-dev

## Cohort & Dependencies (harness self-configuration)

**You are in WAVE 3 of THREE — the closing wave — and Epic 6 runs against one long-lived epic branch** (owner decision 2026-09-04).

```
master
  └── epic/6-golden-corpus-and-scorecard
        ├── wave 1 (merged):  6.1                          ← the corpus runner
        ├── wave 2 (merged):  6.2, 6.3, 6.4, 6.5, 6.8, 6.9 ← fixtures, scorecard, prompt helper, browser CI
        ├── wave 3 (closing): 6.6, 6.7                     ← you are here
        └──                   + retrospective + integration PR
                                                          │
        epic branch ──── one PR ──────────────────────────┴──→ master  (OWNER'S CLICK)
```

**Your peer is **6.6 (defect attribution & summary)**.** Run `~/.terminal-agents/scripts/set-cohort.sh` with them. **Your ownership is the README, the harness integration guide and packaging** — `package.json` publish surface, `npm pack` / `publish --dry-run` verification, and the CI step that proves it. 6.6 owns the `scorecard` command surface. **The one place you meet is documentation**: you must document 6.6's commands, and 6.6 cannot hand you a surface it has not finished. **Intent-sync early, get their flags and output shape as soon as they have them, and write that section last.**

**Seven merged stories are your contract, and every author is gone.** Waves 1 and 2 ran in earlier cohorts with earlier supervisors. **Read their merged source and their PR bodies** — Epic 4's retro §3 lesson 4 established that the handover works *when the earlier wave writes its PR body as a handover document*, and Epic 5 confirmed it across two wave boundaries. **But read the source too**: Epic 5 retro §2 observation 5 records an agent building a security decision on a peer's merged code he remembered wrongly, while quoting the rule against exactly that.

### ⚠️ Your wave CLOSES the epic — the closure bookkeeping is your wave's business

Wave 3 is the last wave. After your two stories merge, the epic branch needs its **retrospective** (`docs/findings/epic-6-retro-<date>.md`, following the five that precede it) and its **integration PR to `master`** — the owner's click. Neither is a story AC, and both are your wave's responsibility to raise rather than assume. **Epic 5's retro §8 records that the supervisor brief still tells a closing supervisor to establish a `docs/findings/` precedent that has existed since 2026-08-31 and now holds six files** (action item e4-G / e3-I, fourth sighting). **Follow the existing files; ignore the brief on that point.**

**A reporting duty comes with Stage 4, and it lands hardest on the closing wave.** Epic 5 retro §2 observation 9: the owner never read a single story PR that epic, so *anything living only in a PR body reached nobody*. Findings must be carried out deliberately — into the window, the reports file, the journal and the retrospective. **Where that discipline slips, nothing catches it.**

Your PR targets **`epic/6-golden-corpus-and-scorecard`**, never `master`.

**After any unblock or `epic-updated` nudge:** `git fetch origin`, then — this project **squash-merges** — `git rebase --onto origin/epic/6-golden-corpus-and-scorecard <sha-of-your-last-merged-commit>` (H-4). **If your own PR is already merged, ignore a rebase nudge** (H-3).

**Your PR title becomes the squash subject and must be a valid Conventional Commit** (e3-H). Put the story id in a `Refs:` trailer, not in the title.

### ⚠️ Stage 4: your own sign-offs may be the last thing between your code and the epic branch

`SUPERVISOR_MERGE=1` is set for this project, and `SECURITY_GATE=1` with it. The gate is **three factual conditions**:

1. `codex_review` — `clean` or `addressed`, **and fresh**;
2. `security_check` — `clean` or `addressed`, **and fresh**;
3. the **rebase verdict** — `clean` (or absent).

The supervisor's judgement is **not among them**. It may decline and escalate, but never merge on its own say-so when a fact fails. **Sign off on what you actually verified, from the actual review report and not from a status line** (e3-A's mitigation).

**"Fresh" is a CONTENT rule, not a clock rule.** `REVIEW_IGNORE_PATHS` (`project.env:189`) covers `docs/implementation-artifacts/*.md`, `docs/findings/*.md` and `sprint-status.yaml`; committing only those after signing off keeps you fresh, and touching one line under `src/` or `tests/` re-stales you. **`CLAUDE.md` is deliberately NOT ignored** — it is the scope source of truth. **Nor is `fixtures/corpus/`**, added this epic: an `expected.json` is the file that says what a correct verdict looks like.

**The rebase verdict** is computed only when an agent was told to rebase and did. Absence passes. When present: `clean` merges; `touched` and `unknown` both refuse — and the fix is yours: **re-run codex review and the security check over the rebased branch and re-record both verdicts.**

**`gh pr merge --repo` and `gh pr edit --base` are blocked outright, at every stage, for every agent.**

## Story

As the first real user,
I want a README, a harness integration guide, and a verified publishable package,
So that SpecWitness can gate a real epic in the author's harness next (UJ-4; addendum §A).

## Acceptance Criteria

From `docs/planning-artifacts/epics.md` § "Story 6.7" (authoritative — expand, never weaken):

1. **Given** the repo
   **When** this story completes
   **Then** README covers install / quickstart / commands / exit codes / config reference; an integration guide documents harness usage (**absolute-path invocation, `--root`, `--json`, allowlisting `Bash(specwitness *)` in agent settings, supervisor §8a slot, no-TTY guarantees, timestamp freshness**).
2. **Given** `npm pack` / `npm publish --dry-run`
   **When** run in CI
   **Then** the tarball contains the **built CLI and templates only** (no fixtures/tests), **`npx specwitness --help` works from the packed tarball**, and the version is semver with a **`next` dist-tag plan documented**.

Clarifications (detail only, no weakening):

- **⚠️ THERE IS NO README IN THIS REPOSITORY.** Six epics, 3452+ tests, eleven source directories, and not one line telling a person what this is or how to run it. **You are writing the product's first words to its first reader**, and Epic 7's dogfooding procedure starts by following them. That reader is also, concretely, the author using this in his own harness within days.
- **Everything you document must be verified against the built binary, not against a spec.** This is the story where five epics of documentation drift comes due, and **an overclaim here is the defect class Epic 5's retro §2 observation 4 names as the one this project's tooling cannot catch**: four of one story's nineteen findings were *"sentences true of the case in front of the author and false in general"* — the code was right and the prose was wrong, and only reviewers caught it. **Run every command you document. Paste real output. If a flag does not behave as the spec says, the binary is the truth and the finding is yours to report.**
- **AC1's integration-guide list is not a suggested outline — every item is a real constraint the harness needs, and each has to be verified:**
  - **absolute-path invocation** and **`--root`**: the harness invokes from arbitrary working directories.
  - **`--json`**: the machine contract. **Say which schema version it carries and that `--json` stdout and the persisted `result.json` are the same bytes** (AD-11, Q53/Q55) — that is the property a consumer relies on.
  - **allowlisting `Bash(specwitness *)`** in agent settings — the concrete line a user pastes.
  - **the supervisor §8a slot** — where in the harness's flow the gate sits (`docs/planning-artifacts/prds/.../addendum.md` §A is the survey; **read it and check it is still true**).
  - **no-TTY guarantees**: every agent-callable command is prompt-free. **Verify by running with stdin/stdout not a terminal**, not by trusting the convention.
  - **timestamp freshness**: what a consumer can rely on about ISO-8601 UTC timestamps in the result.
- **The exit-code table is the most consequential paragraph you will write.** 0 PASS / 1 FAIL / 2 NEEDS_HUMAN / 3 infra / 64 usage (ADR-002). **Automations branch on these.** Document them from `src/cli/exit.ts`, state that **infra failures are never reported as product FAIL** — the product's first non-negotiable rule — and make it unmissable, because a harness that treats 3 as a failing verdict inverts the product's entire value.
- **AC2's "built CLI and templates only" needs verifying, not asserting.** `package.json` currently declares `files: ["dist", "templates"]` and `bin: {"specwitness": "dist/cli.js"}` — **read it yourself and confirm it is still that.** Then **actually pack and inspect the tarball's contents**: no `fixtures/`, no `tests/`, no `docs/`, no source. **`fixtures/corpus/` is new this epic and contains fixture apps, configs and — in the non-Node fixture — a second language's source.** It must not ship. **Assert it by listing the tarball, not by reading `files`.**
- **`npx specwitness --help` from the packed tarball is an end-to-end test and must run in CI.** Pack, install the tarball into a clean temp directory, run the binary, assert it works and exits 0. **This is the single most valuable check in your story**, because it is the only thing that proves the shipped artifact is not merely built but *runnable by someone who has only the tarball* — and Epic 1's exit criteria asked for exactly this, so there is precedent to follow.
- **The `next` dist-tag plan is documentation, not a publish.** **Do not publish anything.** Write the versioning and release plan: semver, what `next` means, when `latest` gets used, and how a pre-1.0 CLI signals breaking changes to a harness that branches on its exit codes and JSON. **The version is `0.1.0` today** — confirm it and say what happens next.
- **Document what is true today, including what is not implemented.** Six epics shipped a lot and deferred a lot. **A README that documents an intended product rather than the built one is worse than none**, because the first reader is the author, who will find every gap within a day and stop trusting the rest. Specifically say what V0 does not do (from PRD §5 and the roadmap's DO-NOT-BUILD list): no SaaS, no web UI, no cloud telemetry, no GitHub App, no differential BASE/HEAD execution, no challenge/mutation verification, no container isolation, no Windows hardening.
- **Rider on this story: action item e5-E — `report --explain`.** Epic 5 left this unresolved and **your README forces the answer, which is why it is here.** The facts: a merged guard (`tests/.../report.test.ts:399-412`, rationale Q52 — *"`report` re-renders persisted `result.json`, never re-executes; no provider"*) forbids it, while story 5.5's spec clarifications assume it. Two agents reached that conclusion independently. **Your options are exactly two, and both are acceptable:**
  1. **Document its absence** — state plainly that `report` does not re-run the explainer and why (Q52's reasoning), and **amend story 5.5's spec clarification** so the artifact stops asserting something untrue; or
  2. **Escalate for a decision to narrow the guard** — an architecture change, an ADR, and *not* something to implement inside a documentation story.
  **A documented flag that throws is a defect**, and shipping one in the product's first README would be the worst possible first impression. **Pick, record the reasoning, and say which you did.**
- **⚠️ WAVE 2 CHANGED THE GROUND UNDER THIS SPEC. Read the epic journal's wave-2 entry before you write a word** (`~/.local/state/terminal-agents/epics/epic/6-golden-corpus-and-scorecard/journal.md`). This spec was written on 2026-09-04, before wave 2 ran. Since then: **the corpus grew from 2 fixtures to 15**, story **6.9** shipped a non-blocking browser CI job that did not exist when this spec was drafted, story **6.5** shipped `scorecard.jsonl`, story **6.8** changed how every provider prompt is assembled, and a follow-up spec **6.10** was written and merged **without being scheduled into any wave**. Your README documents what exists at the end of wave 3, not what this spec imagined.
- **Two things wave 2 handed you that belong in the integration guide, and would otherwise reach nobody:**
  - **A PASS can exit 1 under a closing pipe** — an ADR-002 violation that reaches a harness, escalated by 6.5's author and deliberately left unfixed because the remedy is process-global state no Epic 6 story owns. **A harness that pipes our stdout can therefore see exit 1 on a passing run.** Whether that is documented as a known limitation or fixed first is the owner's call — **ask, do not decide silently**, and do not ship an exit-code table that is wrong in a case somebody already measured.
  - **`verify (macos-latest)` is flaky at roughly 1 in 6**, and re-running a job overwrites its conclusion, so the branch history under-reports it. Do not document CI as uniformly green.
- **6.9's promotion criterion is an open owner decision and your docs must not pre-empt it.** The browser job is **non-blocking**, its consecutive-green streak is **one**, and **no branch protection or ruleset exists on this repository at all** — `gh api .../branches/master/protection` returns 404. So "required check" is not true of anything today. **Say what is actually enforced**, which is nothing, rather than what the workflow file implies.
- **Story 6.10 is unassigned and unscheduled**, and its own spec says the gap it describes turned out not to block any acceptance criterion. **Do not adopt it, do not document it as shipped, and do not treat writing it as scheduling it.** If you think the closing wave should absorb it, that is a question for the owner.

- **Your documentation is read by agents as well as people.** The harness invokes this CLI; the integration guide is effectively an API document. **Be precise about flags, exit codes and JSON shape, and vague about nothing.**

## Tasks / Subtasks

- [ ] Task 0 (first, before any code): read the merged product, not the specs (AC: all)
  - [ ] **Build the CLI and run `--help` on every command.** `init`, `doctor`, `contract`, `plan`, `report`, `verify`, `clean`, and 6.6's `scorecard`. **What the binary says is the truth.**
  - [ ] `src/cli/exit.ts` — the single exit-code table, verbatim.
  - [ ] `src/config/**` — the config model, for the config reference. **Document the real keys and their validation, from the schema.**
  - [ ] `src/cli/commands/**` — every flag, including `--root`, `--json`, `--no-ai`.
  - [ ] `docs/planning-artifacts/prds/prd-specwitness-2026-08-30/addendum.md` **§A** — the first-client harness survey. **Check it is still true; e2-B3 flagged §B as stale and 6.2 was asked to check it.**
  - [ ] `package.json` — `files`, `bin`, `engines`, `version`, `packageManager`.
  - [ ] `.github/workflows/ci.yml` — where your pack check goes (story 6.1 wired the corpus job; follow its shape).
  - [ ] **6.6's command surface**, from your wave peer, as soon as they have it.
  - [ ] The merged guard behind e5-E: `report`'s tests and Q52's rationale.
- [ ] Task 1: the README (AC: 1)
  - [ ] What SpecWitness is, in a paragraph a stranger understands: an **independent verification gate** that runs after coding agents and a supervisor believe an epic is done, and proves with reproducible evidence whether the assembled epic satisfies the frozen spec.
  - [ ] Install; quickstart end to end; every command; **the exit-code table**; the config reference.
  - [ ] **What V0 deliberately does not do.**
  - [ ] **Every command and output verified against the built binary.** Paste real output.
- [ ] Task 2: the harness integration guide (AC: 1)
  - [ ] All seven items from AC1, each verified: absolute-path invocation, `--root`, `--json` (with its schema version and the same-bytes property), the `Bash(specwitness *)` allowlist line, the supervisor §8a slot, no-TTY guarantees, timestamp freshness.
  - [ ] **Verify no-TTY by running with stdin/stdout not a terminal.**
- [ ] Task 3 (TDD): packaging verification in CI (AC: 2)
  - [ ] `npm pack`; **list the tarball and assert its contents** — `dist` + `templates` only, and explicitly **no `fixtures/`, `tests/`, `docs/` or source.**
  - [ ] Install the tarball into a clean temp directory and **run `npx specwitness --help`, asserting success**.
  - [ ] `npm publish --dry-run` clean.
  - [ ] Wire it into CI on both platforms.
- [ ] Task 4: versioning (AC: 2)
  - [ ] Document semver policy and the `next` dist-tag plan. **Publish nothing.**
- [ ] Task 5: rider e5-E — `report --explain`
  - [ ] Decide: document the absence and amend 5.5's clarification, **or** escalate for an ADR. **Record which and why.**
- [ ] Task 6: epic closure (with your wave peer)
  - [ ] Raise the retrospective (`docs/findings/epic-6-retro-<date>.md`) and the integration PR to `master`.
  - [ ] Update your row in `docs/implementation-artifacts/sprint-status.yaml`. **Your row only.**

## Dev Notes

### Goal & Context

**This story is the gate to Epic 7, and Epic 7 is the point of the whole project.**

The roadmap's first-dogfooding procedure begins with the author installing this package and pointing it at a real BMAD epic in `/Users/jundymek/dev/gitnebula`. Everything between here and that moment is your story: a README that makes the tool comprehensible, an integration guide that makes it invokable from the harness, and a tarball that actually runs when installed.

Two things make this harder than it sounds.

**First, there is no README at all** — not a stub, not a stale one. Six epics of work have no front door. Whatever you write becomes the product's definition for its first reader, and that reader is the author, who will test every sentence against the binary within a day.

**Second, you are documenting at the exact moment the code stopped moving.** Five epics of specs, ADRs and PR bodies describe intentions, some of which changed. Epic 5's retro found four sentences that were *"true of the case in front of the author and false in general"* — prose that passed every gate this project has, because **there is no test to make prose red**. Your entire deliverable is prose. **The only defence is to run everything you document and paste what actually came back.**

### Exact scope

- `README.md` — **new; the repository has none**
- a harness integration guide (placement your decision — `docs/` is the natural home; record the choice)
- packaging verification: `npm pack` content assertion + tarball smoke test, wired into CI
- versioning / `next` dist-tag documentation
- the e5-E decision, and — if you take option 1 — the amendment to story 5.5's spec clarification
- `docs/implementation-artifacts/sprint-status.yaml` — your row only
- possibly a small `package.json` publish-surface correction, **if the tarball proves one is needed**

### Out of scope (do NOT build)

- **Publishing to npm.** `--dry-run` and `pack` only. **Epic 7 and the owner's decision.**
- **6.6's `scorecard` commands.** Your wave peer's. **Document them; do not build or change them.**
- **New features, flags, or behaviour changes** to make documentation tidier. **If the product is awkward, document the awkwardness and report it.** A documentation story quietly changing behaviour is how a README becomes accidentally authoritative over code.
- **Implementing `report --explain`.** Rider e5-E is a **decision**, and one of its two options is an ADR someone else writes. **Narrowing a merged guard is not a documentation story's work.**
- **Corpus fixtures, the scorecard writer, `src/authoring/**`.** Waves 1 and 2, all merged.
- **`src/cli/exit.ts`.** Document it; change nothing.
- **The Epic 7 dogfooding report.** `docs/dogfooding-report-001.md` is story 7.3's.
- **Windows support.** Documented as unsupported, not built.
- **Marketing prose.** This is a local-first developer tool with a specific job. **Describe it accurately; do not sell it.**

### Dependencies & upstream contracts (by ID)

All merged. **Read the source and run the binary — not this table, and not the specs.**

| What | Where | Why |
| --- | --- | --- |
| **The built CLI** | `dist/cli.js` via `pnpm build`; `src/cli/commands/**` | **The truth about every flag** |
| **Exit codes** | `src/cli/exit.ts`; ADR-002 | The table automations branch on |
| **Config model** | `src/config/**` | The config reference, from the schema |
| **The JSON contract** | `src/schemas/result.ts`, `src/report/json.ts`; AD-11, Q53/Q55 | `--json` == `result.json`, same bytes |
| **Harness survey** | PRD addendum **§A** | The §8a slot and invocation facts — **check it is still true** |
| **Packaging** | `package.json` (`files`, `bin`, `engines`, `version`) | AC2 |
| **CI** | `.github/workflows/ci.yml` | Where the pack check goes |
| **6.6's commands** | your wave peer | Document what they hand you |
| **The e5-E guard** | `report`'s merged tests; Q52's rationale | The decision you must make |

### Architecture compliance

- **AD-11.** One result model, many renderers — **`--json` stdout and `result.json` are the same bytes.** Document it; it is the property a harness relies on.
- **AD-6 / ADR-002.** The exit table. **Infra failures are never product FAIL** — say it loudly.
- **AD-5.** Persisted artifacts carry `schemaVersion` and evolve additively. **Document which version `--json` carries** so a consumer can branch on it. **ADR-008** governs what happens when a newer document meets an older reader — **worth a line in the integration guide**, since a harness will eventually hit exactly that.
- **AD-4 / NFR-1.** Never read, copy or persist any credential store — **and say so in the README**, because it is a property users should be able to verify and trust.
- **Local-first.** No SaaS, no telemetry, no network. **A documented product promise.**
- **Conventions.** ISO-8601 UTC; `ERROR:`+`HINT:` on stderr; prompt-free commands; Conventional Commits.

### Testing requirements

- **The packaging checks are real tests and must run in CI on both platforms.** Pack, assert tarball contents, install into a clean temp dir, run `npx specwitness --help`, assert exit 0.
- **Plant and see red**: temporarily add a path that should not ship and confirm the content assertion fails. **A packaging guard nobody has seen fail is the guard most likely to be vacuous** (Epic 4 retro §2 observation 7 — a test that passed against unfixed code because both assertions ran against empty output).
- **Verify no-TTY by running commands with stdin/stdout not a terminal** and asserting no prompt.
- **Every command in the README executed, with real pasted output.** Where you cannot execute something, **say so explicitly rather than describing it plausibly.**
- **H-8**, **H-13**; self-limiting fixtures; *when a test run is killed outright, no `afterEach` executes at all* (Epic 4 retro §2 observation 8) — relevant because packing into temp directories leaves artifacts.
- **CI is real now.** Report both platforms; **a tarball that installs on macOS proves nothing about Linux.**

### Integration expectations

**Your PR is the last one in the epic, and its body is what the retrospective and Epic 7 read.** It must state:

1. **Every command you ran to verify the documentation**, and anything you documented **without** being able to run — explicitly listed.
2. **Every place the product's behaviour differed from what a spec, ADR or PR body claimed.** These are findings, and under Stage 4 they reach the owner only if you carry them out (Epic 5 retro §2 observation 9).
3. **The tarball's actual contents**, as listed, and that `fixtures/corpus/` does not ship.
4. **The `npx --help`-from-tarball result** on both platforms.
5. **The e5-E decision** — which option, and the reasoning — and, if option 1, that story 5.5's clarification was amended.
6. **The versioning and `next` dist-tag plan.**
7. **Whether PRD addendum §A is still accurate**, having checked it.
8. **Anything in the product you found awkward but did not change**, so Epic 7 meets it knowingly.
9. **The exit-1-under-a-closing-pipe decision** — documented as a limitation, or fixed first — and that you asked rather than decided.
10. **What is actually enforced in CI** (today: nothing is a required check), and that 6.9's promotion criterion remains the owner's.
11. **That story 6.10 was neither adopted nor documented as shipped.**

Carry Epic 4's lesson 5 verbatim and obey it: ***describe your own diff; point at, but do not characterise, anyone else's*** — **and cite `path:line` for every claim about code you did not write** (e5-B). **For a documentation story this is nearly the whole job**: almost every sentence you write is a claim about somebody else's merged code. **Run it or cite it.**

### Failure modes (what "wrong" looks like here)

- **Documenting the intended product instead of the built one.** The first reader is the author; he will find every gap in a day, and then trust none of it.
- **A documented flag that does not exist or throws.** `report --explain` is the live example, which is why e5-E is your rider.
- **Prose that is true of the example and false in general.** Epic 5 retro §2 observation 4 — the defect class no gate here can catch.
- **`fixtures/corpus/` shipping in the tarball.** New this epic, contains fixture apps and a second language's source.
- **A tarball that builds but does not run** when installed clean — the exact gap the `npx --help` check closes.
- **Asserting `files` instead of listing the tarball.** The two disagree in ways only packing reveals.
- **An exit-code table that lets a harness treat 3 as a failing verdict**, inverting the product's core promise.
- **Changing product behaviour to make the documentation neater.**
- **Publishing.** Not yours.
- **Silently dropping a finding into a PR body** the owner will never read (Stage 4).
- **Reporting CI as "pending owner".** It runs now — **and your packaging check is one of the reasons it matters.**

### Security

- **The README is where you make the product's security promises legible**, and each must be true: **no direct LLM API usage in V0** (AI is delegated to local `claude`/`codex` CLIs as subprocesses); **credential stores are never read, copied or persisted** (`~/.claude/`, `~/.codex/`); **only Project-Config-declared commands reach a shell** (AD-3), so provider output cannot introduce shell strings; **local-first — no network, no telemetry.** **Verify each against the code before you promise it**, and cite where a reader can check.
- **The integration guide tells users to allowlist `Bash(specwitness *)`.** That is a real privilege grant in someone's agent settings. **Be precise about what it permits** and do not suggest anything broader.
- **No real credential, token, or anything shaped like one** in any documented example — including in output showing what redaction looks like. **Assert secrets are ABSENT rather than that `[REDACTED]` is present** (Epic 3 retro §7).
- **Never read, copy or persist any credential store** (NFR-1, AD-4, Q59) — including while testing packaging.
- **This story makes zero provider calls**, which is worth stating.
- **Your security sign-off may be the last gate before merge** (Stage 4). Sign off from the actual report.

### Project Structure Notes

You create `README.md` at the repository root — **the first one** — and an integration guide whose home is your decision (`docs/` is natural; record the choice). You add a packaging check to CI following story 6.1's job shape. You create no new source directory, add no dependency, and change no product behaviour.

### References

- Epic ACs: `docs/planning-artifacts/epics.md` § "Story 6.7"
- Roadmap: `docs/planning-artifacts/roadmap.md` § "EPIC 6" — wave 3; exit criteria; **"MVP READY WHEN"**; **"DO NOT BUILD YET"** (the list your README must be honest about); **"FIRST REAL DOGFOODING PROCEDURE"** — *the procedure your documentation must make followable*
- Spine: **AD-11, AD-6, AD-5, AD-3, AD-4**; Conventions; Stack table
- ADRs: **ADR-002** (exit codes), **ADR-008** (version skew — worth a line in the integration guide), ADR-001 (AI via local CLIs — a documented security promise), ADR-006, ADR-007 (Node floor >=22.13)
- PRD: **UJ-4**, **addendum §A** (the first-client harness survey — check it is still true), FR-30 (the JSON contract), FR-29 (bounded output), PRD §5 (non-goals — what V0 does not do), NFR-1
- Questions: **Q52** (`report` re-renders, never re-executes — the e5-E guard's rationale), **Q53/Q55** (stable versioned JSON; harness consumption), Q54 (exit codes)
- Epic 5 retro: `docs/findings/epic-5-retro-2026-09-04.md` — **§2 observation 4** (*"prose is unverified code"* — the defect class of your entire deliverable), **§2 observation 9** (Stage 4's reporting duty), **§5** (technical debt, including `report --explain`), §7 action item **e5-E** (your rider), §9
- Epic 4 retro: `docs/findings/epic-4-retro-2026-09-02.md` — §2 observation 7 / §3 lesson 7 (guards seen red — your packaging assertion), §3 lesson 4 (PR body as handover), §3 lesson 5, §2 observation 8
- Epic 1 exit criteria: *"`npx specwitness --help|init|doctor` work from a packed tarball"* — **the precedent for AC2's smoke test**
- Harness defects: `docs/findings/epic-2-harness-defects-2026-08-31.md` — H-3, H-4, H-8, H-13

### Gates you cannot satisfy — report as pending-owner, never as passed

**⚠️ This list is SHORTER than every previous epic's, and the deletion is the point.**

- ~~CI~~ — **NO LONGER PENDING.** CI executes as of `fa2647b` (2026-09-04: Node floor raised to 22.13, triggers restored, repository public). This retires action item **e3-E** after five epics. Run it; report both `ubuntu-latest` and `macos-latest`. **Claiming CI is unavailable is a stale fact.**
- **Windows.** The win32 branches remain asserted as pure values only. Out of scope.
- **Node exactly 22.13**, the declared floor — CI now pins it, so this is newly satisfiable; confirm rather than assume.
- **A real browser download.** Out of scope for Epic 6 (owner scope decision).
- **Real `claude`/`codex` invocations.** Forbidden for the whole corpus suite by story 6.1's AC1, and Epic 7's job.
- **npm publishing.** Story 6.7 and Epic 7.
- **The merge clicks.** The epic → `master` integration PR is the owner's.

## Dev Agent Record

### Context Reference

- Story spec: `docs/implementation-artifacts/epic-6-golden-corpus-and-scorecard/6.7-dogfooding-readiness-docs-packaging.md`
- Wave 3 of Epic 6 (closing wave), cohort with 6.6. **Get 6.6's command surface early; write that section last.**

### Agent Model Used

_(fill in)_

### Debug Log References

_(fill in)_

### Verification log — every command I ran, and what it actually printed

_(fill in — required; and list anything documented WITHOUT being run)_

### Where the product differed from what a spec, ADR or PR body claimed

_(fill in — required; under Stage 4 these reach the owner only if you carry them out)_

### The e5-E decision

_(fill in — required: which option, and why)_

### Completion Notes List

_(fill in)_

### File List

_(fill in)_
