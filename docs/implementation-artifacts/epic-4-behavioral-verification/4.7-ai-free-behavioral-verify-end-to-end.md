# Story 4.7: AI-free behavioral verify end-to-end

Status: ready-for-dev

## Cohort & Dependencies (harness self-configuration)

**You are COHORT 3 of THREE sequential cohorts, and you are alone in it.** Epic 4 did not launch as one wave. Epic 3 ran eight agents at once and paid ~7.5 hours to three subscription session-limit freezes (Epic 3 retro observation 1, owner action item C); the owner's decision on 2026-09-01 was three smaller cohorts against **one long-lived epic branch**.

```
master
  └── epic/4-behavioral-verification
        ├── cohort 1 (wave A):  4.1, 4.2                     ← MERGED
        ├── cohort 2 (wave B):  4.3, 4.4, 4.5, 4.6           ← MERGED
        └── cohort 3:           4.7 + retrospective          ← you are here, alone
                                                                    │
        epic branch ──────── one PR ────────────────────────────────┴──→ master
```

**You have no peers.** This is stated explicitly rather than left out: cohort 3 is one agent. Expected mapping: **alice=4.7.**

Run after reading this spec:

1. `~/.terminal-agents/scripts/set-cohort.sh` **with no peers** — you are a cohort of one. If the script requires an argument, say so in your PR body rather than inventing a peer.
2. Intent-sync is a formality for a solo cohort, but **still write `intent.md`** — it is the artefact the supervisor reads, and `intent-ready.sh` checks for recommended sections (it matches section titles by **exact string**, so a paraphrased heading warns spuriously — harness friction H-16, harmless).
3. **You set NO `set-depends-on` marker.** All six predecessors merged before you started. **Verify rather than assume:**
   ```
   gh pr list --base epic/4-behavioral-verification --state merged
   ```
   Confirm all six story ids are there. **If any is missing, stop and report it** — you cannot integrate what did not land, and a partial epic reported as integrated is the worst possible outcome for this story.

**Your story PR targets `epic/4-behavioral-verification`.** The **final** PR — epic branch → `master` — is also your cohort's, and it carries the epic (see "Closure bookkeeping" below).

**After any `epic-updated` nudge:** `git fetch origin`, then — this project **squash-merges** — `git rebase --onto origin/epic/4-behavioral-verification <sha-of-your-last-merged-commit>` (H-4). **If your own PR is already merged, ignore a rebase nudge** (H-3).

### Your job is to run the product, and seam defects are what you are for

Epic 3's retrospective states this as its generalisable lesson (§2 observation 5), and it is the reason your story exists:

> Merged code contradicted two recorded decisions, found only by running the built binary. A `verifiability: human` criterion auto-PASSed at exit 0, and every stage hint was discarded — a tampered-contract refusal printed **zero stderr bytes**, inviting exactly the `--freeze` laundering ADR-005 exists to prevent. **Neither was reachable by any unit suite because each lived in a seam between merged stories.** Both were found by 3.7 wiring the real command.

So: **build the binary and run it.** `tests/setup/build-cli.ts` exists for this. Six stories' unit suites are green by construction; the defects you are hunting are the ones that only appear when their outputs meet. Escalate what you find with measurements — **but note that unlike Epic 3, there is nobody to escalate *to* within the cohort.** Every predecessor's agent is gone. A seam defect is therefore **yours to fix**, with the fix argued in your PR body and the owner's attention drawn to it. That is a widening of the normal escalate-not-patch rule, and it is deliberate: the alternative is an epic that ships broken because its integrator was being polite.

## Story

As the epic's integration proof and UJ-3 realized,
I want full verify (gates → services → data → probes → aggregate → report) executing a precompiled plan with zero provider calls,
So that repeat verification is deterministic, fast, and subscription-independent (FR-18, FR-27 full; Golden Corpus fixture 11).

## Acceptance Criteria

From `docs/planning-artifacts/epics.md` (authoritative — expand, never weaken):

1. **Given** a fixture app with frozen contract and compiled plan, provider binaries absent/disabled
   **When** `specwitness verify` runs
   **Then** it completes normally, provider-invocation count is 0 in the run metadata, and the verdict derives purely from criterion results.
2. **Given** a fixture with one failing criterion (e.g. wrong status code)
   **When** verify runs
   **Then** exit 1, the criterion carries expected/actual and evidence refs sufficient for a future repair agent (AR-4), and the terminal report matches brief §36's shape.
3. **Given** `verify` without an existing plan but with providers configured
   **When** invoked
   **Then** plan compilation is auto-invoked first (recorded in run metadata), then execution proceeds — while `--no-ai` in the same situation refuses with `HINT: run specwitness plan`.

Clarifications (detail only, no weakening):

- **AC1's "provider-invocation count is 0" must be asserted MECHANICALLY, not by inspection.** FR-18 and Q66 make it a guarantee, and the roadmap makes it an exit criterion. The strong form: **inject a provider that throws on any call, and prove nothing called it.** A count of zero read from run metadata is the weaker assertion — do both, because they fail differently: the throwing provider catches a call you did not know about; the metadata count catches a call that happened and was not recorded.
  - `RunResult.providerUsage` is the merged field (`ProviderUsage[]` in `src/domain/run-result.ts`). Assert it is **empty**, and assert the same document reaches `--json` stdout and `result.json` — they are the same bytes by AD-11.
  - Also run the binary with the provider binaries genuinely **absent from PATH** (`tests/fixtures/bin/install-shim.ts` shows the established PATH-manipulation pattern) — that is the version of the test that matches how an operator would experience it.
- **AC2's "sufficient for a future repair agent" is FR-28 made concrete, and it is testable.** Every non-pass criterion carries `expected`, `actual` and **≥1 evidence reference**. `deriveCriterionResult` already produces these; your job is to prove they survive the whole pipeline into `result.json` and into `--json` stdout. Assert on the **persisted document**, not on an in-memory object — persistence is where fields get dropped.
- **AC2's "the terminal report matches brief §36's shape" — read the brief before asserting anything.** `docs/specwitness-input-brief.md` §36 is the authority on the report's shape; FR-29 lists the required sections (contract status, revisions, environment, gates, per-criterion ✓/✗/? with one-line summaries, counts, verdict, evidence path). **The renderers are merged (`src/report/**`, story 3.6) and AD-11 says no renderer computes its own facts** — so if a section cannot be rendered, the missing piece is a *fact* that the pipeline did not record, not a renderer change. Fix it at the source and say so.
- **AC3 has two halves and the refusal half is the one that must not be got wrong.**
  - **Auto-compilation**: no plan + providers configured ⇒ compile first, **recorded in run metadata**, then execute. The recording is not decoration: FR-18's whole promise is that reruns are AI-free, so a run that *did* spend provider quota must say so in the document a harness reads. `providerUsage` is where it goes.
  - **`--no-ai` refuses**: no plan + `--no-ai` ⇒ refuse with `HINT: run specwitness plan`. Not a silent skip, not a PASS with zero criteria — a **refusal**. And note the interaction with AC1: `--no-ai` with a plan present must **succeed**, making zero provider calls. Test all four cells of that 2×2 (plan present/absent × `--no-ai` on/off).
  - `--no-ai` is a **new flag on `verify`** and it is yours — 4.2 was explicitly told not to add it.
- **⚠️ THE GREEN-FOR-NOTHING REFUSAL MUST BE AMENDED, NOT DELETED — and you are the story that amends it.** `assertSomethingToAdjudicate` in `src/cli/commands/verify.ts` currently throws a `ConfigError` when `config.gates.length === 0`. Its own comment names you:
  > *"**Epic 4 must widen this, not delete it.** The rule is 'the run could not adjudicate anything', which today reduces to 'no gates declared' only because nothing adjudicates criteria mechanically yet. When probes land, a gate-less project with automated criteria becomes legitimately verifiable and the second clause becomes real."*
  So the amended rule is: **refuse when the run could adjudicate nothing at all** — no gates declared **and** no automated criterion has an executable probe. A gate-less project whose plan maps automated criteria to probes is now legitimately verifiable and must **not** be refused. **Read the whole comment block including its "WHY BEFORE THE RUN" and "WHY `ConfigError`" paragraphs before editing**, and keep both properties: refuse **before** the run (so no misleading `result.json` saying PASS exists beside a CLI exiting 3), and keep it `ConfigError` (exit 3) rather than `UsageError` (64) or NEEDS_HUMAN (Q39 fixes exactly two triggers and this is not a third). Decision 3.7-D4, recorded in `DECISIONS.md`.
  - **Test both directions**: a gate-less project **with** probes now verifies (previously refused); a project with neither gates nor probes **still refuses**. The second test is the one that matters — it is the whole reason the refusal is amended rather than removed.
- **⚠️ HUMAN CRITERIA ARE UNCONDITIONALLY NEEDS_HUMAN AT EXECUTION TIME, TOO.** `deriveCriterionResult` returns `needs_human` for `verifiability: human` **before it looks at attempts** — attempts do not override, unconditionally. Epic 3 caught and reverted a well-meaning "floor" variant and called it a silent redesign of a recorded decision. **Your end-to-end fixture must contain a human criterion and prove it reaches NEEDS_HUMAN (exit 2) even in a run where probes executed and passed.** That is the seam-level version of the property, and it is exactly the kind of thing that was broken in Epic 3 and only found by running the binary (`verifiability` was being dropped at the integrity stage, so a contract whose author had written "no machine may answer this" verified PASS at exit 0). **A probe adjudicating a human criterion requires an ADR, not a branch** (Epic 3 retro §6).
- **The probes stage is almost certainly yours.** `src/pipeline/stages/probes.ts` is still a placeholder whose header says *"Filled by Epic 4, stories 4.4–4.6"*. Cohort 2 was told to settle its owner at intent-sync with the recommendation that **nobody in cohort 2 takes it, because it wires all three executors plus `deriveCriterionResult` and cannot be built correctly until all three exist**. **Check what merged**: if cohort 2 built it, extend it; if not, it is yours.
  - Read `src/pipeline/stages/aggregate.ts` first — it explains why the *aggregate* stage, not the probes stage, materialises `skipped` criteria (ADR-003: a gate failure jumps past probes, so results produced there would be missing from exactly the run whose report is supposed to show every criterion as `skipped`). **That reasoning constrains what your probes stage may do**, and re-deciding it would reintroduce a defect that was already found and fixed once.
  - The probes stage calls `deriveCriterionResult` — the single producer — and pushes `DerivedCriterionResult`s onto `context.run.criteria`. It **never** calls `aggregate()` and never writes `context.run.outcome`.
  - Retry orchestration lives here, not in the executors: **retries are opt-in per probe class, default 0** (AD-9, Q43/Q44), every attempt recorded, and `deriveCriterionResult` decides `flaky`. The executors execute one attempt per call; you call them N times.
- **YOU OWN THE SURFACE-CONFORMANCE TEST (owner decision, 2026-09-01).** All three merged executors must produce **structurally identical results for structurally identical outcomes**. You are the only story that sees all three. Write one test that drives http, observation and shell through the same set of situations — a satisfied assertion, an unsatisfied assertion, an exec error, a retry-then-pass — and asserts the derived `CriterionResult`s are structurally identical (same status, same presence/absence of `expected`/`actual`/`evidence`/`flaky`). **Divergence found here is a real finding and it is yours to fix**, since the authors are gone.
- **THE SEEDED-SECRET TEST IS A ROADMAP EXIT CRITERION AND THE END-TO-END VERSION IS YOURS** (FR-28). Each surface story proved its own capture path; you prove the whole run. Seed a secret into: a gate's output, a service's output, a data command's output, an HTTP response header and body, an observation command's stdout, and a shell probe's output. Then assert **the secret string appears in no stored artifact** — `result.json`, every evidence file in the run directory, `--json` stdout, and the terminal report on stdout/stderr. **Assert absence, not the presence of `[REDACTED]`**: Epic 3's retro §7 records that output containing the marker *with the secret still beside it* survives review in a way a raw leak does not. Walk the whole run directory; do not sample.
- **Redaction remains fail-closed at every caller you wire.** `redactText`'s `{shellCommand: true}` is reserved for **declared commands only**; every capture output is passed undeclared (Epic 3 retro §6).
- **Exit codes come only from `src/cli/exit.ts`.** You write none. A failure throws and `main.ts`'s single broad catch classifies it (AD-7, fail closed); a run that reached an outcome records `exitCodeForOutcome(outcome)`. **`tests/unit/exit-location.test.ts` reads every source under `src/` and enforces this — keep it green.** Exercise the whole table end to end through the built binary: **0 PASS, 1 FAIL, 2 NEEDS_HUMAN, 3 infra, 64 usage** (Epic 3's supervisor did exactly this at epic head and it is the standard for this epic's close).

### Owner-assigned rider: Epic 3 retro debt 2 — SIGINT on the verify path (not optional)

**The verified fact:** there is **no SIGINT handling anywhere in `src/cli/` or `src/pipeline/`**. Ctrl+C during a verify tells the operator nothing about the surviving detached gate process, the worktree left on disk, or `specwitness clean`.

**What is and is not a defect, stated precisely so you do not over-fix:**

- **Exit 130 is NOT an exit-table breach.** Shell-reported signal death is distinguishable via `WIFSIGNALED`, and ADR-002's table governs *chosen* exit codes. Do not add 130 to `cli/exit.ts`.
- **The defect is the silence.** After Ctrl+C the operator has an orphaned process group and a temp worktree and no idea either exists.

**Your deliverable, in order:**

1. **Reproduce it.** Start a verify with a long-running gate or service, send SIGINT, and record what actually survives and what the operator is told.
2. **Decide the scope of the fix and state it.** Two legitimate outcomes, and **the second is acceptable**:
   - **A message on the way out**: a SIGINT handler that prints the house-style `ERROR:`/`HINT:` pair naming the run directory and `specwitness clean`, and — if it can be done safely — best-effort teardown. Beware the trap: a handler that *attempts* full async teardown and is interrupted again leaves a worse state than one that prints a reliable message. Prefer the reliable message; add teardown only if you can prove it terminates.
   - **An ADR instead.** If you conclude the fix needs signal semantics this project has not decided (re-entrancy, teardown-during-teardown, what a second Ctrl+C means), **say so in your PR body and write the ADR in `docs/adr/`** rather than inventing semantics in a branch. Epic 3's action item F names exactly this fork ("ADR or Epic 4 story"), and the retro says: *"If the agent concludes the fix needs an ADR rather than a story, it must say so in its PR body rather than inventing signal semantics."*
3. **Either way, a red-first test.** A SIGINT-during-run integration test is achievable — `tests/integration/process-lifecycle-exit.test.ts` and `process-runner-groups.test.ts` show the established patterns for signalling a child and asserting what survives.

### Closure bookkeeping and the retrospective (your cohort's, not a separate story)

Cohort 3 closes the epic. In addition to the story:

1. **`docs/implementation-artifacts/sprint-status.yaml`** — all seven Epic 4 rows to `done`, `epic-4` to `done`, `epic-4-retrospective` to `done`. Then validate and require `"valid": true`:
   ```
   uv run .claude/skills/bmad-sprint-planning/scripts/sprint_plan.py validate --status-file docs/implementation-artifacts/sprint-status.yaml
   ```
   Carry any still-open Epic 3 action items forward, and append Epic 4's own under `action_items:` in the established shape.
   - **Let the tool write the file; do not hand-edit rows.** `~/.terminal-agents/scripts/sprint-status.sh` (absolute path, from your own worktree — `make sprint-status` is the operator's spelling and is not yours) is the writer. **Two rows are known stale: `4-3` and `4-5` read `review` despite being merged** — cohort 2's supervisor reported this as its own error and said the script's measurement fixes it at closure. Verify the written result rather than assuming.
   - **No follow-up keys need adding.** The push policy pins agents to their story branch, so all four cohort-2 follow-ups landed under existing keys.
2. **`docs/findings/epic-4-retro-<date>.md`** — follow the shape of the three existing retros (`epic-1-retro-2026-08-31.md`, `epic-2-retro-2026-08-31.md`, `epic-3-retro-2026-09-01.md`): delivery summary, post-run observations as symptom/cause/why-no-story-caught-it/fix, what we learned, what went well, technical debt carried forward, **next-epic readiness (Epic 5 — browser & human judgment)**, action items, follow-through on the previous epic's items, and **gates no agent could run**. Ground every claim in an artefact — a PR body, a commit, a measurement — never a status line. Epic 3's retro §2 observation 2 records three sightings of the auto-review classifier reporting clean over non-clean runs, so **sign off from the actual report, never the status line.**
   - **The three-cohort experiment is this retro's most valuable content.** It was run to answer owner action item C (subscription-limit freezes). Report honestly whether it worked: wall-clock, freezes, whether cohort-2 agents were blocked by not being able to talk to 4.2, and whether "merged source is the contract" held up. **A negative result is a useful result** and the owner needs it before Epic 5.
   - **⚠️ Agent names are RECYCLED per cohort and `events.log` is shared.** `~/.local/state/terminal-agents/events.log` spans all three cohorts, but the name pool is assigned positionally per cohort — so `alice` means **4.1 in cohort 1 and 4.3 in cohort 2**, and `bob` means 4.2 then 4.4. Attributing an event to a story by agent name alone will be wrong for half of them. Use timestamps against each cohort's launch window, or the PR numbers, and say in the retro that you did.
   - **Cohort 1's and cohort 2's runtime state is gone by the time you read this** (each launch force-cleans agents of the same name). Where the owner archived it, cohort 2's supervisor record is under `~/.local/state/terminal-agents/archive/` and/or a copy the owner preserved — **ask for it rather than reconstructing from the log**; it is 48 verdict entries with the evidence that produced them, and it is the primary source for observations 1–4 below.
   - **Carry forward what cohort 2's supervisor handed over**, each as a named item with an owner: (a) three surfaces validate params manually and independently because the per-surface schemas in `plan.ts` are module-private — **candidate for a follow-up story, not a fix in this one**; (b) three near-identical `slugify` helpers, flagged by all three surface PRs — consolidation is legitimately yours if it is cheap, a named follow-up if it is not; (c) the untested observation input shapes (folded into Task 6); (d) the `setup` placeholder, still empty and in no story's ACs.
3. **`CLAUDE.md`** — update the "Current phase" paragraph for Epic 4's merge.
4. **The final PR: `epic/4-behavioral-verification` → `master`**, carrying the whole epic. **Its title becomes the squash commit subject on `master`** — a Conventional Commit without the story id (Epic 3 action item H; two non-conventional subjects are permanent epic-branch history from Epic 2).
5. **Do NOT modify** `roadmap.md`, `epics.md`, the PRD, the spine or the ADRs. If one is wrong, say so in your PR body as a question. (Two are already known-stale and are the owner's, not yours: **ADR-003's prose still says `gateFailed: true`** while the thrice-confirmed implementation is `gateFailed?: string`, and **PRD addendum §B** still claims no JSON-schema-constrained output flag exists.)

### A finding already established — do not spend time re-deriving it

Epic 3's retro lists debt 3 as *"a stale comment plus an unflipped tightening in `tests/integration/verify.test.ts` (~line 879)"*. **This was verified against merged source on 2026-09-01 and it is closed**: PR #39 enabled the assertion, and the file now asserts `expect(errors).toHaveLength(1)` and `expect(hints).toHaveLength(1)`. The comment is written in the past tense and describes history, not a gap. **No cleanup is assigned.** If you touch that file anyway and want to reword the comment, that is fine and cosmetic — but do not report closing a debt that PR #39 closed.

## Tasks / Subtasks

- [x] Task 0 (first, before any code): verify what actually merged (AC: all)
  - [x] `gh pr list --base epic/4-behavioral-verification --state merged` — all six story ids present. **If not, stop and report.**
  - [x] Read all six PR bodies (`gh pr view <n> --json body`). 4.2's and 4.1's were written as handoff documents; cohort 2's state each executor's constructor signature and decisions.
  - [x] Read the merged source they describe: `src/schemas/plan.ts`, `src/pipeline/stages/services.ts`, `src/pipeline/stages/data.ts`, `src/surfaces/{http,observation,shell}.ts`, `src/pipeline/stages/index.ts`, `src/pipeline/stages/probes.ts`.
  - [x] **Where a PR body and its merged code disagree, the code wins** — and the disagreement is a finding for your retro.
- [x] Task 1 (TDD): the probes stage (AC: 1, 2)
  - [x] **It is yours — cohort 2 unanimously did not touch it.** `src/pipeline/stages/probes.ts` is still the placeholder.
  - [x] Read `src/pipeline/stages/aggregate.ts` first — do not move `skipped` materialisation into probes (ADR-003).
  - [x] For each plan entry: build a `ProbeRequest`, call the surface executor, collect `ProbeAttempt`s (retries opt-in, default 0), call `deriveCriterionResult`, push onto `context.run.criteria`.
  - [x] **⚠️ BIND `recordEvidence` — IT IS A REQUIRED DEP ON ALL THREE EXECUTORS AND MISWIRING IT FAILS SILENTLY AND GREEN.** All three merged surfaces take a required evidence sink in their deps (`recordEvidence: SurfaceEvidenceRecorder` in `src/surfaces/http.ts`, `EvidenceSink` in `observation.ts`, `ShellEvidenceSink` in `shell.ts`; the observation one is typed `(evidence: Evidence) => void`). **Read `src/surfaces/http.ts`'s "TWO CHANNELS" header block before wiring anything** — it explains the design and names this exact failure:
    - `recordEvidence(member)` carries the **typed, bounded, redacted evidence MEMBER** onto `RunResult.evidence` — bind it to push onto `context.run.evidence`.
    - `ProbeAttempt.evidence` carries **refs to the files on disk** — a different channel doing a different job.
    - A design with only the refs *"would have shipped an epic whose reports contain gate evidence and no probe evidence, green the whole way, because no surface test drives a renderer"* (found by 4.6 at intent-sync, verified against merged source by all three surface authors). **You are the first story that drives a renderer over probe evidence, so you are the only one who can catch a stubbed or missing sink.**
    - On the exec-error path `recordEvidence` is deliberately **not** called by the HTTP executor (`http.ts:770`) — that is correct, not a gap; do not "fix" it.
  - [x] Tests: results land for pass/fail/error/flaky; **`aggregate()` is never called here and `context.run.outcome` is never written**; a gate failure still jumps past probes with every criterion `skipped` (ADR-003, and the aggregate stage's existing tests are the model); **a passing probe run produces NON-EMPTY `RunResult.evidence` containing probe evidence, and the terminal + JSON renderers actually show it** — assert on rendered output, not just on the accumulator, because that is the assertion the whole two-channel design exists to make possible.
- [x] Task 2 (TDD): CLI wiring (AC: 1, 3)
  - [x] Bind the services, data and probes stages into `createStages`, load and validate the plan at the edge, bind teardown so service groups are reaped, and bind evidence writers to `RunStore.writeEvidenceFile` with the run id already applied.
  - [x] **The AD-1 seam is why the edge does this**: `src/pipeline/**` may not import `src/authoring/**`, so the caller loads and verifies. `verify.ts`'s header explains it; follow the pattern the contract already uses.
  - [x] Add `--no-ai`. Implement the auto-compilation path and the refusal.
  - [x] Tests: all four cells of plan-present × `--no-ai`; auto-compilation is recorded in `providerUsage`; the refusal carries `HINT: run specwitness plan`.
- [x] Task 3 (TDD): amend the green-for-nothing refusal (AC: 1)
  - [x] Widen `assertSomethingToAdjudicate` per the clarification. Keep it before the run, keep it `ConfigError`, update the comment to describe the amended rule (and keep the history — the comment's value is that it records *why*).
  - [x] Tests: gate-less + probes ⇒ verifies; neither gates nor probes ⇒ still refuses.
- [x] Task 4 (TDD): AC1 end-to-end, zero provider calls (AC: 1)
  - [x] Fixture project: config, frozen contract, compiled plan, a fixture service, an observation command, a shell command.
  - [x] Tests: a provider injected to throw is never called; `providerUsage` is empty; the run completes with a verdict derived purely from criterion results; **run the built binary with provider binaries absent from PATH** and assert the same.
- [x] Task 5 (TDD): AC2 end-to-end, the failing criterion (AC: 2)
  - [x] Fixture with one wrong status code ⇒ **exit 1**; the criterion in the **persisted** `result.json` carries `expected`, `actual` and ≥1 evidence reference; the referenced evidence file exists and the path is relative; the terminal report has FR-29's sections and brief §36's shape.
  - [x] Also exercise **exit 2** (a human criterion in a run where probes passed), **exit 3** (a readiness timeout or an observation command emitting non-JSON), **exit 0** (all green) and **exit 64** (a malformed epic id) — through the built binary.
- [x] Task 6 (TDD): the surface-conformance test (owner-assigned)
  - [x] One test driving all three executors through the same situations, asserting structurally identical derived results. Divergence is yours to fix.
  - [x] **Cover the gap cohort 2's supervisor measured**: merged `src/surfaces/observation.ts` is correct but leaves **three input shapes untested**. Identify them from the module and fold them into this test rather than writing a fourth one-off suite.
  - [x] **Include `recordEvidence` in the conformance surface**: for structurally identical outcomes, all three executors must record structurally identical evidence members, not just identical derived results.
- [x] Task 7 (TDD): the end-to-end seeded-secret test (roadmap exit criterion)
  - [x] Secrets seeded into every capture path; **walk the entire run directory** plus `--json` stdout and the terminal output; assert the secret string is **absent**. Do not sample; do not assert on `[REDACTED]`.
- [x] Task 8: owner rider — SIGINT (see the rider section)
  - [x] Reproduce, decide, red-first test, and either fix or write the ADR — saying which, in the PR body.
- [ ] Task 9: closure bookkeeping and the retrospective (see that section)
- [x] Task 10: hygiene (AC: all)
  - [x] `pnpm depcruise`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build` green **from a clean clone**, not just in your worktree — Epic 3's supervisor made this the standard for an epic close, and it catches what an incrementally-built worktree hides.
  - [x] **`pnpm lint` forwards to `depcruise` and rejects eslint flags** — the auto-review wrapper's `pnpm lint --no-fix` failure is harness defect H-7, not yours.
  - [x] **Do not name a test file `*.env.test.ts`** (H-13). **Tests must be concurrency-safe** (H-8): ephemeral ports, `mkdtemp`, no fixed-name scratch files under `src/`.
  - [x] **No orphaned processes at suite end.** You spawn more than any other story — services, gates, data commands, shell probes.
  - [ ] **After the squash-merge of your own PR, verify what landed.** Epic 3 lost a reviewed commit to a push racing a merge click (retro §2 observation 4); the cohort rule adopted afterwards is `git show <base>:<path> | grep <marker>` per fix.

## Dev Notes

### Goal & Context

**This is the story where SpecWitness becomes the product it was described as.** Epic 3 made `verify` a real gate over deterministic gates — it could tell you a branch does not build. From here it tells you a branch does not *behave*, and the verdict comes from observed evidence rather than from an exit code somebody else computed.

Three things become true for the first time in this story, and each is worth a test that would notice if it stopped being true:

1. **Criterion results are real.** Every criterion in every previous run was `skipped`.
2. **A full run makes zero provider calls.** UJ-3's promise, and the reason repeat verification is cheap enough to be mandatory.
3. **The verdict is mechanically derived from evidence a human can inspect.** Not from a model's opinion — the product's first non-negotiable rule.

Your other job is adversarial: **six stories are individually green and you are the first person to see them together.** Epic 3's integration story found two decision-contradicting defects that no unit suite could reach. Expect to find some. Budget for it.

### Exact scope

- `src/pipeline/stages/probes.ts` (replaces the placeholder, unless cohort 2 built it)
- `src/cli/commands/verify.ts` — stage wiring, `--no-ai`, auto-compilation, the amended refusal
- `src/pipeline/stages/index.ts` — wiring only, if needed
- SIGINT handling (or an ADR in `docs/adr/`)
- `tests/integration/verify*.test.ts`, `tests/integration/surfaces/conformance*.test.ts`, `tests/unit/pipeline/stages/probes*.test.ts`
- Fixture projects you build in tests
- `docs/findings/epic-4-retro-<date>.md`
- `docs/implementation-artifacts/sprint-status.yaml` — all Epic 4 rows, the epic row, the retro row, action items
- `CLAUDE.md` — the "Current phase" paragraph
- **Any seam defect in merged Epic 4 code**, fixed with the reasoning in your PR body

### Out of scope (do NOT build)

- **New surfaces.** http, observation and shell are merged. **Browser is Epic 5** — do not add `@playwright/test`.
- **Re-implementing `deriveCriterionResult` or `aggregate()`.** Two producers disagree exactly once, in production.
- **Widening the Plan schema, `EVIDENCE_KINDS`, `PROBE_SURFACES`, `CRITERION_STATUSES` or `STAGE_NAMES`.** Closed; widening is an ADR. **A seam *defect* is different from a widening** — fix defects, escalate widenings.
- **The `setup` stage / `setup.install`.** `src/pipeline/stages/setup.ts` is a placeholder in **no Epic 4 story's acceptance criteria**. Leave it and name it in the retro's "technical debt carried forward" so it does not become folklore.
- **`fenceMask` in `src/ingest/bmad-v6/markdown.ts`** (Epic 2 §5a defect i) — **no Epic 4 story ingests markdown, so it stays out of scope.** Carry it forward in the retro as the named follow-up it already is.
- **`docs/planning-artifacts/**` and `docs/adr/`** — except a new ADR if the SIGINT rider needs one. Never edit roadmap, epics, PRD, spine or existing ADRs.
- **Adding exit code 130 to `cli/exit.ts`.** Signal death is not a chosen exit code.
- **Epic 6's Golden Verification Corpus.** AC1 and AC2 name "Golden Corpus fixture 11" and AR-4 — those are *future* corpus fixtures. **Your fixtures are inline.** Say so in your PR body and in the retro, so nobody reads a passing suite as corpus coverage.

### Dependencies & upstream contracts (by ID)

All merged. **Read the source, not this table** — Epic 2's lesson 5, re-confirmed twice: verify shapes against merged source, not a summary, *including this spec's summaries and the six PR bodies*.

| What | Where | Why |
| --- | --- | --- |
| **The single CriterionResult producer** | `src/domain/criterion-result.ts` | `deriveCriterionResult`, `SurfaceExecutor`, `ProbeAttempt` |
| **The one verdict function** | `src/domain/verdict.ts` | `aggregate(gates, criteria)` — call it, never re-implement (it has a property suite) |
| The AD-6 converter and why `skipped` lives there | `src/pipeline/stages/aggregate.ts` | Constrains your probes stage (ADR-003) |
| The state machine and its always-teardown guarantee | `src/pipeline/run-pipeline.ts`, `stage.ts`, `src/domain/stage.ts` | Early stop, timeline, hints |
| The command you extend | `src/cli/commands/verify.ts` | Wiring, the refusal, the AD-1 seam |
| The one exit table | `src/cli/exit.ts` + `tests/unit/exit-location.test.ts` | You write no exit code |
| Renderers | `src/report/**` | AD-11: one result, many renderers; none computes its own facts |
| Run persistence | `src/infra/run-store.ts`, `src/schemas/result.ts`, `src/schemas/manifest.ts` | `result.json`, evidence files, `--json` parity |
| Everything Epic 4 built | `src/schemas/plan.ts`, `src/pipeline/stages/{services,data,probes}.ts`, `src/surfaces/**` | What you integrate |
| Contract verification refusals | `src/authoring/verifiable.ts`, `src/schemas/contract.ts` | Loaded at the edge |
| The shipped fake provider | `src/providers/fake.ts` | Hermetic tests |
| Built-binary test harness and PATH shims | `tests/setup/build-cli.ts`, `tests/fixtures/bin/install-shim.ts` | **Run the product** |
| Process groups and teardown | `src/domain/process-runner.ts`, `src/infra/process-runner.ts`, `src/cli/commands/clean.ts` | SIGINT rider, teardown |

### Architecture compliance

- **AD-6.** One converter (aggregate) from stage results to a `RunOutcome`; gate failure is a stage result, never a throw; the exit table lives in one file.
- **AD-13.** Exactly one producer of `CriterionResult`. Executors return `ProbeAttempt`s.
- **AD-2.** No verdict, no criterion result and no mutation of a frozen artifact ever comes from a provider. `--no-ai` guarantees zero calls.
- **AD-1.** The edge loads and verifies; the pipeline receives values. `src/pipeline/**` never imports `src/authoring/**` or `src/report/**`.
- **AD-8.** Worktree isolation; manifest before resource use; teardown always; `RunStore` the sole writer under `.specwitness/runs/`; the source repo untouched (**assert `git status` clean after every run, including an interrupted one** — FR-19).
- **AD-9/AD-10/AD-11/AD-12.** Determinism, redaction at capture, one result model, hermetic tests.
- **Conventions.** ISO-8601 UTC; `ERROR:` + `HINT:` on stderr; prompt-free; bounded output.

### Testing requirements

- **Run the built binary.** The defects this story exists to catch are invisible to unit suites.
- **TDD, red first, guards seen red.**
- **Hermetic**: fixture apps built by the test, ephemeral ports, no network beyond localhost, no real providers.
- **Assert on persisted documents**, not in-memory objects — persistence is where fields get dropped.
- **Every classification test must produce the state.**
- **The full exit table, live**, through `dist/cli.js`.

### Integration expectations

You are the last agent in the epic. Your PR body and the retrospective are what the owner and Epic 5 will read. State plainly: what you found in the seams and how you fixed it; whether the three-cohort model worked; what Epic 5 inherits (the browser surface's seam, the `browser` evidence kind still unused, the `setup` placeholder still empty).

### Failure modes (what "wrong" looks like here)

- **A green run that verified nothing.** The single worst outcome this product can produce, and the reason the refusal is amended rather than deleted.
- **Deleting the green-for-nothing refusal** instead of widening it.
- **A human criterion auto-PASSing** because probes now run — the exact Epic 3 defect, one epic later.
- **`--no-ai` silently skipping** instead of refusing.
- **Auto-compilation not recorded**, so a run that spent quota looks AI-free.
- **A sampled seeded-secret test** that misses the full-copy evidence file.
- **Asserting `[REDACTED]` is present** rather than the secret absent.
- **Reporting CI green.** No workflow has run in three epics.
- **Reporting corpus coverage.** The corpus is Epic 6.
- **Signing off from a status line** rather than the actual report (three sightings in Epic 3 of a clean classification over a run with real findings).
- **A SIGINT handler that attempts async teardown and hangs**, leaving worse state than a reliable message.

### Security

- The end-to-end seeded-secret proof is **yours** and it is a roadmap exit criterion.
- Redaction stays fail-closed: `{shellCommand: true}` for declared commands only.
- Never mint a `DeclaredCommand`; **`tests/unit/config/boundary-scan.test.ts` covers every file under `src/` outside `src/config/` — verify red with a planted probe, do not author a second scan.**
- Never read or copy `~/.claude/`, `~/.codex/` or any credential store (NFR-1; `tests/unit/doctor/credential-boundary.test.ts` enforces it).
- No shell; no production URL defaults; localhost unless config says otherwise.
- After an interrupted run the source repo must be unchanged (FR-19) and no service process may survive (FR-21).

### Project Structure Notes

You fill the last reserved placeholder (`probes`) and wire the stages the seed always described. The eleven stage names, the closed unions and the single exit table are all frozen — you are completing a structure, not extending one.

### References

- Epic ACs: `docs/planning-artifacts/epics.md` § "Story 4.7"
- Roadmap: `docs/planning-artifacts/roadmap.md` § "EPIC 4" — **all three exit criteria land in your tests**
- Spine: AD-1, AD-2, AD-6, AD-7, AD-8, AD-9, AD-10, AD-11, AD-12, AD-13; Conventions; Stack
- ADRs: **ADR-002** (exit codes), **ADR-003** (gate failure semantics — note its prose is stale, the code is right), **ADR-004** (worktree isolation, not a sandbox), **ADR-005** (amendment/laundering); `docs/adr/ADR-INDEX.md`
- Questions: **Q66** (`--no-ai` guarantees a zero-provider-call rerun), **Q39** (exactly two NEEDS_HUMAN triggers), **Q45/Q46** (aggregation precedence; `fail` outranks `needs_human`), **Q42** (infra representation), **Q22** (source repo read-only; kill mid-run and assert `git status` clean), **Q19/Q20** (cleanup guarantee, `clean`), **Q43/Q44** (retries, flake), **Q47–Q49** (evidence union, relative paths, sanitisation), **Q52–Q55** (report, stable JSON, exit codes, harness consumption)
- PRD: FR-18, FR-27, FR-28, FR-29, FR-30, FR-19, FR-21, FR-22, FR-32; UJ-2, UJ-3; **brief §36** (report shape)
- Epic 3 retro: `docs/findings/epic-3-retro-2026-09-01.md` — **§2 observation 5** (integration stories exist to find seam defects), §5 debt 2 (**your rider**), §6 (next-epic readiness), §7 (action items), §9 (gates no agent could run)
- Harness defects: `docs/findings/epic-2-harness-defects-2026-08-31.md` — H-3, H-4, H-7, H-8, H-10, H-13, H-16

### Gates you cannot satisfy — report as pending-owner, never as passed

State these in your PR body **and** in the retrospective's "gates no agent could run" section:

- **Any CI-executed step.** No workflow has run in **four** epics now (Epic 3 action item E, carried from Epic 2's A1). Do not report "CI green". **State the cause precisely, because it is not neglect**: `.github/workflows/ci.yml` has had its triggers removed by owner decision on 2026-08-30 — the repository is private and its Actions minutes are exhausted, so every job was rejected before starting and left a red, meaningless check on every PR, which *"trains reviewers to ignore CI and would hide a genuine failure when one appears."* The job body still exists and is `workflow_dispatch`-able. Its own header carries the honest caveat that **no version of it has ever executed a single step**, so the first real run should be treated as unproven. Verify this against the file rather than repeating this sentence.
- **Linux.** Every gate in four epics ran on macOS/Darwin. Process groups, signal handling and argv semantics are the most Linux-sensitive code in the product and are unverified there.
- **Node exactly 22.12** (the declared floor). Everything has run on 22.20.
- **Real `claude`/`codex` invocations** against live subscriptions. Every provider test uses PATH shims or the shipped fake; the provenance values a real CLI reports remain unverified. **Your AC3 auto-compilation path is exercised only through the fake** — say so.
- **npm publishing** and **the first-dogfooding procedure** (Epic 7).
- **The Golden Verification Corpus** (Epic 6). Your fixtures are inline.

## Dev Agent Record

### Context Reference

- Story spec: this file.
- Merged predecessors, verified before any code was written (Task 0): `gh pr list --base
  epic/4-behavioral-verification --state merged` returned all six story ids — #41 (4.1),
  #42 (4.2), #46 (4.3), #44 (4.4), #43 (4.5), #45 (4.6) — plus follow-ups #47–#51. All six
  PR bodies were read, then the merged source they describe.
- `CLAUDE.md`, the architecture spine (AD-1, AD-2, AD-6, AD-7, AD-8, AD-9, AD-10, AD-11,
  AD-13), ADR-002/003/005, and Epic 3's retrospective (§2 observation 5, §5 debt 2, §7).
- Supervisor message `from-superman-001.md` (2026-09-02): Task 9 descoped by owner decision.

### Agent Model Used

Claude Opus 5 (`claude-opus-5`), autonomous mode, cohort 3 of Epic 4 (solo).

### Debug Log References

- **SIGINT reproduction, before any fix** — a `verify` against a fixture with a gate that
  never ends, interrupted once its process group reached the manifest. Measured: death by
  signal with no chosen exit code; **zero stderr bytes** about the interruption; the
  detached worktree surviving and still registered with git; the gate's process group
  surviving; the manifest present with `reaped: false` carrying both; `git status
  --porcelain` empty. Recorded in the header of `tests/integration/verify-sigint.test.ts`.
- **Exec-error evidence, measured per surface** — shell records 1 member and 2 refs; http
  records 0 members and ≥1 ref; observation recorded **0 members and 0 refs**. That single
  measurement is what identified the FR-28 violation fixed in `src/surfaces/observation.ts`.
- **Seeded-secret guards verified against a planted leak** — `redactText` neutered to return
  its input; all six assertions went red; restored, all six green. This is how the
  `specwitness report` case was caught asserting against an empty stdout (`report` requires
  an argument, so the bare call exited 64 and both `not.toContain` passed against nothing).
- **Guards planted and seen red**: `tests/unit/exit-location.test.ts` (planted a process
  exit write in `cli/verify/interrupt.ts` — flagged by path); `tests/unit/config/
  boundary-scan.test.ts` (planted `'rm -rf /' as unknown as DeclaredCommand` in
  `cli/verify/probe-dispatch.ts` — flagged by path); the probes stage's own guards (reversed
  `PROBE_PRECEDENCE`, removed the wrapped-probe skip, removed the flake carry — each named
  exactly the test written for it).
- **Orphaned processes — my first measurement was wrong and the supervisor caught it.** I
  compared TOTAL COUNTS, which cannot distinguish "reaped four, leaked four" from "leaked
  nothing"; the supervisor measured **four orphaned `node gates/slow.cjs` at ppid=1** in the
  window right after this suite last ran. Re-measured properly, by PID at ppid=1 with
  `lstart`, per suite in isolation: `verify-sigint` **0 new**, the merged `verify.test.ts`
  **0 new**, `verify-probes` + `verify-secrets` + `verify-no-ai` **0 new**, and a full
  `pnpm test` **0 new**, run twice. The four had four separate worktree cwds one second
  apart, matching this suite's four interrupt tests, so I treat them as mine and reaped
  them (25 → 21, the pre-existing baseline, which predates this story and is not mine).
  **The structural hole**: `interruptMidRun` asserted before it signalled the child, so a
  poll timeout left a `verify` running with a detached gate group. Closed — the child is
  registered before anything that can throw and SIGKILLed in `afterEach` before the reap.
  **I could not reproduce the leak** (forcing the poll to fail leaks nothing either way,
  because execa's `cleanup: true` kills the direct child at worker exit), so the mechanism
  is plausible and matched to the evidence rather than demonstrated.

### Completion Notes List

**Task 9 is deliberately unticked.** The closure bookkeeping — `sprint-status.yaml`, the
Epic 4 retrospective, `CLAUDE.md`'s "Current phase" and the epic → `master` PR — was
**reassigned to the supervisor by owner decision on 2026-09-02**, relayed in
`from-superman-001.md`. It was descoped, not skipped or abandoned. In exchange this story's
PR body carries the seam findings at length, as the retrospective's primary source. The
final hygiene subtask (verify what landed after the squash-merge) is likewise unticked
because it can only be done after the merge.

**What was built.** The probes stage (the last reserved placeholder), the CLI wiring for
services/data/probes, `--no-ai` with auto-compilation and its refusal, the amended
green-for-nothing refusal, a SIGINT notice, and six test suites — the AC1/AC2 end-to-end
proof, the `--no-ai` 2×2, the owner-assigned surface-conformance proof, the end-to-end
seeded-secret proof, the SIGINT suite and the probes stage's unit suite.

**Three seam defects found and fixed** (full reasoning in `DECISIONS.md` D1, D2, and the
PR body):

1. A criterion the PLAN carried as `needs-human` (Q38's `not-safely-automatable`, Q39's
   second trigger) derived to `skipped`, which `aggregate` treats as inert — so a
   plan-author that explicitly refused to automate a criterion produced **PASS at exit 0**.
   Fixed additively in the single producer; AD-13 untouched.
2. `ObservationSurfaceExecutor` never forwarded `onProcessGroup`, so an observation probe's
   process group never reached the manifest and `specwitness clean` could not reap it. Every
   other spawning module carries the hook.
3. The observation surface was the only one of the three leaving a non-pass criterion with
   **zero evidence references**, which FR-28 forbids. Found by the conformance test. Fixed
   with the move 4.4 had already used — the member and the reference are separate channels —
   so no union was widened and no ADR was needed.

**Deviations from the spec, each argued in the PR body:**

- `src/domain/criterion-result.ts` is edited, and the spec lists "re-implementing
  `deriveCriterionResult`" as out of scope. The change is additive and preserves the
  single-producer invariant; it is defect 1 above.
- The spec's "Exact scope" does not name `src/surfaces/observation.ts`, `src/surfaces/index.ts`
  or `src/pipeline/run-pipeline.ts`. All three are seam fixes the spec's own "Any seam defect
  in merged Epic 4 code" clause covers; `run-pipeline.ts` gains one optional input so
  `providerUsage` from auto-compilation can reach the run document.
- **A project with no plan AND no `plan-author` provider falls through to gates-only rather
  than refusing** (`DECISIONS.md` D6). AC3's precondition is "with providers configured", and
  refusing would retire Epic 3's shipped gates-only mode. The residual product question is
  raised for the owner in the PR body rather than decided here.

**Gates that could not be satisfied** are listed in the PR body and are unchanged from the
spec's own list: no CI has executed in four epics (triggers removed by owner decision
2026-08-30 — verified against `.github/workflows/ci.yml`); Linux is unverified; Node is
22.20 rather than the declared 22.12 floor; no real `claude`/`codex` invocation happened, so
the AC3 auto-compilation path is exercised only through the shipped `fake` provider; the
Golden Verification Corpus is Epic 6 and **every fixture here is inline**.

### File List

**NEW**

- `src/cli/verify/probe-dispatch.ts` — the composition root for probe execution.
- `src/cli/verify/interrupt.ts` — the SIGINT/SIGTERM notice.
- `tests/unit/pipeline/stages/probes.test.ts`
- `tests/unit/surfaces/observation-process-group.test.ts`
- `tests/unit/surfaces/observation-attempt-record.test.ts`
- `tests/integration/helpers/probe-fixture.ts`
- `tests/integration/verify-probes.test.ts`
- `tests/integration/verify-no-ai.test.ts`
- `tests/integration/verify-secrets.test.ts`
- `tests/integration/verify-sigint.test.ts`
- `tests/integration/surfaces/conformance.test.ts`
- `DECISIONS.md`

**UPDATE**

- `src/pipeline/stages/probes.ts` — replaces the placeholder.
- `src/pipeline/stages/index.ts` — `probes` added to `StageDependencies`; wiring only.
- `src/pipeline/run-pipeline.ts` — optional `providerUsage` seed (AC3's recording).
- `src/cli/commands/verify.ts` — stage wiring, `--no-ai`, auto-compilation, the amended
  refusal, the interrupt notice, teardown draining the service registry.
- `src/domain/criterion-result.ts` — `DerivationOptions.plannedNeedsHuman` (seam defect 1).
- `src/surfaces/observation.ts` — `onProcessGroup` (defect 2), the attempt record (defect 3).
- `src/surfaces/index.ts` — the two missing re-exports, as named exports.
- `tests/unit/domain/criterion-result.test.ts` — the plan-disposition arm.
- `tests/unit/surfaces/observation.test.ts`, `tests/integration/surfaces/observation.test.ts`
  — three merged assertions that encoded the pre-fix evidence behaviour, updated in place.
