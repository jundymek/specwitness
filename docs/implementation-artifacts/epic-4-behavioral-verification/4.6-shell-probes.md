# Story 4.6: Shell probes

Status: ready-for-dev

## Cohort & Dependencies (harness self-configuration)

**You are in COHORT 2 of THREE sequential cohorts.** Epic 4 does not launch as one wave. Epic 3 ran eight agents at once and paid ~7.5 hours to three subscription session-limit freezes (Epic 3 retro observation 1, owner action item C); the owner's decision on 2026-09-01 is three smaller cohorts against **one long-lived epic branch**.

```
master
  └── epic/4-behavioral-verification
        ├── cohort 1 (wave A):  4.1, 4.2                     ← ALREADY MERGED before you start
        ├── cohort 2 (wave B):  4.3, 4.4, 4.5, 4.6           ← you are here
        └── cohort 3:           4.7 + retrospective
```

**Your cohort has four agents: 4.3, 4.4, 4.5, you (4.6).** Expected mapping from launch order within *this* cohort: **alice=4.3, bob=4.4, pamela=4.5, arnold=4.6.** Mapping is positional **per cohort**, so cohort 2's `alice` is 4.3, **not** 4.1. An agent assuming Epic 3's mapping will depend on the wrong peer. If `tmux-overview.sh` disagrees, depend on whoever holds the story id.

Your PR targets **`epic/4-behavioral-verification`**, never `master`.

Run after reading this spec:

1. `~/.terminal-agents/scripts/set-cohort.sh alice,bob,pamela` — **only your own cohort's peers.**
2. Complete cohort **intent-sync** (write `intent.md`, read every peer's, ack).
3. **You set NO `set-depends-on` marker.** See the next paragraph.

> **Your predecessors merged before you started.** 4.2 (the Plan schema) and 4.1 (services) are on the epic branch when your cohort launches. A `depends-on` marker for either would block you waiting for an agent that no longer exists. **Verify rather than assume:**
> ```
> gh pr list --base epic/4-behavioral-verification --state merged --search "4.2-plan-model-compilation"
> ```
> If 4.2 is *not* merged, stop and report it — do not build against a schema that is not there.

> **You do NOT depend on 4.3's merge either.** The owner decided (2026-09-01) that the three surface stories depend on 4.2's schema only. 4.3 publishes its data-resolution shape in its `intent.md` during your shared intent-sync — read it there.

> The step ordering is not cosmetic even though you skip step 3: the intent gate blocks `set-depends-on` and `agent-wait` until intent-sync completes, and Epic 2 lost five agents to block loops in their first minutes over exactly this (H-10).

**After any unblock or `epic-updated` nudge:** `git fetch origin`, then — this project **squash-merges** — `git rebase --onto origin/epic/4-behavioral-verification <sha-of-your-last-merged-commit>` (H-4). **If your own PR is already merged, ignore a rebase nudge** (H-3).

### 4.2's merged source is your contract — there is nobody to negotiate with

Epic 3's wave-B agents could settle shapes with predecessors live. **That channel does not exist.** Read `src/schemas/plan.ts` as merged plus 4.2's PR body (a handoff document with a worked example plan) before writing code — **especially the shell probe's params and its argument-allowlist field**, which is the heart of your story and which 4.2 already shaped. **You may not widen the Plan schema in your branch**; an additive field is a message to the owner or a follow-up PR.

### Cohort-2 file ownership, and two things to settle at intent-sync

- 4.3 owns `src/pipeline/stages/data.ts` and its resolver.
- **4.4 creates `src/surfaces/` and owns `src/surfaces/http.ts` and `src/surfaces/index.ts`.** 4.5 owns `src/surfaces/observation.ts`. **You own `src/surfaces/shell.ts`** and add nothing else there. An export in `index.ts` is a one-line addition to coordinate with 4.4 — three agents editing one barrel file is how a trivial conflict becomes a bad one.
- **`src/pipeline/stages/probes.ts` is contested** between you, 4.4 and 4.5. Settle its owner at intent-sync and write the decision into all four `intent.md`s. **Recommendation: nobody in cohort 2 owns it — 4.7 does**, since it wires all three executors plus `deriveCriterionResult` and cannot be built correctly until all three exist.
- **The command-splitting question is shared with 4.5 — settle it once, together.** Both of you must turn a declared command line into binary + argv, and `src/pipeline/stages/gate-command.ts` already solves it (`splitCommandLine`, `hasUnterminatedQuote`, `usesUnsupportedEscaping`, `hasGluedExecutableSuffix`). But it lives under `src/pipeline/`, which `adapters-core-only` **forbids** `src/surfaces/**` from importing. Agree one answer — most likely that the **caller** splits and passes you binary + argv — and write it into both `intent.md`s. **Do not write a second splitter**, and do not move a merged module without the owner's say-so.

**Read 4.4's `intent.md` before designing your executor.** 4.4 is the first surface and sets the shape all three follow. **4.7 owns the surface-conformance test** proving all three behave identically for structurally identical outcomes (owner decision, 2026-09-01). Diverging from 4.4's shape is how that test fails in cohort 3, with nobody left to reconcile it.

## Story

As coverage for structural/stack-specific criteria,
I want shell probes constrained to config-declared commands with exit-code/output assertions,
So that white-box checks are possible without opening arbitrary execution (FR-26, AD-3).

## Acceptance Criteria

From `docs/planning-artifacts/epics.md` (authoritative — expand, never weaken):

1. **Given** a shell probe referencing a config-declared command id with an argument allowlist
   **When** it executes in the worktree
   **Then** exit code and output assertions produce the criterion result, output stored as evidence (bounded).
2. **Given** a plan attempting a shell probe with an undeclared id or out-of-allowlist arguments
   **When** validation or execution runs
   **Then** it is rejected before any execution (schema + runtime double enforcement).

Clarifications (detail only, no weakening):

- **⚠️ YOUR STORY IS THE ONE WITH A NAME THAT LIES.** It is called "shell probes" and **there is no shell**. `ProcessRunner.run` takes `(binary, args[])` and its header is explicit: *"There is no `shell` option and no way to add one without changing this file. That is what makes it impossible for provider output — text a model wrote — to become an executable command."* `&&`, `$(…)`, `;` and `*` reach the child as **literal argv elements**. If you ever reach for `sh -c`, `shell: true`, or string concatenation into a command line, stop — that is the single change that would undo the epic's security property. Say so in your module header, in your own words, so the next reader does not have to rediscover it.
- **AC2 IS THE SECURITY ACCEPTANCE CRITERION AND "DOUBLE ENFORCEMENT" IS ITS EXACT WORDING.** Two independent gates, and **you own the runtime half**:
  1. **Schema (4.2's, merged).** A plan cannot express a command string at all — a shell probe carries a config **id** plus an argument allowlist, and a draft carrying `run: "rm -rf /"` fails validation as an unknown key on a `z.strictObject`. **Verify that this is true of what 4.2 actually merged**, and if it is not, that is a finding for your PR body and a message to the owner — not something you patch in `src/schemas/plan.ts`.
  2. **Runtime (yours).** At execution, re-check: the id must be declared; every argument must be in the allowlist. **Reject before spawning anything.**
  The reason for both is stated in the AC itself and it is worth internalising: a schema gate protects against a *drafted* plan, and a runtime gate protects against a plan that was **edited on disk after compilation** — plans are committed YAML in the target project (Q11), so a human or another tool can change one between compile and run. Neither gate alone is sufficient.
- **"Rejected before any execution" must be tested by proving nothing ran**, not by asserting an error type. Inject a `ProcessRunner` that throws (or counts calls) if invoked at all, and assert zero calls. Epic 3's learning 3: a guard must encode the distinguishing fact, not a proxy for it.
- **What "the argument allowlist" means is 4.2's schema's answer, not yours to invent — but you must state your enforcement semantics precisely and test each one.** Read the merged field, then decide and document, at minimum:
  - What an **empty** allowlist means: no arguments permitted (recommended) versus all permitted (**dangerous — do not choose this default**). Fail closed.
  - Whether matching is **exact string equality** (recommended: simple, auditable, no surprises) or something looser. **A pattern language is a second parser and a second attack surface**; if 4.2's schema does not require one, do not build one.
  - How **duplicate** or **repeated** arguments are treated.
  - That an argument matching **no** allowlist entry rejects the whole probe — not just that argument.
  Whatever you choose, the rule must be legible from your module header, because it is a security boundary that a reader must be able to audit without running it.
- **The rejection is an `InfraError` or a `ConfigError`, never a product FAIL** (AD-6/AD-7). An undeclared id or an out-of-allowlist argument means the **plan is wrong**, which says nothing about whether the branch satisfies its contract. Exit 1 would tell a harness the branch has defects; exit 3 says SpecWitness could not reach a conclusion, which is what actually happened. `getObservationCommand` in `src/config/types.ts` is the merged precedent for the id case — it throws a `ConfigError` naming the declared ids rather than returning a fallback, deliberately, because *"quietly substituting anything would be a hole in the AD-3 boundary."*
- **⚠️ AC1's "produce the criterion result" is NOT YOUR CODE.** AD-13, enforced by merged code: a `SurfaceExecutor` **returns a `ProbeAttempt` and never a `CriterionStatus`** — *"there is nowhere in this interface to put one"*. The single producer is `deriveCriterionResult` in `src/domain/criterion-result.ts`. **You evaluate assertions and report what you saw.** Read that module's header before writing a line.
- **AC1's exit-code and output assertions are DATA, evaluated mechanically.** Produce one `AssertionEvaluation {description, satisfied, expected?, actual?}` per declared assertion, **including satisfied ones** (FR-28 needs expected/actual on non-pass results, and `deriveCriterionResult` reads `find(e => !e.satisfied)`). **Nothing is interpreted, nothing is inferred, no AI is consulted.**
- **The classification split you must get right — and it is subtler here than anywhere else in the epic.** For a shell probe, a non-zero exit code has **two** possible meanings and the plan decides which:
  - **The plan asserts an exit code** (`exitCode == 0`, or `== 3`, or whatever): a mismatch is an **unsatisfied assertion** ⇒ criterion `fail`. The probe ran, observed the exit code, and it was wrong. **This is a product observation.**
  - **The command could not run at all** — `not-found`, `spawn-failed`, `timed-out`: **`execError: ProbeExecError`** ⇒ criterion `error`. You could not look.
  **Do not copy the gates stage's mapping here.** Gates treat `completed` + non-zero as a *gate* failure because a gate's implicit assertion is "exit 0". Your probe's assertion is **explicit and comes from the plan** — a probe legitimately asserting `exitCode == 1` must **pass** when the command exits 1. Getting this backwards makes every negative-case probe unwritable. Write the distinction into your module header.
  - Map every `ProcessOutcome` arm with a `never` exhaustiveness check. The gates stage's `classify` is the structural model even though your mapping differs: a `switch` handling only `completed` would treat a missing binary as "no failure seen".
- **Q39 forecloses a third option: execution-time uncertainty is `error`, NEVER `needs_human`.** Exactly two NEEDS_HUMAN triggers exist, both compile-time (contract `verifiability: human`; plan `not-safely-automatable`). Nothing you observe creates a third.
- **⚠️ A PROBE MAY NEVER ADJUDICATE A HUMAN-VERIFIABILITY CRITERION.** `deriveCriterionResult` returns `needs_human` for `verifiability: human` **before it looks at attempts** — attempts do not override, unconditionally. Epic 3 caught and reverted a well-meaning "floor" variant; review called it a silent redesign of a recorded decision. You will not hit this if you never special-case it. **If you ever want a probe to decide a human criterion, that is an ADR in `docs/adr/`, not a branch** (Epic 3 retro §6).
- **Evidence: use `CommandEvidence`, and do not widen the union.** `EVIDENCE_KINDS` is closed and there is **no `shell` kind** — `command` is exactly right and is already used by the gates stage for the same purpose: `{kind: 'command', commandId, displayCommand, exitCode, stdout: BoundedText, stderr: BoundedText, capturedAt, durationMs}`. Widening the union is an ADR, not a branch. Use the merged constructor; never hand-build the object.
  - **Bounded output (AC1 says "bounded")**: `boundedText()` redacts *and* caps at `EVIDENCE_INLINE_CAP_BYTES` (8192) with `truncationMarker()` pointing at the full file. **Read the module header's rule 2**: `redactText` does not truncate and exists separately because both copies are persistence and both must be redacted, but only one may be truncated — *"a caller writing a full copy MUST pass it through `redactText` first"*, or the full file carries secrets verbatim while the seeded-secret test passes green against the inline copy. The gates stage's `persistStream` is the merged precedent — read it.
- **REDACTION IS FAIL-CLOSED, and you have both string kinds — treat them differently.**
  - The command's **text** (`commandText(command)` for `displayCommand`) is **declared** — the project owner wrote it in their config.
  - The command's **stdout/stderr** is capture output and must be passed **undeclared** (the fail-closed default), even though it came from a declared command. Epic 3 retro §6 states this for all Epic 4 callers; §2 observation 7 records the six leak shapes behind the rule: *shell context is declared by the caller, never inferred from text*, because an apostrophe in prose is indistinguishable from a shell delimiter.
  - **The arguments are the interesting third case.** They come from the **plan** — provider-authored text, constrained by an allowlist. They are not project-owner-declared, so **do not treat them as a declared shell string**. If you echo them into an error message (you probably should, so an operator can see which argument was rejected), redact them undeclared first. The gates stage redacts declared commands even in its error messages, and its comment explains why: an error reaches `printError`, which writes to stderr verbatim, so the persisted copy would be clean while the terminal showed the secret.
  - **The leak shape worth remembering** (retro §7): output containing `[REDACTED]` *with the secret still beside it* survives review in a way a raw leak does not. Assert the secret is **absent**, never that the marker is present.
- **AC1's "in the worktree"** means the cwd is `context.run.environment.worktreePath`, passed in by the caller — gates, services and data commands all run there and yours must too (AD-8, FR-19). Because `adapters-core-only` forbids `src/surfaces/**` from importing `src/config/**` or `src/pipeline/**`, the **caller resolves everything and passes it in**: the resolved command, the cwd, the allowlist check inputs. Design your constructor accordingly.
- **Retries are OPT-IN, default 0 (AD-9, Q43/Q44), and orchestration is NOT yours.** Your executor executes **one** attempt per call and stamps `attempt` (1-based). `deriveCriterionResult` decides `flaky`. **Do not loop internally.**
- **`durationMs` comes from the injected `Clock` (AD-9)**, never `Date.now()`. Integer milliseconds — and prefer the runner's own measurement, which already uses the injected clock, over a second clock read (the gates stage does exactly this).

## Tasks / Subtasks

- [x] Task 0 (first, before any code): read the merged contracts (AC: all)
  - [x] `src/domain/criterion-result.ts` **in full** — the AD-13 seam and the derivation you must not duplicate.
  - [x] `src/domain/evidence.ts` — `CommandEvidence`, its constructor, `redactText`, `boundedText`, `truncationMarker`, `EVIDENCE_INLINE_CAP_BYTES`, and the header's two rules.
  - [x] `src/schemas/plan.ts` (4.2, merged) + its PR body — **the shell probe params and the allowlist field**.
  - [x] `src/domain/process-runner.ts` (**the whole header** — the no-shell property is your story's foundation), `src/config/declared-command.ts` (the brand and why there is no escape hatch), `src/pipeline/stages/gates.ts` + `gate-command.ts`.
  - [x] **4.4's `intent.md`** — the executor shape all three surfaces share.
- [x] Task 1 (TDD): the executor skeleton (AC: 1)
  - [x] `src/surfaces/shell.ts` implementing `SurfaceExecutor` with `surface: 'shell'`. Constructor takes injected values only — a `ProcessRunner`, a `Clock`, a timeout, the resolved command, a cwd, the allowlist inputs. **It imports nothing from `src/config/**` or `src/pipeline/**`** and constructs no adapter.
  - [x] **Match 4.4's shape.** 4.7's conformance test depends on it.
  - [x] Tests: `surface === 'shell'`; `execute()` returns a `ProbeAttempt`; **a `CriterionStatus` is unrepresentable in the return type** (a compile-level assertion is ideal — `tests/unit/schemas/result-mirror.type.test.ts` shows the established pattern for making a structural rule a compile error).
- [x] Task 2 (TDD): the runtime allowlist gate (AC: 2) — **the story's most important code**
  - [x] Before spawning: the id must be declared (the caller resolved it; an unresolved id must not silently become a no-op), and **every argument must satisfy the allowlist**.
  - [x] Rejection is a thrown `InfraError`/`ConfigError` with an `ERROR:` + `HINT:` pair naming **which** argument was rejected and what is permitted.
  - [x] Tests: an out-of-allowlist argument ⇒ rejected, **and a runner injected to throw on any call was never called**; an undeclared id ⇒ rejected, nothing spawned; **an empty allowlist with arguments supplied ⇒ rejected** (fail closed); an argument that is a *prefix* or *superstring* of an allowed one ⇒ rejected (proves exact matching); a shell metacharacter smuggled into an allowed argument value is either rejected or **provably passed as a literal argv element that does nothing** — assert it server-side by having the fixture command echo its own `process.argv` and comparing.
- [x] Task 3 (TDD): execution and mechanical assertions (AC: 1)
  - [x] Spawn binary + argv in the worktree cwd with an explicit timeout; evaluate declared exit-code and output assertions.
  - [x] Tests (fixture commands written by the test): an `exitCode == 0` assertion satisfied and unsatisfied; **an `exitCode == 1` assertion that PASSES when the command exits 1** — the case the gates stage's mapping would get wrong; a stdout-contains assertion satisfied and unsatisfied; **every declared assertion appears in `assertionEvaluations`, including satisfied ones**; expected/actual name both values; **the argv the child actually received is exactly what was declared** (echo `process.argv` from the fixture and compare — this is the no-shell proof).
- [x] Task 4 (TDD): exec-error classification (AC: 1)
  - [x] `not-found`, `spawn-failed`, `timed-out` ⇒ `execError` with a message and a useful hint. Every `ProcessOutcome` arm mapped with a `never` check.
  - [x] Tests, producing the state: a genuinely missing binary ⇒ `execError`, **and feeding the attempt through `deriveCriterionResult` yields `error`, NOT `fail`**; a command exceeding an injected millisecond timeout ⇒ `execError`; **on every error path no `AssertionEvaluation` claims a product violation**; a non-zero exit **with an exit-code assertion declared** is a normal evaluation, **not** an `execError` (the distinction from gates).
- [x] Task 5 (TDD): evidence with redaction at capture (AC: 1)
  - [x] `command` evidence through the merged constructor for every attempt. Bounded inline; any full copy **through `redactText` first**, written via the injected evidence writer — **`RunStore` is the sole writer under `.specwitness/runs/`** (AD-8), so take a writer callback exactly as the gates stage does; never construct a run-directory path. `evidenceRef` paths are **relative** (Q48).
  - [x] Tests: **seeded-secret test** — a secret in stdout, in stderr, and in a rejected argument echoed into an error message; assert **the secret string is absent** from every stored artifact (inline evidence, the full-copy bytes handed to the writer, `expected`/`actual`, and every error message). The command *text* may be redacted as declared; **its output and the plan's arguments must not be** (fail-closed). Output over the cap is truncated with a marker naming the full file.
- [x] Task 6 (TDD): integration with the single derivation (AC: 1)
  - [x] Drive real `ProbeAttempt`s through `deriveCriterionResult` and assert **pass**, **fail**, **error** and **flaky** (failing attempt 1 then passing attempt 2 ⇒ `flaky: true`; passing then failing ⇒ `fail`, not flaky).
  - [x] A non-pass result carries `expected`, `actual` and ≥1 evidence reference (FR-28).
  - [x] **Do not re-implement any of it** — you are proving your attempts feed the merged function correctly.
- [x] Task 7: the AD-3 boundary, verified rather than assumed (AC: 2)
  - [x] **Never mint a `DeclaredCommand`, never cast, never import past `src/config/index.ts`** — and per `adapters-core-only` you import `src/config/**` not at all. Commands arrive resolved from the caller.
  - [x] **`tests/unit/config/boundary-scan.test.ts` already covers `src/surfaces/**` the moment the directory exists — verify, do not author a second scan.** Plant a probe containing `x as DeclaredCommand` in your file, watch the named failure, remove it. **Yours is the story where this matters most**, since you are the surface that runs commands. If it does not go red, that is a real finding — report it.
  - [x] **`adapters-core-only` already covers `src/surfaces/**`** (verified against merged `.dependency-cruiser.cjs` on 2026-09-01; its `from` is `^src/(config|infra|providers|surfaces)/`). **Verify it fires** — add a deliberate `src/pipeline/` import, watch `pnpm depcruise` name the rule, remove it. **Do not author a rule.**
  - [x] **State the no-shell property in your module header, in your own words**, the way the gates stage does for its own first-execution moment.
- [x] Task 8: hygiene (AC: all)
  - [x] `pnpm depcruise`, `pnpm typecheck`, `pnpm lint`, `pnpm test` green. **`pnpm lint` forwards to `depcruise` and rejects eslint flags** — the auto-review wrapper's `pnpm lint --no-fix` failure is harness defect H-7, not yours.
  - [x] **Do not name a test file `*.env.test.ts`** (H-13). **New tests must be concurrency-safe** — the auto-review runs `pnpm test` in your worktree concurrently with you (H-8); use `mkdtemp` for fixture commands, no fixed-name scratch files under `src/`.
  - [x] **Your suite leaves no orphaned processes.** You spawn real commands. Epic 2's timeout test leaked nine `sleep 3600` processes onto this machine.

## Dev Notes

### Goal & Context

Shell probes exist because some criteria are not observable over HTTP or through a state query — a migration was applied, a generated file has the right shape, a binary reports the expected version. FR-26 calls them *"structural criteria and stack-specific checks"*, and the story's subtitle is the whole design: **"without opening arbitrary execution."**

That phrase is why your story is the security-sensitive one in cohort 2. Every other surface either touches no shell (HTTP) or runs a command the project owner declared in full (observation). Yours runs a declared command **with arguments a provider chose**. The allowlist is what keeps that from being a hole, and the no-shell spawn is what keeps a hole from being catastrophic even if the allowlist were wrong.

Two layers of defence, stated plainly:

1. **The plan cannot name a command.** It names a config id. The command text comes from the project owner's file, and `DeclaredCommand` can only be minted inside `src/config/` — *"there is no code path — none — by which a raw string, or output from a `claude`/`codex` subprocess, becomes an executable command."*
2. **The plan's arguments are checked against an allowlist, at compile time and again at run time**, and the process is spawned as binary + argv with no shell, so even an argument that slipped through is inert as a metacharacter.

You are building the second half of both.

### Exact scope

- `src/surfaces/shell.ts`
- One coordinated line in `src/surfaces/index.ts` (4.4's file — coordinate at intent-sync)
- `tests/unit/surfaces/shell*.test.ts`, `tests/integration/surfaces/shell*.test.ts`
- Fixture commands you write in tests
- `docs/implementation-artifacts/sprint-status.yaml` — your row only

### Out of scope (do NOT build)

- **`deriveCriterionResult`, `CriterionStatus`, retry orchestration, `flaky` — merged in `src/domain/criterion-result.ts`.** Call it in tests; never duplicate or edit it.
- **`aggregate()`, `RunOutcome`, exit codes.** You touch none of them.
- **The Plan schema and its allowlist field — 4.2, merged.** Read; never edit. A gap is a finding, not a licence.
- **HTTP probes — 4.4.** **Observation probes — 4.5.** Cohort peers with their own files.
- **`src/surfaces/index.ts` as a file you create** — 4.4 owns it.
- **A second command splitter.** `src/pipeline/stages/gate-command.ts` exists; settle the layer question with 4.5 at intent-sync.
- **Deterministic data resolution — 4.3.** **Services — 4.1, merged.** **The `data` stage — 4.3.**
- **Browser probes / Playwright — Epic 5.** Do not add `@playwright/test`.
- **The probes stage** (`src/pipeline/stages/probes.ts`) unless intent-sync assigns it to you in writing. Recommendation: it is 4.7's.
- **The surface-conformance test — 4.7 owns it** (owner decision, 2026-09-01). You make it possible by matching 4.4's shape.
- **Wiring anything into `verify` — 4.7.** Nothing in `src/cli/` is yours.
- **Any `shell: true`, `sh -c`, or command-line string construction.** Not merely out of scope — **forbidden**, and the reason the epic has a security property at all.
- **Widening `EVIDENCE_KINDS`, `PROBE_SURFACES`, `CRITERION_STATUSES` or `SurfaceExecutor`.** Closed; widening is an ADR.

### Dependencies & upstream contracts (by ID)

All merged. **Read the source, not this table** — Epic 2's lesson 5, re-confirmed in Epic 3: verify shapes against merged source, not a summary, *including this spec's summaries*.

| What | Where | Why |
| --- | --- | --- |
| **AD-13: the interface you implement** | `src/domain/criterion-result.ts` | `SurfaceExecutor`, `ProbeRequest`, `ProbeAttempt`, `Observation`, `AssertionEvaluation`, `ProbeExecError`, `deriveCriterionResult` |
| **AD-3: the no-shell spawn** | `src/domain/process-runner.ts` | `ProcessRunOptions {binary, args}`, `ProcessOutcome`, `ChildEnvironment`. **Read the whole header** |
| **AD-3: the brand and why there is no escape hatch** | `src/config/declared-command.ts` | Why you never mint |
| **AD-10: evidence + redaction** | `src/domain/evidence.ts` | `CommandEvidence`, its constructor, `redactText`, `boundedText`, `truncationMarker`, `evidenceRef` |
| **The Plan schema** | `src/schemas/plan.ts` (4.2) + its PR body | Shell probe params, **the allowlist field**, assertions |
| The first module that executed a declared command | `src/pipeline/stages/gates.ts` | Classification with `never`, evidence ordering, redaction in error messages, `persistStream`. **Your mapping deliberately differs — see the clarifications** |
| Command splitting + its three refusals | `src/pipeline/stages/gate-command.ts` | Do not write a second one |
| The declared-command accessor pattern | `src/config/types.ts` `getObservationCommand` | Throws naming declared ids; never substitutes |
| Result vocabulary | `src/domain/result.ts` | `CriterionStatus`, `CriterionResult` |
| Contract types | `src/domain/contract.ts` | `Severity`, `Verifiability` — **and the unconditional human⇒NEEDS_HUMAN clause** |
| Determinism ports | `src/domain/ports.ts` | `Clock` — injected, never `Date.now()` |
| Sole run-directory writer | `src/infra/run-store.ts` | `writeEvidenceFile` (injected, never constructed) |
| Layer rules | `.dependency-cruiser.cjs` | `adapters-core-only` — **already covers you** |
| The shape all surfaces share | 4.4's `intent.md`, then `src/surfaces/http.ts` | 4.7's conformance test depends on the match |

### Architecture compliance

- **AD-3 — your invariant.** Only Project-Config-declared commands reach execution; plans reference them **by config id plus an argument allowlist**, never by command string; a draft containing an inline command string fails schema validation; no shell, ever.
- **AD-13.** One executor interface, one `ProbeAttempt`, **never** a `CriterionStatus`.
- **AD-1.** `src/surfaces/**` is an adapter: `src/domain/**`, `src/schemas/**`, siblings and npm only — **not `src/config/**`, not `src/pipeline/**`**, not application layers, not `cli`.
- **AD-6/AD-7.** An unsatisfied assertion is product `fail` (exit 1); "could not run" and "the plan is wrong" are both `error`/thrown (exit 3). Never interchangeable.
- **AD-8.** Commands run in the worktree; nothing is written into the project working tree; `RunStore` is the sole writer under `.specwitness/runs/`.
- **AD-9.** Retries opt-in, default 0, orchestration outside your executor; `Clock` injected.
- **AD-10.** Redaction at capture, through the constructors, fail-closed.
- **AD-12.** Hermetic: fixture commands built by the test, no network beyond localhost, no real providers.
- **Conventions.** ISO-8601 UTC; integer ms; `ERROR:` + `HINT:` pairs; bounded output with a pointer to the full file.

### Testing requirements

- **TDD, red first, guards seen red.** Epic 3's learning 3: a guard must encode the distinguishing fact, not a proxy for it. **Your allowlist tests are the ones this applies to most** — a test that passes before the check exists proves nothing.
- **"Rejected before any execution" is proven by a runner that was never called**, not by an error type.
- **The no-shell property is proven by echoing `process.argv` from the fixture** and comparing, not by asserting the absence of a flag.
- **Fixture commands are written by the test** — a small `node -e` script or a temp file under `mkdtemp`. Never against this repository, never against the network beyond localhost.
- **Unit tests spawn zero subprocesses**; only the integration suite spawns.
- **The Golden Verification Corpus proper is Epic 6.** Your fixtures are inline. Say so in your PR body so nobody reads a passing suite as corpus coverage.

### Integration expectations

4.7 wires your executor into the probes stage in cohort 3 and **cannot ask you anything**. Your PR body must state:

1. The exported executor's name and constructor signature — exactly what the caller must inject, **including that the caller resolves the config id to a command and supplies the cwd** (you cannot import `src/config/**` or `src/pipeline/**`).
2. **Your allowlist semantics, precisely**: matching rule, empty-allowlist meaning, duplicate handling, and what rejection throws.
3. **Your exit-code classification**, and explicitly that it differs from the gates stage's, with the reason.
4. That you evaluate assertions and **never** produce a `CriterionStatus`.
5. Whether the command-splitting layer question was resolved with 4.5, and how.

### Failure modes (what "wrong" looks like here)

- **Reaching for a shell.** `sh -c`, `shell: true`, string concatenation into a command line. The one change that undoes the epic's security property, and it will look like the obvious way to support a pipe.
- **An empty allowlist meaning "anything permitted."** Fail-open, and it will read as a reasonable default.
- **Prefix or substring matching** in the allowlist, so `--dry-run` permits `--dry-runner`.
- **Checking the allowlist after spawning**, or only at schema time. AC2 says "before any execution" and "double enforcement".
- **Copying the gates stage's exit-code mapping**, so a probe legitimately asserting `exitCode == 1` reports `error` or `fail`.
- **A missing binary reported as `fail`.** Infrastructure blamed on the branch.
- **Returning or inferring a `CriterionStatus`.** Four surfaces, four answers, one irreproducible verdict.
- **Treating plan-supplied arguments as declared shell strings** for redaction. They are provider-authored; redact undeclared.
- **A secret in the full evidence copy while the inline copy is clean** — the exact hole `redactText`'s separate existence closes.
- **Retrying inside the executor.** `flaky` becomes wrong and the attempts are invisible.
- **Diverging from 4.4's executor shape**, which fails 4.7's conformance test in cohort 3.

### Security

**This is the story where a mistake is a vulnerability rather than a wrong answer.** Read `src/config/declared-command.ts`'s header in full before you start; it is written for exactly this moment.

- No shell. Binary + argv through `ProcessRunner`. There is no supported way to add one, and adding one is not a story-level decision.
- Never mint a `DeclaredCommand`. No `as DeclaredCommand`, no escape hatch in `src/config/`, no `unsafeDeclaredCommand()`.
- The allowlist fails **closed**: anything not explicitly permitted is rejected.
- Reject **before** spawning, and prove it by a runner that was never called.
- Command output and plan-supplied arguments are **untrusted** — redact undeclared everywhere they travel: evidence, `expected`/`actual`, error messages, timeline details.
- Assert secrets are **absent**, not that `[REDACTED]` is present.
- Never read or copy `~/.claude/`, `~/.codex/` or any credential store (NFR-1).
- Commands run in the worktree, never in the source repo or the operator's workspace.
- The epic's end-to-end seeded-secret proof is 4.7's; **your capture path's own proof is yours** (Task 5).

### Project Structure Notes

The spine's Structural Seed reserves `surfaces/` for *"http, browser (playwright), observation, shell executors"*, and `.dependency-cruiser.cjs` already names it in `adapters-core-only`. You are filling reserved space, which is why the directory and its layer rules are not negotiable.

### References

- Epic ACs: `docs/planning-artifacts/epics.md` § "Story 4.6"
- Roadmap: `docs/planning-artifacts/roadmap.md` § "EPIC 4" (wave B; shared contract *"AD-3 config-id command referencing"* is the heart of your story)
- Spine: **AD-3, AD-13, AD-10**, AD-1, AD-6, AD-7, AD-8, AD-9, AD-12; Conventions; Stack
- Questions: **Q14** (commands declared only in config), **Q15** (safe execution: ProcessRunner, own process group, timeout, captured output; *"plans reference commands by config id, never inline strings"*), **Q40/Q41/Q42** (pass / fail / infra representation), **Q39** (two NEEDS_HUMAN triggers; execution-time uncertainty is `error`), **Q43/Q44** (retries, flake), **Q47/Q48/Q49** (evidence union, relative paths, sanitisation and caps)
- PRD: FR-26, FR-28, FR-22, FR-27, FR-32; NFR-2, NFR-3; PRD §5 security non-goals
- Epic 3 retro: `docs/findings/epic-3-retro-2026-09-01.md` — §6 (human⇒NEEDS_HUMAN unconditional; `{shellCommand}` fail-closed), §2 observation 7 (six redaction leak shapes), §9 (gates no agent can run)
- Harness defects: `docs/findings/epic-2-harness-defects-2026-08-31.md` — H-3, H-4, H-7, H-8, H-10, H-13

### Gates you cannot satisfy — report as pending-owner, never as passed

- **Any CI-executed step.** No workflow has run in three epics (action item E).
- **Linux**; **Node exactly 22.12** (everything has run on macOS/Darwin and 22.20). **Argv handling and process semantics are platform-sensitive** — say explicitly that Linux is unverified.
- **Real `claude`/`codex` invocations.** Not applicable — this story makes zero provider calls, which is worth stating.

## Dev Agent Record

### Context Reference

- Story spec: this file. Cohort 2 of Epic 4 (alice=4.3, bob=4.4, pamela=4.5, arnold=4.6),
  against the long-lived `epic/4-behavioral-verification` branch.
- Predecessors verified merged before any code was written: 4.2 (`0a3c3c8`, PR #42) and
  4.1 (`9452443`, PR #41). No `depends-on` marker set, per the spec.
- Merged contracts read in full before implementation: `src/domain/criterion-result.ts`,
  `src/domain/evidence.ts`, `src/domain/plan.ts`, `src/schemas/plan.ts`,
  `src/domain/process-runner.ts`, `src/config/declared-command.ts`,
  `src/pipeline/stages/gates.ts`, `src/pipeline/stages/gate-command.ts`,
  `src/pipeline/stages/gate-evidence-path.ts`, `src/config/types.ts`,
  `src/domain/run-result.ts`, `src/report/terminal.ts`, `.dependency-cruiser.cjs`.
- Cohort intent-sync: `intent.md` in this worktree, plus 4.3's, 4.4's and 4.5's.
- Decision log: `DECISIONS.md` (D1-D15).

### Agent Model Used

Claude Opus 5 (`claude-opus-5`), Claude Code, autonomous supervised-cohort mode.

### Debug Log References

**Guards verified RED before being trusted** - each plant was made, the named failure
observed, and the plant removed:

1. **`tests/unit/config/boundary-scan.test.ts` covers `src/surfaces/**`.** Planted
   `x as unknown as DeclaredCommand`; the scan failed with
   `{"detail": "as-assertion at line 848", "file": "src/surfaces/shell.ts"}`. Not authored -
   it already covered the directory the moment it existed, as the spec predicted.
2. **`adapters-core-only` covers `src/surfaces/**`.** Planted an import of
   `../pipeline/stages/gate-command.js`; `pnpm depcruise` reported
   `error adapters-core-only: src/surfaces/shell.ts -> src/pipeline/stages/gate-command.ts`.
   No rule authored.
3. **The seeded-secret guard is not vacuous.** Replaced `redactText(result[stream], ...)` with
   the raw stream in the full-copy write; the seeded-secret test failed on exactly that hole -
   the inline copy stays clean while the file beside it carries the credential, which is the
   hole `redactText`'s separate existence closes.
4. **The AD-13 compile guard is not vacuous.** Widened `execute`'s return type to
   `Promise<ProbeAttempt & {status: CriterionStatus}>`; `tsc` named both
   `returnsExactlyAProbeAttempt` and `noCriterionStatusAnywhereInTheReturn`.

**Observed, not fixed (not my story):** `tests/unit/domain/evidence-perf.test.ts >
"handles a 256 KB unbroken run without hanging"` failed once during a full-suite run
(2447ms against a 2000ms wall-clock threshold) and passed in isolation and on an immediate
re-run of the full suite (123 files, 2488 passed). It is a machine-speed-dependent threshold
in a merged test, made flaky by four cohort agents running `pnpm test` concurrently on one
machine (H-8's shape). My branch touches no file under `src/domain/`. Reported rather than
patched, per the cohort addendum.

### Codex Review Findings

**2 findings, both real, both FIXED** (full reasoning in `DECISIONS.md` D16/D17):

1. **[P1] `onProcessGroup` was never forwarded to the runner.** Every shell probe
   started in its own process group but its pgid never reached the run manifest, so
   teardown and `specwitness clean` could not find or terminate a probe or its
   descendants after an interrupted run — an AD-8 lifecycle gap. Verified before
   accepting: both merged spawning modules carry the hook for exactly this reason
   (`pipeline/stages/gates.ts:144`, `pipeline/stages/services.ts:405`). Fixed with an
   optional dep forwarded conditionally, matching the gates stage.

2. **[P2] Evidence stems could collide, silently overwriting evidence.** Codex
   reported the truncation case (the slug caps at 64 characters while `Identifier`
   permits 128). On investigation **the defect is broader than reported**:
   substitution collides with no truncation at all, because `a.b` and `a..b` are two
   distinct schema-valid probe ids that 4.2 permits side by side within one criterion
   and that both normalise to `a.b`. Either way the second write overwrites the first
   and the first probe's evidence references then point at another probe's content —
   silently wrong evidence, in a product whose only output is evidence. The merged
   `gate-evidence-path.ts` solves this with the gate's declaration index and its
   comment names the truncation case explicitly; I carried across its sanitising but
   not its uniqueness guarantee. Fixed with an 8-hex FNV-1a fingerprint of the raw
   id, appended **only when the slug lost information**, so ordinary names stay
   readable and no params change is imposed on 4.7.

All three new guards were seen red before the fixes landed.

### Completion Notes List

**What was built.** `src/surfaces/shell.ts` - the `shell` `SurfaceExecutor`. It runs a
caller-resolved, config-declared command with plan-supplied argv in the verification
worktree, through `ProcessRunner.run({binary, args})` with **no shell**, evaluates the
plan's exit-code and output assertions mechanically, captures redacted bounded `command`
evidence, and returns a `ProbeAttempt`. It never produces a `CriterionStatus`.

**AC1 - satisfied.** Exit-code and output assertions produce one `AssertionEvaluation` each
(including satisfied ones, which FR-28 and `deriveCriterionResult`'s
`find(e => !e.satisfied)` both need); output is stored as bounded `command` evidence with
full copies beside it. Proven end-to-end by feeding real attempts through the merged
`deriveCriterionResult` and asserting `pass`, `fail`, `error` and `flaky`.

**AC2 - satisfied, and the schema half verified rather than assumed.** 4.2's merged
`ShellProbeSchema` is a `z.strictObject` carrying `{commandId, args, argumentAllowlist}`
with a `superRefine` already enforcing `args` as a subset of `argumentAllowlist` - there is
nowhere in it to put a command string. **No finding; the schema half is present as the AC
requires.** The runtime half is mine: the command id must match the resolved command and
every argument must be in the allowlist, checked immediately before spawning. Proven by a
`ProcessRunner` injected to throw on any call, asserting **zero calls** - not by asserting
an error type, which would pass just as green if the rejection happened after the spawn.

**Allowlist semantics (D1-D3), stated in the module header as a security boundary a reader
must be able to audit without running it:** exact string equality; an empty allowlist permits
no arguments (fail closed); repeats permitted, matching the merged schema's own `Set`
membership test; any argument matching no entry rejects the whole probe; an unused entry is
not an error. Prefix and superstring cases are both tested.

**The classification split (D5).** `completed` with any exit code is a normal evaluation -
a probe asserting `exitCode == 1` **passes** when the command exits 1, proven against a real
subprocess. Only `not-found` / `spawn-failed` / `timed-out` set `execError`. This
deliberately differs from the gates stage, and the header says why: a gate's implicit
assertion is "exit 0" while a probe's is explicit and comes from the plan. Exhaustive over
`ProcessOutcome` with a `never` check, as is the comparison switch over
`ASSERTION_COMPARISONS` and the target switch.

**Deviations from the spec, all additive and all recorded:**

1. **`recordEvidence` - one dep the spec does not mention.** `ProbeAttempt.evidence` is
   `readonly EvidenceRef[]`, and there is nowhere in AD-13 to put the typed member - but
   `RunResult.evidence` is the closed *union*, and `src/report/terminal.ts:189` renders from
   the member inline because AD-11 forbids a renderer to open a file. Refs-only would have
   produced reports with gate evidence and **no probe evidence at all**, silently, with every
   surface suite green. Raised by 4.5 at intent-sync, resolved with the citation above, ruled
   the cohort shape by 4.4 after independent verification, adopted by all three surfaces. No
   widening of `ProbeAttempt` - `adapters-core-only` itself prescribes an injected port as the
   remedy for an adapter that cannot reach the caller. See D6.
2. **`argumentAllowlist` arrives substituted alongside `args`** (4.3's catch, D9). Without it
   every data-binding probe would reject forever and a `volatile` binding could never be
   passed at all. No code change here; the check still runs on the final post-substitution
   argv. I added a diagnostic hint naming half-substitution when a placeholder survives a
   rejection - diagnostic only, nothing interprets the syntax.
3. **A local `slugify` (D14)** because `gate-evidence-path.ts` is under `src/pipeline/` and
   `adapters-core-only` forbids importing it. Duplication is the cost of the layer rule;
   flagged for 4.7 to consolidate.

**Not done, deliberately:** `src/surfaces/index.ts` carries no re-export line for this
module - 4.4 owns that file and it had not merged when this PR opened. Stated in the PR
body; one line for me after 4.4 merges, or for 4.7. Everything else in Tasks/Subtasks is
complete.

**Gates that cannot be satisfied here, reported as pending-owner rather than passed:**
CI has never run in three epics; **Linux is UNVERIFIED** - everything ran on macOS/Darwin
with Node 22.20, not Node exactly 22.12, and this story's argv handling and process
semantics are platform-sensitive, which is why the spec singles it out. Zero provider calls
are made by this story, so real `claude`/`codex` invocation is not applicable. The Golden
Verification Corpus is Epic 6; every fixture here is inline and built by its test, so a
passing suite must not be read as corpus coverage.

### File List

| File | Mode | Why |
| --- | --- | --- |
| `src/surfaces/shell.ts` | NEW | The shell surface executor. The only production file. |
| `tests/unit/surfaces/shell.helpers.ts` | NEW | Test doubles, including the runner that fails the test if it spawns. |
| `tests/unit/surfaces/shell.test.ts` | NEW | Skeleton, AD-8 spawn shape, and the AC2 allowlist gate. |
| `tests/unit/surfaces/shell.evaluation.test.ts` | NEW | Assertions, classification, evidence, redaction, derivation, and the two Codex-review guards. |
| `tests/unit/surfaces/shell-executor.type.test.ts` | NEW | AD-13 as a compile error: no `CriterionStatus` in the return type. |
| `tests/integration/surfaces/shell.test.ts` | NEW | Real subprocesses; the no-shell proof via the child's own `process.argv`. |
| `docs/implementation-artifacts/epic-4-behavioral-verification/4.6-shell-probes.md` | UPDATE | Tasks marked, this record. |
| `docs/implementation-artifacts/sprint-status.yaml` | UPDATE | My row only. |
| `DECISIONS.md`, `PR_SUMMARY.md`, `intent.md` | NEW | Harness bookkeeping, branch-local, not shipped code. |

**Not touched:** `src/surfaces/index.ts`, `src/surfaces/http.ts`, `src/surfaces/observation.ts`,
`src/pipeline/**`, `src/config/**`, `src/schemas/**`, `src/domain/**`, `src/cli/**`,
`.dependency-cruiser.cjs`, `docs/planning-artifacts/**`, `docs/adr/**`.
