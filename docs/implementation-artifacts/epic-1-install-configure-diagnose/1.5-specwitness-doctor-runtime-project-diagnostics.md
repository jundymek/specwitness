# Story 1.5: `specwitness doctor` — runtime & project diagnostics

Status: ready-for-dev

## Cohort & Dependencies (harness self-configuration)

All six Epic 1 stories launch at once; the harness serializes them via depends-on. PRs target the epic integration branch `epic/1-install-configure-diagnose` (never `master`). Expected agent↔task mapping from launch order: alice=1.1, bob=1.2, pamela=1.3, arnold=1.4, rambo=1.5, chuck=1.6 — if `tmux-overview.sh` shows a different mapping, depend on whoever holds the story id named below, not the name.

Run immediately after reading this spec:

- `~/.terminal-agents/scripts/set-cohort.sh alice,bob,pamela,arnold,chuck`
- `~/.terminal-agents/scripts/set-depends-on.sh pamela:done` — story 1.3 must be MERGED into the epic branch before you write code. You start BLOCKED; the harness auto-unblocks you on the merge.

**Immediately after you unblock:** your worktree was created before that merge — run `git fetch origin && git rebase origin/epic/1-install-configure-diagnose` before doing anything else.

## Story

As a developer,
I want `doctor` to check my runtime and project configuration,
so that I trust failures are diagnosed before they masquerade as verification errors (FR-3).

## Goal & Context

Doctor is the trust-building diagnostic (UJ-4): it separates "your environment isn't ready" from "your product failed verification" BEFORE any verify run exists. This story ships the runtime + project half of FR-3 (Node, Git, Playwright capability, config validity, base branch, command resolvability) built on an extensible **check registry** — the seam Epic 2 story 2.7 uses to plug in provider checks (binary/version/auth/billing-risk) without modifying anything you write. Output is agent-consumable: per-check pass/warn/fail, `--json` with a stable shape, prompt-free.

Wave C story: runs in parallel with 1.4 (init) and 1.6 (run storage). **You own `src/cli/commands/doctor.ts` (replacing the 1.1 stub) and the new `src/cli/doctor/` module; do not touch `init.ts`, `report.ts`, `src/infra/run-store*`, `templates/`, or anything 1.4/1.6 own this wave.**

## Acceptance Criteria

From `docs/planning-artifacts/epics.md` (authoritative — expand, never weaken):

1. **Given** a project with config
   **When** I run `specwitness doctor`
   **Then** it reports pass/warn/fail per check: Node version, Git present, Playwright capability, config validity, base branch exists, declared gate/service/observation commands resolvable — and exits non-zero iff a required check fails.
2. **Given** `--json`
   **When** doctor runs
   **Then** a stable JSON checks array (id, status, detail) is emitted with an ISO-8601 UTC timestamp.
3. **Given** doctor's check registry
   **When** Epic 2 adds provider checks
   **Then** they plug into the same registry without modifying existing checks (extension seam demonstrated by test).

Clarifications (detail only, no weakening):

- AC1 required vs optional: required (fail ⇒ non-zero exit) = node-version (>=22.12), git-present, config-valid (when `.specwitness/config.yaml` exists — a missing config file is itself a required failure with `HINT: run specwitness init`), base-branch-exists, commands-resolvable. Optional (⇒ warn, exit stays 0) = playwright-capability (only browser probes need it — Epic 5), ports-free (added per Q27: declared service ports currently bindable; warn naming port + likely PID hint).
- AC1 exit code: required-check failure ⇒ exit **3** (environment/config = infra class per ADR-002; never 1 — that's a product FAIL, and never 2). All checks still run and are all reported (no early stop on a failed check).
- AC1 "commands resolvable": resolve the FIRST token of each declared gate/service/observation/setup/data command — on PATH (`which` semantics implemented via PATH scan, not a shell) or as an existing file relative to project root. **Never execute the commands** — resolution only (AD-3: execution paths arrive in Epic 3).
- AC1 "base branch exists": `git rev-parse --verify --quiet <baseBranch>` (accept local or `origin/<baseBranch>`) via execa — git is trusted tooling, not a project-config command, so invoking it is allowed.
- AC2 stable JSON: `{schemaVersion: 1, timestamp: <ISO-8601 UTC>, status: 'pass'|'warn'|'fail', checks: [{id, status: 'pass'|'warn'|'fail', required, detail}]}` on **stdout** with nothing else on stdout; human rendering goes to stderr in `--json` mode. Check order deterministic (registration order). Snapshot-test the shape.
- AC3 seam: registry accepts `DoctorCheck` objects `{id, required, run(ctx): Promise<CheckResult>}` where `ctx` carries project root + loaded-config-or-error; test registers a fake extra check and asserts it runs and reports without any existing file changing.

## Tasks / Subtasks

- [x] Task 1 (TDD): registry + check contract (AC: 3)
  - [x] `src/cli/doctor/registry.ts`: `DoctorCheck`/`CheckResult` types, ordered registration, `runAll(ctx)` → results (checks isolated: one check throwing ⇒ that check reports `fail` with the error detail, others still run)
  - [x] `src/cli/doctor/context.ts`: build ctx — project root (CWD), attempt `loadConfig` (1.3) capturing `ConfigError` instead of throwing (config-valid check consumes it; downstream checks needing config skip gracefully with `warn`/`fail` detail "config invalid")
  - [x] Extension-seam test (fake check)
- [x] Task 2 (TDD): built-in checks (AC: 1)
  - [x] `src/cli/doctor/checks/node-version.ts` — `process.version` >= 22.12, required
  - [x] `checks/git-present.ts` — `git --version` succeeds, required
  - [x] `checks/config-valid.ts` — required; detail = the `ConfigError` message (YAML path + reason) on failure
  - [x] `checks/base-branch.ts` — required; skips-to-fail with clear detail when config invalid/missing
  - [x] `checks/commands-resolvable.ts` — required; lists every unresolvable command id + token in detail
  - [x] `checks/playwright-capability.ts` — optional/warn; detect project-local `@playwright/test` (node_modules resolution from project root) else warn `HINT:`-style detail that browser probes (Epic 5) will need provisioning; do not download anything
  - [x] `checks/ports-free.ts` — optional/warn; for each declared service `port`, attempt a localhost bind+release
- [x] Task 3 (TDD): command + renderers (AC: 1, 2)
  - [x] Replace stub `src/cli/commands/doctor.ts`: run registry, render human report (one line per check: ✓/⚠/✗, id, detail) to stdout in normal mode, summary line, then exit via the 1.1/1.2 table: all-required-pass ⇒ 0, any required fail ⇒ throw `InfraError`-mapped path or return code 3 through `cli/exit.ts` (never a literal `process.exit` here)
  - [x] `--json` mode per AC2 (stdout = JSON only) + snapshot test
- [x] Task 4: integration tests
  - [x] Spawn built CLI against fixture projects in tmp dirs: healthy config (exit 0), broken config (exit 3, config-valid fail names YAML path), missing base branch (exit 3), unresolvable gate command (exit 3), no config file (exit 3 with init hint), `--json` parseable with `JSON.parse` and ISO-8601 UTC timestamp

## Dev Notes

### Exact scope

- `src/cli/commands/doctor.ts` (stub replacement), `src/cli/doctor/` (registry, context, checks), tests + fixtures.

### Out of scope (do NOT build)

- Provider checks: claude/codex binary discovery, version/capability probe, auth readiness, billing-risk env-var warnings — ALL Epic 2 (story 2.7) via your registry seam. Do not pre-build placeholders for them.
- Executing any config-declared command (resolution only). Playwright provisioning/downloads (Epic 5 story 5.1). Fixing anything (doctor diagnoses only). `--verbose` logging infrastructure (later).

### Dependencies & upstream contracts

- Requires merged: 1.1 (CLI skeleton, stub, exit table, `InfraError`), 1.2 (nothing direct beyond exit-table completion), 1.3 (`loadConfig`, `ConfigError`, config shape incl. service `port`).
- Binding: FR-3 (+ consequences: doctor never reads `~/.claude`/`~/.codex`; non-zero iff required check fails; pass/warn/fail per check), NFR-1, Q27 (port pre-check), NFR-8/Conventions (prompt-free, bounded output, ISO-8601 UTC), ADR-002 (exit 3 class).

### Architecture compliance

- AD-1: `src/cli/doctor/**` is edge code — may import `config/`, use `execa`/`net`/`fs`. Keep checks small and single-purpose; the registry is the only orchestrator.
- Exit codes only via `cli/exit.ts` (1.1's scan test will catch violations).
- Env vars: this story needs no env reads beyond PATH (allowed in `cli/`).

### Testing requirements

- Unit: registry semantics (ordering, isolation, extension), each check against fake ctx (inject fakes for fs/exec where practical — checks should take their effects via ctx or small injectable fns so unit tests don't need real git).
- Integration: real spawns per Task 4 (macOS + Linux CI). Snapshot: `--json` shape (schemaVersion 1 — future changes must be additive per NFR-7 discipline).

### Integration expectations

- Story 2.7 registers provider checks into your registry and doctor gains billing warnings — your `DoctorCheck`/`CheckResult`/ctx types become public contract at merge; design them for that consumer (ctx must be extensible without breaking existing checks — e.g. a single ctx object, not positional args).
- Epic exit criteria served: "Doctor reports runtime + config checks with `--json`; config validation errors name YAML paths."
- UJ-4 edge case (no agent CLI installed → generation unavailable but execution works) lands with 2.7 — your required/optional split must not make a missing provider fatal.

### Failure modes to consider

- Config invalid ⇒ dependent checks (base-branch, commands-resolvable, ports-free) must degrade with informative detail, not throw or silently pass.
- git installed but CWD not a repo ⇒ base-branch check fails with detail (distinct from git-absent).
- PATH resolution on macOS vs Linux CI images; commands declared as relative paths (`./scripts/x.sh`) resolve against project root.
- Port check race (bind/release) — treat any bind failure as "occupied" warn; never leave the socket open.
- A check hanging (e.g. git on a weird repo) — put a timeout on subprocess checks; timeout ⇒ fail with detail.

### Security

- NFR-1 is a TESTED property here: doctor must never read `~/.claude/`, `~/.codex/`, or any credential store — auth probing (Epic 2) will use only official CLI public behavior. Nothing in this story touches the home directory at all.
- Never execute declared project commands (AD-3 — resolution only).

### Project Structure Notes

- New `src/cli/doctor/` module (edge layer — allowed; spine's seed lists top-level layers, cli-internal structure is free). Kebab-case files.
- Story branch `story/1.5-specwitness-doctor-runtime-project-diagnostics`; Conventional Commits (e.g. `feat(cli): implement doctor runtime and project checks`).

### References

- [Source: docs/planning-artifacts/epics.md#Story 1.5] — acceptance criteria (verbatim above)
- [Source: docs/planning-artifacts/prds/prd-specwitness-2026-08-30/prd.md#FR-3] — doctor requirement + credential/billing consequences
- [Source: docs/planning-artifacts/architecture/architecture-specwitness-2026-08-30/architecture-questions.md#Q26-Q27] — ports explicit; doctor pre-checks ports free
- [Source: docs/planning-artifacts/architecture/architecture-specwitness-2026-08-30/ARCHITECTURE-SPINE.md#Consistency Conventions] — non-interactive first, ports row, error style
- [Source: docs/adr/ADR-002-exit-codes.md] — exit 3 = environment/SpecWitness class
- [Source: docs/planning-artifacts/prds/prd-specwitness-2026-08-30/prd.md#UJ-4] — doctor user journey
- [Source: docs/planning-artifacts/roadmap.md#EPIC 1] — wave C ownership; Epic 2 dependency on "doctor registry"

## Dev Agent Record

### Agent Model Used

claude-opus-5 (Claude Code, autonomous cohort agent `rambo`).

### Debug Log References

- **The registry is a factory, not a module singleton.** `createRegistry()`
  returns an instance holding its own ordered list; there is no module-level
  registry. A shared mutable singleton would have been the obvious way to let
  story 2.7 "plug in", and it violates the spine's "no global mutable state"
  convention — the seam is a value passed to the command, not a global anyone
  can mutate at import time.
- **Verified the NFR-1 guard actually fails.** A test that has never been red
  proves nothing, so both violation families were planted and observed failing
  before the guard was trusted: `process.env.HOME` in a check module (caught as
  `process.env.HOME`) and a `~/.claude/credentials.json` string literal in
  another (caught as `string literal containing ".claude"`). Both were reverted;
  `grep -c planted` over the module confirms nothing remains.
- **Node version comparison is numeric, and there is a test that proves the
  string form would have been wrong.** `'v22.9.0' > 'v22.12.0'` lexicographically,
  so a string compare passes a runtime that is too old.
- **`git rev-parse --verify master` resolves a FILE named `master`.** Switched to
  fully-qualified `refs/heads/<branch>` and `refs/remotes/origin/<branch>`; a
  check satisfiable by an unrelated file is not a check.
- **`ready` changed shape twice upstream during this story** (pamela, 1.3):
  optional union → required strict object with optional `url`/`command`. The
  guard on `.command` survived both changes — a url-only readiness block has
  `command === undefined`, which unguarded would hand the PATH resolver an
  `undefined` token.
- CI could not be used: GitHub Actions is disabled repo-wide (minutes
  exhausted; `ci.yml` is `workflow_dispatch` only). The six workflow steps were
  run locally instead and pasted into the PR body, with what stays unverified
  stated plainly.

### Completion Notes List

- All four tasks and every subtask are implemented and covered. 60 new tests
  (44 unit + 12 integration + 3 NFR-1 guard, plus the registry set); full suite
  443 tests green, `typecheck` clean, `depcruise` clean across 49 modules.
- **AC1** — seven checks report pass/warn/fail; required = `node-version`,
  `git-present`, `config-valid`, `base-branch-exists`, `commands-resolvable`;
  optional = `playwright-capability`, `ports-free`. Exit is **3** on a required
  failure (ADR-002 environment class), never 1 and never 2, produced by throwing
  `InfraError` so `cli/exit.ts` remains the only module writing an exit code.
  Every check always runs and the whole report prints *before* the throw.
- **AC2** — `--json` puts the document and nothing else on stdout
  (`{schemaVersion: 1, timestamp, status, checks[]}`), human rendering moves to
  stderr; order is registration order; an integration test round-trips the
  timestamp through `Date` to prove it is ISO-8601 UTC.
- **AC3** — `tests/unit/doctor/registry.test.ts` registers a fake provider-shaped
  check and asserts it runs and reports with no existing check file changed.
  Checks are isolated: one throwing reports `fail` with its message while the
  others still run; duplicate ids are rejected rather than silently shadowed.
- **Deliberate deviations, each recorded in `DECISIONS.md` with its source:**
  1. `ports-free` **warns** rather than raising `InfraError`, diverging from the
     wording of architecture question Q27. The spec's AC1 clarification makes it
     optional, and Q27's hard failure belongs to the Epic 3 services stage. The
     port and an `lsof` hint are still reported; only the exit code differs.
  2. Shell-syntax commands (`FOO=bar cmd`, `a && b`) report their literal first
     token as unresolvable. AD-3's execution model has no shell, so a green check
     there would be a promise doctor cannot keep. Medium confidence — if Epic 3's
     runner accepts a shell form, this check should learn the runner's grammar.
  3. An explicit path that exists but lacks the executable bit is a failure with
     a distinct `chmod +x` detail, not a pass.
- **Known-red edge case, agreed with arnold (1.4) and pamela (1.3) and stated in
  all three PR bodies:** on a virgin `git init` with no commits,
  `refs/heads/<anything>` does not exist, so `base-branch-exists` is red whatever
  `init` writes. That is correct — you cannot verify against a branch with no
  commits — but it means "init then doctor is green" holds for a repo with at
  least one commit on its default branch, not universally. Story 1.4 reading the
  real branch from `.git/HEAD` (suggested in this thread, adopted there) fixes
  the `main`-vs-`master` half of the problem, not the empty-repo half.
- **NFR-1 is enforced, not asserted.** An AST scan fails the build on any home
  directory or credential-store access inside the doctor module, and pins `PATH`
  as the only environment variable it reads. Honest limit: a fully computed
  `process.env[variable]` defeats any static scan; that is deliberate
  circumvention, and code review is the backstop.
- **No provider checks and no placeholders for them.** Binary discovery, version
  probe, auth readiness and billing-risk warnings are story 2.7, which registers
  them through this registry. `BUILTIN_CHECKS` has a test asserting no
  provider-shaped id is present, so a well-meaning stub cannot creep in.
- **Nothing config-declared is ever executed.** The PATH scan is implemented by
  hand (no `which` subprocess, no shell); the only process doctor spawns is
  `git`, with a fixed argument array, `GIT_TERMINAL_PROMPT=0` and a 5s timeout.
- Removed `doctor` from the transitional stub-command lists in
  `tests/integration/cli.test.ts` and updated the comment above them, per the
  convention agreed with arnold and chuck. Story 1.4 removes `init` and takes the
  block with it.

### File List

**NEW — implementation**
- `src/cli/doctor/registry.ts` — check contract, ordered registration, isolated `runAll` (the Epic 2 seam).
- `src/cli/doctor/context.ts` — the single extensible context; captures a `loadConfig` failure as data instead of an exception.
- `src/cli/doctor/effects.ts` — every side effect behind one injectable surface (git spawn, stat, module resolution, port bind/release).
- `src/cli/doctor/render.ts` — human and `--json` renderings; `overallStatus` (describes checks) vs `hasRequiredFailure` (drives the exit code).
- `src/cli/doctor/checks/index.ts` — `BUILTIN_CHECKS` in report order; the required/optional split.
- `src/cli/doctor/checks/git.ts` — shared git timeout and failure vocabulary, so "missing", "hung" and "said no" stay distinguishable.
- `src/cli/doctor/checks/node-version.ts` — required; numeric comparison against the >=22.12 floor.
- `src/cli/doctor/checks/git-present.ts` — required.
- `src/cli/doctor/checks/config-valid.ts` — required; renders `ConfigError.message` verbatim so the YAML path survives.
- `src/cli/doctor/checks/base-branch.ts` — required; distinguishes git-absent, not-a-repository and branch-not-found.
- `src/cli/doctor/checks/commands-resolvable.ts` — required; hand-rolled PATH scan, resolution only, never execution.
- `src/cli/doctor/checks/playwright-capability.ts` — optional; project-local resolution, downloads nothing.
- `src/cli/doctor/checks/ports-free.ts` — optional; binds and always releases.

**NEW — tests**
- `tests/unit/doctor/registry.test.ts` — order, isolation, duplicate ids, and the AC3 extension seam.
- `tests/unit/doctor/checks.test.ts` — every check against injected effects; `BUILTIN_CHECKS` order and required/optional split.
- `tests/unit/doctor/render.test.ts` — the AC2 JSON shape, status rules, human rendering.
- `tests/unit/doctor/credential-boundary.test.ts` — NFR-1 as an AST guard.
- `tests/unit/doctor/helpers.ts` — fake effects; configs built through the real `loadConfig`, never by forging a `DeclaredCommand`.
- `tests/integration/doctor.test.ts` — the built CLI against temp-dir fixtures: exit 0/3, `--json`, warn-only stays 0.

**UPDATED**
- `src/cli/commands/doctor.ts` — story 1.1's stub replaced with the real command.
- `tests/integration/cli.test.ts` — `doctor` removed from the transitional stub lists; comment updated.
- `docs/implementation-artifacts/sprint-status.yaml` — this story's row only.
- `docs/implementation-artifacts/epic-1-install-configure-diagnose/1.5-specwitness-doctor-runtime-project-diagnostics.md` — checkboxes and this record.
- `DECISIONS.md` — created; this story's section.
