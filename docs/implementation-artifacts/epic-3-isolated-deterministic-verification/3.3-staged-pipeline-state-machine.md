# Story 3.3: Staged pipeline state machine

Status: ready-for-dev

## Cohort & Dependencies (harness self-configuration)

All eight Epic 3 stories launch at once; the harness serializes them via depends-on. PRs target the epic integration branch `epic/3-isolated-deterministic-verification` (**never `master`**). Expected agent↔task mapping from launch order: alice=3.1, bob=3.2, pamela=3.3, arnold=3.4, rambo=3.5, chuck=3.6, predator=3.7, blade=3.8 — if `tmux-overview.sh` shows a different mapping, depend on whoever holds the story id named below, not on the name.

Run after reading this spec:

1. `~/.terminal-agents/scripts/set-cohort.sh alice,bob,arnold,rambo,chuck,predator,blade`
2. Complete cohort **intent-sync** (write `intent.md`, read every peer's, ack).
3. **No `set-depends-on.sh`.** You are wave A: you start unblocked and build on merged Epic 1 + Epic 2 code only.

> The ordering above is not cosmetic. The intent gate blocks `set-depends-on` and `agent-wait` until cohort intent-sync completes, and in Epic 2 five agents took block loops in their first minutes because their specs said to run it immediately (harness defect H-10). Intent-sync first, always.

**After any unblock or rebase nudge:** `git fetch origin`, then — because this project **squash-merges** — `git rebase --onto origin/epic/3-isolated-deterministic-verification <sha-of-your-last-merged-commit>`. A plain `git rebase` conflicts on every already-merged commit (H-4). **If your own PR is already merged, a rebase nudge is not for you — ignore it** (H-3: three Epic 2 agents were reopened from `done`, and one did 45 minutes of work that had nowhere to land).

### You are the critical path for the entire epic

**Four agents cannot write a line until your PR merges.** arnold (3.4), rambo (3.5), chuck (3.6) all depend on `pamela:done` directly; predator (3.7) depends transitively on all three. Nothing in wave B starts before you land.

Two consequences, and they are the whole shape of this story:

- **Scope tightly and PR early.** Every hour you spend is an hour four agents spend waiting. If you find yourself tempted to execute a real command, run a real gate, or write a real file — stop. That is 3.4, 3.2 and 3.5's work.
- **Publish `RunResult` in writing during intent-sync, in the first 20 minutes, before you have implemented it.** Send arnold, rambo, chuck and predator the exact TypeScript for `RunResult`, `StageName`, `StageOutcome`, `StageTimelineEntry`, the `Evidence` union and `EvidenceRef`. They build against your published shape while you implement it. Epic 2's intent-sync produced **zero interface disputes at merge across seven agents** — this is that mechanism, and you are its most important user this epic.

**Tell your consumers to re-check the shape against your merged source before building on it** (Epic 2 lesson 5: two agents caught stale peer summaries by reading shipped code — both summaries were written in good faith and had simply gone stale). Say this explicitly in your intent-sync message.

**No consumer may widen `RunResult` in its own branch.** If 3.4/3.5/3.6/3.7 need an additive field, that is a message to you or a follow-up story — never a parallel edit to `src/domain/run-result.ts`. Say so in intent-sync.

## Story

As the architecture's backbone,
I want verify to run as named stages with typed stage results and early-stop semantics,
so that classification, timing, and reporting are uniform and 7 agents share one execution model (FR-20, FR-22, AD-6/7/11).

## Acceptance Criteria

From `docs/planning-artifacts/epics.md` (authoritative — expand, never weaken):

1. **Given** the stage sequence (resolve → integrity → worktree → setup → gates → services → data → probes → aggregate → persist → teardown)
   **When** any stage fails
   **Then** later stages are skipped (teardown always runs), the failure is classified through the AD-7 hierarchy, and the stage timeline (per-stage status + duration) appears in the RunResult.
2. **Given** the integrity stage
   **When** the contract fingerprint mismatches
   **Then** the run ends as IntegrityError (exit 3) with zero commands executed in the worktree (FR-9 runtime).
3. **Given** stage results
   **When** unit tests exercise the state machine with faked stages
   **Then** every early-stop path and the always-teardown guarantee are covered without real I/O.

Clarifications (detail only, no weakening):

- **AC1 the eleven stage names are frozen by the spine's Structural Seed and by this AC.** Declare them once as a `const` tuple in `src/domain/`, in exactly this order, and derive `StageName` from it. Renaming, reordering, adding or dropping one is an ADR — four other stories key their stage entries off these strings.
- **AC1 "any stage fails" is a STAGE RESULT, not a thrown exception, for gate failure specifically.** AD-6 is explicit: `GateFailure` is a stage result and the **aggregate stage is the only converter** from stage results to a `RunOutcome`. Your runner must therefore distinguish two different kinds of "not ok":
  - a stage that produced a **product-relevant negative outcome** (gates failed) — the pipeline stops early, but the run proceeds through aggregate → persist → teardown and ends with a `Verdict`;
  - a stage that hit an **infrastructure problem** (a thrown AD-7 error) — the run cannot reach a product conclusion and ends with an `{infraError}` outcome.
  Both skip the intervening stages; both still run teardown. Conflating them is the defect this epic exists to prevent, and the type system should make the distinction unavoidable rather than a convention.
- **AC1 "teardown always runs"** means: after any early stop, after any thrown error, and after an error thrown *by teardown itself*. A teardown that throws must not replace the run's outcome with its own failure — record it, classify it, and keep the outcome that was already decided (a run that FAILed on a gate and then failed to remove a worktree is still a FAIL; a run that PASSed and then leaked a worktree is a PASS with a recorded teardown problem, and 3.2's `clean` is the remedy). State your choice in the module header and test it.
- **AC1 "the failure is classified through the AD-7 hierarchy":** call the merged classification. `exitCodeForError` in `src/cli/exit.ts` maps thrown errors, and it already fails closed (anything unrecognised → infra). Your pipeline maps a **thrown AD-7 error** to an `InfraErrorClassification` from `src/domain/run-outcome.ts` — `ConfigError → 'config'`, `IngestError → 'ingest'`, `IntegrityError → 'integrity'`, `ProviderError → 'provider'`, `InfraError → 'infra'`, **anything else → `'infra'` (fail closed)**. `UsageError` is deliberately absent from that enum: it is raised at the CLI edge before a run exists. If one reaches your runner, that is a programming error — let it propagate rather than inventing a classification for it, and say so in a comment.
- **AC1 stage timeline entries carry `durationMs` from the injected `Clock`** (AD-9), never `Date.now()`. `src/domain/ports.ts` is merged and its header says Epic 3 brings its ports here. `FixedClock` in `tests/fakes/ports.ts` makes durations exact integers rather than "greater than zero", which is the assertion that passes even when the clock is read once and reused.
- **AC2 "zero commands executed in the worktree" is an assertion you own, mechanically.** Inject a `ProcessRunner` that throws on any call, drive an integrity-mismatch run, and assert it was never reached. Do not assert this in prose. Note the ordering this AC pins: **integrity precedes worktree**, so a tampered contract costs nothing — not a worktree, not a spawn.
- **AC2 uses the merged guard, it does not re-implement it.** `assertVerifiableContract` in `src/authoring/verifiable.ts` (story 2.6, merged) already refuses absent / never-frozen / tampered contracts with three distinct hints, and `contractState` in `src/schemas/contract.ts` is the single integrity comparison. Your integrity stage **calls** them. Two implementations of "does this content match its fingerprint" would eventually disagree, and the disagreement would surface as a contract that verifies in one code path and reports tampering in another.
- **AC3 "without real I/O"** is a property of your whole suite, not of one test: `src/pipeline/**` and `src/domain/**` unit tests spawn **zero** subprocesses, touch **zero** files, and read neither the wall clock nor randomness. Enforce it — inject a `ProcessRunner` that throws (`forbiddenProcessRunner` already exists in `tests/fakes/agent-provider.ts`, merged by story 2.3 — reuse it rather than writing a second one).
- **`domain/evidence.ts` is yours** (see Project Structure Notes). 3.4 needs typed `gate`/`command` evidence to produce; 3.6 needs a bounded thing to render. Shipping it in wave A is what stops those two stories inventing two evidence shapes in parallel branches.
- **`domain/criterion-result.ts` is yours too, as AD-13's seam** — the `SurfaceExecutor` and `ProbeAttempt` types plus the single derivation function. Epic 4/5 fill it; you land it. Keep it minimal: for a gates-only run, every criterion is `skipped`, so the function must be *correct* for the trivial case and *shaped* for the real one.

## Tasks / Subtasks

- [ ] Task 1 (TDD): the stage vocabulary and the `RunResult` shape (AC: 1)
  - [ ] `src/domain/stage.ts` — `STAGE_NAMES` (the frozen eleven-element tuple, in spine order), `StageName`, `StageStatus`, `StageTimelineEntry {stage, status, durationMs, detail?}`. Types + the const tuple only; imports nothing (AD-1: `src/domain/**` is dependency-free — `tsPreCompilationDeps: true` means even `import type { z } from 'zod'` fails `domain-is-dependency-free`; story 2.3 verified this the hard way)
  - [ ] `src/domain/run-result.ts` — **the epic's keystone**. Complete shape, published in intent-sync before you implement it: `schemaVersion`-free (that belongs to 3.5's persisted document), `runId`, `epic`, `baseSha`, `headSha`, `startedAt`/`finishedAt` (ISO-8601 UTC strings), `outcome: RunOutcome`, `stages: StageTimelineEntry[]`, `gates: GateResult[]`, `criteria: CriterionResult[]`, `evidence: EvidenceRef[]`, `providerUsage: ProviderUsage[]`, `environment: RunEnvironment`
  - [ ] **`ProviderUsage` and `RunEnvironment` do not exist yet — you define both, in `src/domain/run-result.ts`.** Every other type in the list above is merged (see the dependency table) or yours from Task 1/2; these two are new, and no other story creates them:
    - **`ProviderUsage`** — one record per provider invocation, per AD-4 ("every provider invocation is recorded (role, duration, retries) in run/generation metadata") and Q65. Minimum: `role`, `provider` (the config key), `durationMs`, `attempts`. **Epic 3 produces an empty array** — verify is AI-free (FR-18, Q66) — so shape it for Epic 4/5 and prove the empty case. Note `model` and `providerCliVersion` are `null` on every path today (Epic 2 retro §5 debt 3). **Wiring them is story 3.8 (blade), this cohort** — and it is a *contract* `meta` field, a different artifact from your run-level `ProviderUsage`. Leave room for them, do not wire them, and do not block on blade.
    - **`RunEnvironment`** — the summary FR-29 requires the terminal renderer to print. Keep it small and factual (e.g. node version, platform, the resolved worktree path). **A renderer must never compute or look any of it up** (AD-11), so whatever chuck has to print must be in this object.
  - [ ] Import `RunOutcome`/`Verdict` from the merged `src/domain/run-outcome.ts`, `GateResult`/`CriterionResult`/`CriterionStatus`/`GateStatus` from the merged `src/domain/result.ts`. **Do not redefine any of them**, and do not widen `CriterionResult` destructively — `src/domain/result.ts` documents that Epic 3 extends it **additively** from `domain/criterion-result.ts` with expected/actual, evidence refs and severity
  - [ ] Tests: the stage tuple's order and length; `RunResult` type-level assertions (a `RunResult` carrying both `verdict` and `infraError` must not compile — `run-outcome.ts` already makes that a type error via `infraError?: never`; assert the property is inherited rather than assuming it)
- [ ] Task 2 (TDD): typed evidence with capture-time redaction (AC: 1 — AD-10)
  - [ ] `src/domain/evidence.ts` — the closed union AD-10 names: `http`, `browser`, `observation`, `command`, `gate`, `provider`, plus the labeled non-authoritative `explanation`. Epic 3 only *produces* `gate`, `command` and `provider`; the other three are **declared now and produced later** (Epic 4/5), so the union is closed once rather than widened three times
  - [ ] **Redacting constructors.** Redaction happens at construction, before anything is persisted, and **renderers never re-redact** (AD-10). Redact: `Authorization` / `Cookie` / `Set-Cookie` header values; values of names matching `*_KEY` / `*_TOKEN` / `*_SECRET` / password patterns; plus config-declared extra patterns passed in by the caller (Epic 3 wires none — accept them as a parameter and default to empty, so 4.x has nowhere new to put them)
  - [ ] `EvidenceRef {kind, path}` — **relative** paths from the run-directory root (Q48), so a run directory stays readable after being moved between machines. Absolute paths must be impossible to construct here, not merely discouraged
  - [ ] Bounded inline content + a truncation marker naming the full file (Q49, AD-11). You define the cap and the marker's shape; 3.4 produces it, 3.6 renders it. Publish both in intent-sync
  - [ ] Tests: every redaction pattern, incl. a seeded secret that must not appear in the constructed evidence (FR-28's testable consequence); truncation at the boundary (cap-1, cap, cap+1); a rejected absolute path; the union's exhaustiveness under a `never` check
- [ ] Task 3 (TDD): AD-13's criterion-result seam (AC: 1)
  - [ ] `src/domain/criterion-result.ts` — `SurfaceExecutor`, `ProbeAttempt {observations, assertionEvaluations[], evidence[], execError?}`, and the ONE pure function deriving a `CriterionResult` from attempts (retry orchestration, `flaky` marking, error classification). AD-13 says there is exactly one producer of `CriterionResult`; this is it
  - [ ] For Epic 3, prove the paths a gates-only run actually reaches: zero attempts ⇒ `skipped`; and pin the semantics the later epics inherit — all assertions true ⇒ `pass`; ≥1 false ⇒ `fail`; `execError` ⇒ `error`; a pass after a failed attempt ⇒ `pass` + `flaky: true` (FR-32: recorded, never silently converted to a clean pass, and never changes a verdict)
  - [ ] Tests: each derivation above, table-driven
- [ ] Task 4 (TDD): the stage runner (AC: 1, 3)
  - [ ] `src/pipeline/stage.ts` — the `Stage` interface and the two-armed stage result (`ok` vs. product-negative), typed so a stage cannot return an unclassified failure
  - [ ] `src/pipeline/run-pipeline.ts` — the state machine: run stages in order, stop early on failure, **always run teardown**, record a `StageTimelineEntry` per stage (including `skipped` ones), classify thrown AD-7 errors, and call **`aggregate` from the merged `src/domain/verdict.ts`** in the aggregate stage. Do not re-implement aggregation — it is the product's trust anchor and it already exists, tested, with a property suite (`tests/unit/verdict.property.test.ts`)
  - [ ] Tests, all with faked stages and zero I/O: happy path (all eleven run, in order); failure at each of the ten non-teardown stages ⇒ correct skip set, teardown still ran; a stage that throws each AD-7 error class ⇒ correct `InfraErrorClassification`; a stage that throws a plain `Error` ⇒ `'infra'` (fail closed); teardown itself throwing ⇒ outcome preserved, problem recorded; the gates-stage-failed path ⇒ FAIL + `gateFailed`, reaching aggregate/persist/teardown rather than short-circuiting past them
- [ ] Task 5 (TDD): the resolve and integrity stages (AC: 2)
  - [ ] `src/pipeline/stages/integrity.ts` — calls `assertVerifiableContract`; on mismatch the run ends `{infraError: 'integrity'}` (exit 3 via the merged table)
  - [ ] `src/pipeline/stages/resolve.ts` — **the pure half only.** Epic-id normalization via the merged `normalizeEpicId` (`src/domain/ids.ts`); config-derived defaults for base/head. Git ref resolution is **3.1's** (alice) — your stage takes the resolved SHAs as input. Do not spawn `git`
  - [ ] **You ship all nine remaining stages as named placeholders that record a timeline entry**, so the sequence is complete and later stories replace them one at a time. Say in each file's header which story fills it — **and note that three of the nine are yours to actually implement, not to leave empty**:
    | Stage | Placeholder or implemented | Filled by |
    | --- | --- | --- |
    | `worktree` | placeholder | **3.1 (alice), this wave** — she replaces yours. **She is expecting this**; her spec names the file and told her to settle it with you in intent-sync. Ship the placeholder and confirm it, so you do not both create the path |
    | `setup` | placeholder | Epic 4 |
    | `gates` | placeholder | **3.4 (arnold), wave B** — his spec says to replace yours |
    | `services` | placeholder | Epic 4 story 4.1 |
    | `data` | placeholder | Epic 4 story 4.3 |
    | `probes` | placeholder | Epic 4 stories 4.4–4.6 |
    | `aggregate` | **implemented by you** | calls the merged `aggregate` from `src/domain/verdict.ts` |
    | `persist` | placeholder | **3.5 (rambo), wave B** — persisting needs `RunStore.writeResult`, which does not exist until his story. Do **not** implement it |
    | `teardown` | **implemented by you** | the always-runs guarantee is AC1's, so its skeleton is yours even though 3.1/3.2 add the resource-releasing calls later |
  - [ ] Tests: integrity mismatch ⇒ `integrity` classification, exit 3, and **the injected `ProcessRunner` was never called** (AC2's real assertion); a frozen valid contract ⇒ integrity passes; absent and never-frozen contracts ⇒ their own distinct refusals
- [ ] Task 6: the AD-1 layer rule for `src/pipeline/**` (AC: all)
  - [ ] Add a `pipeline-layer` rule to `.dependency-cruiser.cjs`, modelled on the merged `ingest-core-only`. **`src/pipeline/**` is application-layer with a wider allowance than `ingest`:** the spine's AD-1 graph shows `PIPE → DOM`, `PIPE → SURF`, `PIPE → CFG`, `PIPE → INFRA`, `PIPE → PROV`. So permit `domain`, `schemas`, its own siblings, `config`, `infra`, `providers`, `surfaces` and npm — and **forbid `cli`** (already covered by `nothing-imports-cli`; do not duplicate it) **and `authoring`, `ingest`, `report`** (application layers must not import each other — that is the rule with actual teeth here, and it is what keeps 3.6's renderers from being reachable from inside the pipeline)
  - [ ] Pin it in `tests/unit/dependency-rules.test.ts` with **both** a forbid case and a permit case, following the file's post-2.8 hermetic design: probes go in a per-test temp copy of the tracked source, never under the real `src/`. Read that file's header first — 2.8 rewrote it and reintroducing a fixed-name scratch module under `src/` would re-break every agent's concurrent test runs (H-8)
  - [ ] **Verify the rule red before trusting it.** A clean depcruise run does not prove a rule matches. Plant a probe that violates it, observe the named failure, remove it. (Story 2.6 did exactly this and found its prediction was right; story 2.1 found `ingest-core-only` was the only thing standing between `src/ingest` and `src/config`.)

## Dev Notes

### Goal & Context

Epic 3 is where SpecWitness first does its job, and you own the execution model all seven stories share. Everything before this epic authored artifacts; from here on `specwitness verify` executes against a branch and returns a verdict a harness will act on — which makes two properties load-bearing that were theoretical until now:

1. **Classification is the product promise.** A wrong classification silently blocks a mergeable branch or waves through a broken one. AD-6/AD-7's three-way distinction (PASS 0 / FAIL 1 + `gateFailed` / InfraError 3) is decided in *your* state machine and nowhere else.
2. **One model, many renderers.** `RunResult` is the single object 3.5 persists and 3.6 renders. If it lacks a fact, a renderer will compute one — and AD-11 exists precisely to forbid that. Publishing a complete shape early is cheaper for everyone than four stories discovering gaps in wave B.

You are in wave A with 3.1 (alice, git/worktree) and 3.2 (bob, process lifecycle). None of you touch each other's files.

### Exact scope

- `src/domain/stage.ts`, `src/domain/run-result.ts`, `src/domain/evidence.ts`, `src/domain/criterion-result.ts` (NEW — types + pure functions, zero imports outside `src/domain/`)
- `src/pipeline/**` (NEW — the state machine, the stage interface, the integrity + resolve stages, nine named placeholders)
- `.dependency-cruiser.cjs` — **one new rule**, `pipeline-layer`
- `tests/unit/dependency-rules.test.ts` — append your forbid + permit cases (hermetic design, per 2.8)
- `tests/unit/pipeline/**`, `tests/unit/domain/**`
- `docs/implementation-artifacts/sprint-status.yaml` — your row only

### Out of scope (do NOT build)

- **Git, worktrees, ref resolution — 3.1 (alice).** Your resolve stage takes SHAs as input and normalizes an epic id. Do not spawn `git`, not even to check a ref exists.
- **Process groups, the run manifest, `clean` — 3.2 (bob).** Do not touch `src/domain/process-runner.ts`, `src/infra/process-runner.ts` or `src/infra/run-store.ts`. bob is editing two of those this wave.
- **Executing gates — 3.4 (arnold).** Your gates stage is a named placeholder. Do not read `config.gates`, do not run a `DeclaredCommand`, do not import `commandText`.
- **Persisting `result.json`, its zod schema, the `jsonReport` registry entry — 3.5 (rambo).** Your persist stage is a placeholder. Do not add a key to `src/schemas/versions.ts`.
- **Renderers — 3.6 (chuck).** Do not create `src/report/**`. Do not print anything from `src/pipeline/**`.
- **Registering a `verify` command — 3.7 (predator).** Nothing in `src/cli/` is yours. `src/cli/main.ts` is predator's one-line registration and yours would collide with it.
- **Services, readiness, probes, plans, test data** — Epic 4. Placeholders only.

### Dependencies & upstream contracts (by ID)

Everything below is **already merged on `master`** — read it, do not re-specify it:

| What | Where | Notes |
| --- | --- | --- |
| `aggregate(gates, criteria)` | `src/domain/verdict.ts` | **Call it; never re-implement it.** Precedence: gate fail → FAIL+`gateFailed`; criterion `fail` → FAIL; `error` → `{infraError:'infra'}`; `needs_human` → NEEDS_HUMAN; else PASS. Total by construction — empty arrays return PASS, which is the gates-only green run. |
| `RunOutcome`, `Verdict`, `INFRA_ERROR_CLASSIFICATIONS`, `isVerdictOutcome`, `isInfraErrorOutcome` | `src/domain/run-outcome.ts` | Mutually exclusive arms enforced by `infraError?: never`. Use the narrowing helpers rather than `'verdict' in o`. |
| `CriterionStatus`, `GateStatus`, `CriterionResult`, `GateResult` | `src/domain/result.ts` | The closed taxonomy. Widening it is an ADR, not an edit. `CriterionResult` is extended **additively** by your `criterion-result.ts`. |
| `Clock`, `Ids` | `src/domain/ports.ts` | AD-9. Its header explicitly says Epic 3 brings its ports here. Inject; never read the wall clock in `domain`/`pipeline`. |
| `normalizeEpicId`, `buildCriterionId`, `isCriterionId`, `parseCriterionId` | `src/domain/ids.ts` | `7` ≡ `epic-7` ≡ `epic-07` → `epic-7`. The only implementation. |
| `assertVerifiableContract`, `LoadedContract`, `ContractStatusState` | `src/authoring/verifiable.ts` | Story 2.6 built this **for you** — its header names story 3.7 as the wiring story and this epic as its consumer. Three refusals, three hints; `tampered` must never be reported as "not frozen yet". |
| `contractState`, `parseContract`, `ContractNotFrozenError` | `src/schemas/contract.ts` | The single integrity comparison. |
| AD-7 error classes | `src/domain/errors.ts` | `UsageError`, `ConfigError`, `IngestError`, `IntegrityError`, `ProviderError`, `InfraError` — all `(message, hint?)`. Do not add a class. |
| `exitCodeForOutcome`, `exitCodeForError`, `EXIT` | `src/cli/exit.ts` | The ONLY exit table. You never import it (`nothing-imports-cli`); 3.7 maps your outcome to a code. |
| `GateConfig`, `ServiceConfig`, `PlanningConfig`, `SpecwitnessConfig` | `src/config/types.ts` | Gates are an **ordered array with unique ids** (uniqueness enforced at load). You may read the *shape*; executing them is 3.4's. |
| `forbiddenProcessRunner`, `scriptedProcessRunner` | `tests/fakes/agent-provider.ts` | Story 2.3 wrote these so later stories would not write their own. Use `forbiddenProcessRunner` for AC2 and AC3. |
| `FixedClock`, `SequenceIds` | `tests/fakes/ports.ts` | Deterministic durations and ids. |

Binding: FR-20, FR-22, FR-27, FR-9 (runtime integrity); AD-1, AD-6, AD-7, AD-9, AD-10, AD-11, AD-13; ADR-002, ADR-003; architecture questions Q40–Q46 (criterion semantics and aggregation), Q47–Q49 (evidence union, relative paths, size caps).

### Architecture compliance

- **AD-1.** `src/domain/**` imports nothing outside `src/domain/`. `src/pipeline/**` is application-layer: `domain`, `schemas`, siblings, `config`, `infra`, `providers`, `surfaces`, npm — never `cli`, never `authoring`/`ingest`/`report`. **One exception you must handle deliberately:** your integrity stage wants `assertVerifiableContract` from `src/authoring/`, which the rule above forbids. Resolve it the way the spine intends — **the caller (3.7's CLI edge) loads and verifies the contract and passes the result into the pipeline**, exactly as config is "loaded once, validated, passed down". Your integrity stage then operates on data it was handed. Say this in your intent-sync message: it is a seam 3.7 must build to, and discovering it in wave B would cost predator a redesign.
- **AD-6.** Closed enums, defined once, already merged. `GateFailure` is a stage result; the aggregate stage is the only converter.
- **AD-7.** Every failure classified at the boundary; unclassified exceptions escalate to `InfraError`, fail closed.
- **AD-9.** `Clock`/`Ids` injected. No `new Date()`, no `Math.random()`, no `crypto` in `domain`/`pipeline`.
- **AD-10.** Redaction at construction, in `domain/evidence.ts`. Renderers never re-redact.
- **AD-11.** One `RunResult`; renderers compute nothing absent from it. Your job is to make sure nothing *is* absent.
- **AD-13.** One `CriterionResult` producer.
- **Conventions.** ISO-8601 UTC timestamps; `durationMs` integer ms; camelCase JSON keys; kebab-case filenames; criterion ids `E<n>-<NN>`.

### Testing requirements

- **TDD**, per story.
- **Zero I/O in your suites**, enforced not assumed: inject `forbiddenProcessRunner`; do not import `node:fs` in any `tests/unit/pipeline/**` file.
- **Every early-stop path, not a sample.** Eleven stages means ten distinct failure positions plus the always-teardown guarantee. Table-drive it.
- **Each AD-7 class mapped, plus the fail-closed default.** A plain `throw new Error('boom')` must classify as `'infra'`. This is the assertion that catches a `switch` someone later extends without a `default`.
- **The AC2 no-spawn assertion is mechanical**, per above.
- **Redaction proven with a seeded secret** (FR-28's testable consequence): construct evidence containing `ANTHROPIC_API_KEY=sk-ant-...` and assert the value does not appear anywhere in the result.
- **Verify at least one new guard red.** The `pipeline-layer` rule, by planting a probe. Epic 2's lesson: a guard nobody has watched fail is not a guard.
- **Concurrency-safe test files** (H-8): the harness runs `pnpm test` in your worktree while you run it too. No fixed-name scratch files anywhere under `src/`.
- **Do not name any file `*.env.test.ts`** — the harness's secret-path matcher rejects filenames containing `.env` as a substring (H-13); story 2.3 was forced into a rename and a permanent explanatory comment.

### Integration expectations

- **3.4 (arnold)** implements the gates stage against your `Stage` interface, produces `GateResult[]` and `gate` evidence via your constructors, and returns a product-negative stage result on failure — never a thrown exception.
- **3.5 (rambo)** persists your `RunResult` as `result.json` and adds the `jsonReport` schema version. He needs the shape stable and complete; a field you add after his branch cuts is a merge conflict in a file he owns.
- **3.6 (chuck)** renders your `RunResult` and nothing else. If a renderer needs a fact, the fact belongs in your model — but as a message to you, not a parallel edit.
- **3.7 (predator)** wires the CLI edge: loads config, loads and verifies the contract, resolves refs via 3.1's seam, builds the stage list, runs your pipeline, maps the outcome through `exitCodeForOutcome`. Publish the pipeline's entry-point signature during intent-sync — it is the one thing predator cannot proceed without.

### Failure modes (the ones that would matter in production)

- **A thrown gate failure.** If the gates stage throws instead of returning a stage result, gate failure becomes exit 3 rather than exit 1 — the harness reads "environment broken, retry" for a branch that simply does not build, and a broken branch merges after a retry. The type system should make this unrepresentable.
- **A teardown that swallows the outcome.** A teardown error overwriting a decided verdict turns a legitimate FAIL into an infra error, and the branch looks retryable.
- **A `default:` that silently PASSes.** Any switch over `CriterionStatus`, `Verdict` or `StageName` gets a `never` exhaustiveness check — `verdict.ts` and `exit.ts` both show the pattern. Fail closed, never fall through.
- **A `RunResult` field that a renderer computes instead.** Watch for 3.6 needing counts, durations or "which gate failed" — all of those are facts, and facts live in the model.

### Security

- **AD-3 stays intact.** Nothing in `src/pipeline/**` builds a command string this story. No `DeclaredCommand` is minted, imported or asserted; `tests/unit/config/boundary-scan.test.ts` walks the TypeScript AST of **every** source file outside `src/config/` and rejects both deep imports and `as DeclaredCommand` casts — so `src/pipeline/**` falls under it automatically the moment the directory exists. You do not extend that test; 3.4 verifies its coverage when it becomes the first module to execute a declared command.
- **AD-10.** Redaction at capture is a security control, not formatting. The seeded-secret test is the proof.
- **NFR-1.** Nothing reads `~/.claude/`, `~/.codex/` or any credential store. `tests/unit/doctor/credential-boundary.test.ts` is the AST guard; if your evidence redaction names env variables in order to redact them, expect its exact-list assertion to need a deliberate, commented extension — **do not relax it to `toContain`** (Epic 2 kept it exact three times, and each time it caught something).

### Project Structure Notes

- `src/pipeline/` is new and is named in the spine's Structural Seed with exactly your stage sequence in a comment. Match it.
- **`domain/evidence.ts` lands here, in wave A, rather than in Epic 4.** The spine lists it under AD-10 but no story had created it. Deferring it would leave 3.4 producing gate output with no typed home and 3.6 rendering an untyped blob — two stories inventing two shapes in parallel branches, which is the exact failure mode intent-sync exists to prevent. It is types plus pure constructors, so it costs you little and unblocks two consumers.
- **`domain/criterion-result.ts` likewise.** AD-13 names it as the single producer; `src/domain/result.ts`'s header already points at it by name.
- `.dependency-cruiser.cjs`: **you and 3.6 both add a rule to this file, in different waves** (you wave A, chuck wave B for `src/report/**`). Sequence makes it safe; chuck rebases over your rule. Both of you must extend `tests/unit/dependency-rules.test.ts` — append a new `describe`, never restructure the existing ones (Epic 2's pattern: two stories sharing a file shared **zero `expect()`**, which is stronger than "we coordinated").

### References

- [Source: docs/planning-artifacts/epics.md#Story 3.3] — the three ACs, verbatim above
- [Source: docs/planning-artifacts/architecture/architecture-specwitness-2026-08-30/ARCHITECTURE-SPINE.md#AD-6] — closed enums; aggregation as one pure function; `GateFailure` is a stage result; exit mapping in one table
- [Source: .../ARCHITECTURE-SPINE.md#AD-7] — the hierarchy; gate failure deliberately outside it; unclassified ⇒ `InfraError`
- [Source: .../ARCHITECTURE-SPINE.md#AD-9] — determinism ports; no direct clock/random in `domain`/`pipeline`
- [Source: .../ARCHITECTURE-SPINE.md#AD-10] — typed evidence union; redaction at capture; renderers never re-redact
- [Source: .../ARCHITECTURE-SPINE.md#AD-11] — one `RunResult`, many renderers
- [Source: .../ARCHITECTURE-SPINE.md#AD-13] — `SurfaceExecutor`/`ProbeAttempt`; one `criterion-result.ts`
- [Source: .../ARCHITECTURE-SPINE.md#Structural Seed] — `src/pipeline/` and the eleven-stage comment
- [Source: .../architecture-questions.md#Q40-Q46] — criterion status semantics and aggregation precedence
- [Source: .../architecture-questions.md#Q47-Q49] — evidence union, relative evidence paths, size caps
- [Source: docs/adr/ADR-002-exit-codes.md] — 0/1/2/3/64
- [Source: docs/adr/ADR-003-gate-failure-verdict.md] — FAIL + `gateFailed`, exit 1, criteria `skipped` (carried action A3: the ADR's prose says `gateFailed: true`; the merged `src/domain/run-outcome.ts` implements `gateFailed?: string` carrying the gate id — **follow the code**)
- [Source: docs/planning-artifacts/prds/prd-specwitness-2026-08-30/prd.md#FR-20,FR-22,FR-27] — gates early stop, classification, mechanical aggregation
- [Source: docs/planning-artifacts/roadmap.md#EPIC 3] — wave A; "RunResult shape (3.3's) merged before wave B"
- [Source: src/domain/verdict.ts] — the aggregation you call
- [Source: src/domain/result.ts] — the closed taxonomy and its "extended additively by Epic 3" note
- [Source: src/authoring/verifiable.ts] — the guard 2.6 built for your integrity stage
- [Source: tests/unit/dependency-rules.test.ts] — the post-2.8 hermetic design your cases must follow
- [Source: docs/findings/epic-2-retro-2026-08-31.md#3] — learning 1 (the unasserted half of a decision is where the bug lives); learning 3 (sharing a probe is not enough — the judgement over it must be shared too, and this retro names your verify path as where that shape recurs); learning 5 (verify shapes against merged source, not a summary)
- [Source: docs/findings/epic-2-harness-defects-2026-08-31.md] — H-3, H-4, H-8, H-10, H-13

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
