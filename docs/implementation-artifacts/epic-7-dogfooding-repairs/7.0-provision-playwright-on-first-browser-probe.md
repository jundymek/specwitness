# Story 7.0: `verify` provisions Playwright when a browser probe needs it

Status: ready-for-dev

**Found during the FIRST REAL DOGFOODING RUN, 2026-09-06**, gating `gitnebula`'s Epic 6 — the
first time this product ever executed a browser probe against a real project. Recorded as
**D-5** in `docs/findings/dogfooding-defects-2026-09-05.md`, which is the evidence file for
this story and should be read before the spec.

**You are one agent on a fresh branch with a supervisor, and your single story is the whole
cohort.** Run `~/.terminal-agents/scripts/set-cohort.sh` with no peers (or skip it and say so,
and why, in your PR body). Set NO `set-depends-on` marker: nothing blocks this story, and no
other cohort is touching `src/surfaces/`, `src/infra/playwright-env.ts` or
`src/cli/commands/verify.ts`.

**A second cohort is running CONCURRENTLY in a DIFFERENT repository** (`gitnebula`, its own
Epic 6). It shares no files with this one. It does, however, share a subscription — if every
pane stalls at once, read `docs/findings/epic-2-harness-defects-2026-08-31.md` H-1 before
concluding anything is wrong with your work.

**Your PR title becomes the squash subject and must be a valid Conventional Commit** (e3-H).
Put the story id in a `Refs:` trailer, not the title.

## Provenance — and a rule this spec imposes on itself

Written by the owner's session on 2026-09-06, from a live failure rather than from planning.
It does not appear in `docs/planning-artifacts/epics.md`.

**Action item e6.11-A applies to this file.** A supervisor-authored spec must cite `path:line`
read in the session that wrote it, for every claim about merged behaviour — the 6.11 spec
imposed that rule on its implementer and got two of its own claims wrong. Every citation
below was read on `master` at `103ec20` in the authoring session. **Verify them yourself
before building on them** (Task 0), and if one is wrong, say so in your PR body rather than
working around it.

## The defect, in one paragraph

`provisionPlaywright` is exported from `src/infra/playwright-env.ts:931`, is unit-tested, and
**is called from nowhere in `src/`**. A browser probe on a project that does not resolve
`@playwright/test` from its own root therefore fails with `InfraError` and the run stops at
exit 3 — while two user-facing messages promise a provisioning step that does not exist.

## Verified facts — cite these, do not re-derive them from memory

| # | fact | citation |
| --- | --- | --- |
| 1 | `provisionPlaywright` is exported | `src/infra/playwright-env.ts:931` |
| 2 | Nothing in `src/` calls it — only three test files do | `grep -rn "provisionPlaywright(" src/ \| grep -v export` → no output |
| 3 | `verify` RESOLVES (never provisions) once, before the pipeline | `src/cli/commands/verify.ts:341` |
| 4 | The resolved value is injected into the executor unchanged | `src/cli/commands/verify.ts:479` |
| 5 | The executor refuses, never skips | `src/surfaces/browser.ts:1136-1141` (`#requireRuntime`) |
| 6 | `doctor` promises provisioning "on the first browser probe" | `src/cli/doctor/checks/playwright-capability.ts:131` |
| 7 | `doctor` deliberately never downloads (AD-12) | `src/cli/doctor/effects.ts:81-82` |
| 8 | `provisionPlaywright` has three routes: ready / project-owned / own-cache | `src/infra/playwright-env.ts:920-930` (header) |
| 9 | It writes NOTHING into the target project, even when asked | `src/infra/playwright-env.ts` — the `⚠️` block after the readiness check |

**Reproduction**, on a pnpm monorepo whose Playwright is a package devDependency:

```console
$ specwitness verify 6 --no-ai
  ✓ ok  setup   installed with 'pnpm install --frozen-lockfile' (exit code 0)
  ✓ ok  gates   4 passed
VERDICT: (none) — infra error: infra
ERROR: infra: browser probe for E6-10 cannot run: @playwright/test does not resolve from … and is not in the SpecWitness cache
HINT: run `specwitness doctor` to resolve or provision Playwright …
```

`~/Library/Caches/specwitness/` does not exist afterwards. Nothing was downloaded; nothing tried.

## What is NOT wrong, and must not be "fixed"

The surrounding design is deliberate. Changing any of it fails this story.

- **Refusing to skip is correct.** `src/infra/playwright-env.ts` header: an unavailable browser
  environment is `InfraError` (exit 3) or criterion `error`, **never** `skipped`. Epic 4's
  retrospective recorded twice that a criterion nobody could adjudicate reported PASS.
  **There must still be no skip path in this file when you are done.**
- **Exit 3 rather than 1 is correct.** A missing browser says nothing about the branch.
- **`doctor` not downloading is correct** and is required by AD-12. Do not make `doctor` provision.
- **Resolving once in `verify` is correct** (`verify.ts:338-341`): read-only, offline, no spawn,
  so a run with no browser probes pays almost nothing.

## Acceptance criteria

**AC1 — a run that needs a browser provisions one.**
Given a project with a compiled plan carrying at least one `browser` probe, and no resolvable
`@playwright/test` and no SpecWitness cache, when `verify` runs, then Playwright is provisioned
into SpecWitness's own cache and the browser probes execute. Exit 3 with "cannot run" is no
longer the outcome of a merely unprovisioned machine.

**AC2 — a run that needs no browser provisions nothing.**
Given a plan with no `browser` probe, when `verify` runs on the same unprovisioned machine,
then nothing is downloaded, no `npm` is spawned, and the run's duration is unchanged.
**This is the acceptance criterion most likely to be broken by a careless fix**: provisioning
unconditionally at `verify.ts:341` would make every gates-only run pay for a browser it never
uses. Decide from the PLAN whether a browser probe exists.

**AC3 — nothing is written into the target project.**
The `⚠️` rule in `provisionPlaywright` holds through your call site: no bytes land under
`projectRoot`, including when `PLAYWRIGHT_BROWSERS_PATH` or `XDG_CACHE_HOME` point there.
Pin it with a test, and seen red.

**AC4 — a project's own pinned Playwright still wins.**
Route 2 of the header (`playwright-env.ts:920-930`): when the project resolves its own
`@playwright/test`, that version is honoured and only browsers are downloaded, into the
project's own registry — never SpecWitness's cache, and never the project tree.

**AC5 — provisioning failure is `InfraError`, never a skip and never a product FAIL.**
A network failure, an `npm` that is absent, a refused write: each is exit 3 with a message
naming what could not be done. `stageProductNegative` must remain unreachable from this path.
There must still be no path from an unusable browser environment to `pass` or `skipped`.

**AC6 — the messages become true.**
`doctor`'s hint (`playwright-capability.ts:131`) and the executor's refusal
(`browser.ts:1136-1141`) must describe what the product now does. Today the first promises
provisioning "on the first browser probe" that never happens, and the second points the
operator at `doctor`, which cannot provision. After this story, either both describe real
behaviour, or — if you and the supervisor conclude provisioning must stay opt-in — they name
the command that actually provisions. **Do not leave a message that promises what the code
does not do; that is half of what made this defect invisible for two epics.**

**AC7 — a corpus fixture pins it.**
`fixtures/corpus/README.md` is the contract; `expected.json` is HAND-WRITTEN and never
generated by the code under test. Add a fixture covering the behaviour this story adds.
**Verify the fixture fails before your change and passes after** — the 6.11 author reverted
the composition root to the placeholder to prove exactly this, and that experiment is called
"the single strongest artifact this epic produced" in the 6.11 retrospective. Do the same and
quote the red output in your PR body.

## Exact scope

**Yours:** `src/cli/commands/verify.ts` (the call site), `src/infra/playwright-env.ts` (only if
the provisioning entry point needs a signature change), `src/cli/doctor/checks/playwright-capability.ts`
(AC6, message only — no behaviour change to `doctor`), `src/surfaces/browser.ts` (AC6, message
only), the tests for all of these, and one new corpus fixture.

**Not yours:** the retry policy, the classification table, `doctor`'s network behaviour, the
plan schema (that is D-1, a separate story), and anything under `docs/planning-artifacts/`.

**If you find a defect outside this scope, REPORT IT — do not patch it.** Story 6.11's author
did that three times and the 6.11 retrospective calls it "the most valuable thing the agent
did": each finding became an owner decision, and two shipped inside half an hour. A silent
cross-story patch would have made the security sign-off cover code the spec never mentioned.

## Task 0 — before you write anything

Verify each row of the table above yourself, in the tree, this session. Two claims in the 6.11
spec were wrong and survived until an implementer read the code. If a citation here is wrong,
that is a finding for your PR body, not something to work around.

## Testing notes

- **TDD.** Every test recorded red first; quote the failure text in the PR body.
- **`tests/provisioning/playwright.provision.ts` already exists** and drives the real thing —
  read it before inventing a harness. `vitest.provision.config.ts` is its runner.
- **The unit tests must not download.** `provisionPlaywright` spawns `npm` and Playwright's
  `cli.js` through `ProcessRunner`; inject a fake, as the merged unit tests already do.
- **CI is live** (`fa2647b`) and this is the first product change since the first dogfooding
  run. Expect the Linux runner to exercise a path macOS did not.

## One thing nobody can tell you

This is the same shape of defect story 6.11 closed for `setup.install`: a capability the
diagnostics validate and the pipeline never invokes. It survived because Epic 5's browser
suites self-skip wherever chromium is absent, so no CI run ever executed the path. **Whatever
you build here, ask what would execute it on a machine that has nothing** — and if the honest
answer is "nothing in CI", say so in your PR body. Story 6.9 put the browser suites behind a
non-blocking chromium job for this exact reason; the answer may be that this belongs there too.
