# Story 7.8: a project should not have to write JavaScript to be verified

Status: review

**Implements ADR-009**, decided 2026-09-10. Read the ADR before this spec; it carries the
measurement, the rules this story may not break, and the three things it deliberately leaves
open. This spec does not restate its reasoning.

**You are one agent on a fresh branch with a supervisor, and your single story is the whole
cohort.** Set NO `set-depends-on` marker and do NOT run `set-cohort.sh`.

**Your PR title becomes the squash subject and must be a valid Conventional Commit** (e3-H).
Put the story id in a `Refs:` trailer, not the title.

## Provenance

Written by the owner's session on 2026-09-10, from the onboarding cost measured across two
dogfooding runs against `gitnebula`. It does not appear in `docs/planning-artifacts/epics.md`.

**Action item e6.11-A applies to this file.** Every citation was read on
`epic/7-dogfooding-repairs` at `db84f65` in the authoring session. **Verify them yourself
before building on them** (Task 0); a wrong citation is a finding for your PR body.

## What this story is for, in one paragraph

`specwitness init` produces a config that, comments stripped, is three lines. Everything a
plan can probe is then the operator's to write, and in the first client project that came to
645 lines of hand-written JavaScript in seven files — five of which only read the filesystem.
Three of the eight failures in the second dogfooding run were defects in that hand-written
code, all in generic file-reading logic. This story adds the surface that makes those five
scripts declarations instead of code, so a new project reaches a meaningful first verdict
without writing any.

## Verified facts — cite these, do not re-derive them from memory

| # | fact | citation |
| --- | --- | --- |
| 1 | The probe union is closed and widening it is an ADR | `src/schemas/plan.ts:436-447` |
| 2 | The union is pinned against `PROBE_SURFACES` in both directions | `tests/unit/schemas/plan-surfaces.test.ts` |
| 3 | A surface executor never produces a `CriterionStatus` (AD-13) | spine AD-13; `src/domain/criterion-result.ts` |
| 4 | Absence vs exception is a protected distinction | `src/surfaces/browser.ts:580-593` |
| 5 | `visible == false` on a missing element is satisfied (story 7.5) | `ELEMENT_ABSENT_IS_NOT_VISIBLE` in `src/surfaces/browser.ts` |
| 6 | The plan may only name ids from `observations:` | `src/cli/commands/verify.ts:1049-1053` |
| 7 | The scaffolded template is three lines of content | `templates/config.yaml` |

## Acceptance criteria

**AC1 — the surface exists and is closed properly.**
`file` joins the discriminated union in `src/schemas/plan.ts` and `PROBE_SURFACES`, and the
existing two-way pin still passes. A plan naming `surface: file` validates; one naming an
unknown surface is still refused.

**AC2 — it executes no command and reads only the worktree.**
A `file` probe spawns no process and opens no socket. Every path it reads resolves inside the
verification worktree root; `..`, an absolute path, and a symlink resolving outside it are each
refused as an **InfraError**, never as a product FAIL — nothing was adjudicated. Pin all three
with tests, seen red. **This is the acceptance criterion a careless implementation breaks**:
the surface takes data rather than a command id precisely so that nothing a provider writes can
become an executable string, and a path escape would reopen that door from the other side.

**AC3 — it obeys AD-13 unchanged.**
It implements the same `SurfaceExecutor` interface, returns the same `ProbeAttempt`, evaluates
assertions mechanically and produces no `CriterionStatus`. Retries, `flaky` and classification
stay in `domain/criterion-result.ts`. Assert that this surface is reachable from the same
dispatch path as the other four rather than a parallel one.

**AC4 — absence is a fact, and its meaning is per source (ADR-009 §5).**
A missing path read as existence is the value `false` and compares like any other value, so
`expected: "true"` on a missing path still FAILS. A missing path read for its content is
unsatisfied for every comparison including negative ones. An unreadable path — a permission
error, a directory where a file was named — escapes as an `execError`. Three tests, three
distinct outcomes, each seen red first.

**AC5 — it replaces real scripts, shown against a real project.**
Take `gitnebula`'s `docs-presence.mjs` and `pkg-scripts.mjs` — both in
`~/dev/gitnebula/scripts/specwitness/`, both readable from here — and express what they report
as `file` probes in a corpus fixture. You are not editing gitnebula; you are proving the surface
covers what a real project actually needed. **Include the case its `findReport` got wrong:** a
document nested two directories deeper than the search looked. A surface that reproduces that
bug has not earned its place.

**AC6 — the template teaches it.**
`templates/config.yaml` gains worked, commented `file` examples that a new project can uncomment
and edit. A reader who has just run `init` must be able to write a first probe from the template
alone, without reading `src/`.

**AC7 — corpus fixtures pin it.**
`fixtures/corpus/README.md` is the contract; `expected.json` is HAND-WRITTEN and never generated
by the code under test. Cover at minimum: a satisfied existence check, a failed one, a content
match, a path escape refused as infra, and the nested-document case from AC5. **Verify they fail
before your change and pass after**, and quote the red output in your PR body.

**AC8 — nothing else changes.**
`observation` keeps its behaviour and is not deprecated (ADR-009 §6). No existing corpus fixture
changes its expected result. The full suite passes: at `db84f65` it is 213 files, 4060 passed,
3 skipped — quote your own numbers.

## The thing this story could make worse

ADR-009 records it as an accepted cost and it is the reason AC5 is written against a real
project rather than an invented one: **a declarative matcher makes it cheaper to write a probe
that passes over unmet work.** That is the failure mode the entire product exists to prevent.
The plan-authoring prompt refuses to map a criterion it cannot probe safely, and that refusal
must not weaken because a `file` probe is now cheap to emit.

You are not asked to solve this. You are asked to (a) not make it worse in the prompt, and
(b) say in your PR body what you observed about it while building — whether a plan author
handed this surface reaches for it where an `observation` or a browser probe was the honest
answer. That observation is worth more than a guess in this spec.

## Exact scope

**Yours:** `src/schemas/plan.ts` (the union), `src/config/schema.ts` if the surface needs a
config-side declaration, a new `src/surfaces/file.ts`, its wiring in `src/cli/verify/probe-dispatch.ts`,
`templates/config.yaml`, the authoring prompt only where it must learn the new surface exists,
tests for all of it, and corpus fixtures.

**Not yours:** the `observation`, `shell`, `http` and `browser` surfaces' behaviour; the
classification table; the retry policy; `domain/criterion-result.ts`; and anything in
`~/dev/gitnebula` — you read that project, you do not edit it.

**If you find a defect outside this scope, REPORT IT — do not patch it.**

## Task 0 — before you write anything

Verify the seven citations above in the tree, this session. Then read all seven of gitnebula's
probe scripts (`~/dev/gitnebula/scripts/specwitness/`, 645 lines total) and decide for yourself
which are expressible declaratively and which genuinely need code. The ADR says five and two.
**If your reading disagrees, say so in your PR body** — that number is an input to the design,
not decoration.

## Testing notes

- **TDD.** Every test recorded red first; quote the failure text in the PR body.
- The path-confinement tests must exercise real filesystem behaviour for the symlink case; a
  unit test over a string path would have passed through the whole class of defect.
- **CI is live.** Expect the Linux runner to exercise path and symlink behaviour macOS did not.

## One thing nobody can tell you

Three defects were found in gitnebula's hand-written probes, and all three were in code a
product would have written once: a non-recursive directory walk, hand-picked regexes that
missed the documents that satisfied the criterion, and a pattern count that counted comments.
**The third is the interesting one** — it counted occurrences inside the very test that
enforced the convention it measured. Ask what your matcher does about comments, about the file
that defines the thing being counted, and about a document that satisfies a criterion in
words the author of the probe did not think of. A declarative surface that cannot express
those distinctions has moved the defect rather than fixed it.

## Tasks / Subtasks

- [x] Task 0 — verify the seven citations; read all of gitnebula's probe scripts and decide what is expressible
- [x] AC1 — `file` in the plan union and `PROBE_SURFACES`; two-way pin extended to five; unknown surface still refused
- [x] AC2 — no process, no socket; `..`, absolute path and escaping symlink each refused as InfraError; seen red
- [x] AC3 — same `SurfaceExecutor` / `ProbeAttempt`; reached through `createProbeDispatcher`; joins the conformance suite
- [x] AC4 — existence absence is a value; content absence unsatisfied for every comparison; unreadable is execError; seen red
- [x] AC5 — `docs-presence.mjs` and `pkg-scripts.mjs` (plus `ui-source-census.mjs`) expressed as file probes in a corpus fixture, nested-document case included
- [x] AC6 — `templates/config.yaml` teaches worked `file` examples; a test proves each validates when uncommented
- [x] AC7 — three hand-written corpus fixtures, red on the base build and green after
- [x] AC8 — `observation` behaviour unchanged, no existing fixture changed, full suite green

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (`claude-opus-5`), story agent `alice`, 2026-09-10.

### Task 0 — citations and the scripts

Checked on `story/7.8-declarative-file-surface` at `06c0c80` (ADR-009 on top of `db84f65`; no cited
file differs between the two).

| # | claim | verified |
| --- | --- | --- |
| 1 | closed union, "widening it is an ADR" at `src/schemas/plan.ts:436-447` | exact — doc comment 435-441, union 442-447 |
| 2 | two-way pin in `tests/unit/schemas/plan-surfaces.test.ts` | exact — compile-time and run-time halves |
| 3 | executors never produce a `CriterionStatus` | exact — `SurfaceExecutor` has no field for one |
| 4 | absence vs exception at `src/surfaces/browser.ts:580-593` | correct — the "DELIBERATELY DOES NOT CATCH" block in `GENERATED_SPEC` |
| 5 | `ELEMENT_ABSENT_IS_NOT_VISIBLE` | exact — declared at `browser.ts:2059` |
| 6 | plan names only `observations:` ids, `verify.ts:1049-1053` | exact — inside `declaredPlanIds` |
| 7 | template is three lines of content | exact — `version`, `project`, `baseBranch` |

**One finding about the spec's own numbers.** `~/dev/gitnebula/scripts/specwitness/` holds EIGHT files
totalling 645 lines. Seven are the observation scripts (565 lines); the eighth, `serve-bundle.mjs` (80), is
the service bridge behind `services.viewer`, not a probe script. "645 lines in seven files" conflates them.

**Expressibility (the ADR says five and two).** My reading agrees on the split and disagrees on how clean
it is: `docs-presence.mjs` and `pkg-scripts.mjs` are fully expressible; `ui-source-census.mjs`,
`testid-census.mjs` and `file-integrity.mjs` are expressible EXCEPT for a residue that still needs code
(`expectsWithoutMessage` needs a parser; per-file testid lists and the class-selector count are regex
captures; "byte-for-byte unchanged" is a base-vs-head comparison and the summed dependency count needs key
counting). `analysis-shape.mjs` and `ui-config-shape.mjs` are code, as the ADR says. So: five mostly, two
not — and three of the five keep a remainder. Detail in `DECISIONS.md` D1.

### Debug Log References

- Red, schema layer (before any `src/` change): `13 failed | 46 passed` across `plan-surfaces`,
  `plan-file-probe` and `retries` — e.g. `expected [ 'browser', 'file', 'http', …(2) ] to deeply equal
  [ 'browser', 'http', …(2) ]`.
- Red, executor (module absent): `Cannot find module '../../../src/surfaces/file.js'`.
- Red by MUTATION, because "module not found" proves little: with the confinement check forced to
  `true`, 4 AC2 symlink tests fail (file, relative directory, glob-matched, dangling); with a content
  absence read as the empty string, 3 AC4 tests fail. `src/surfaces/file.ts` restored byte-identical
  (`cmp`) after each.
- Red, prompt: `6 failed | 1 passed` — the one passing test is the word-for-word pin on the existing
  refusal, which must pass before and after.
- Red, corpus (AC7): the three fixtures run against a build of the base commit in a temporary worktree:
  every one exits 3 with `ERROR: plan is malformed: … plan.criteria.0.probes.0.surface: Invalid
  discriminator value. Expected 'http' | 'browser' | 'observation' | 'shell'`; totals `3 failed | 34
  passed (37)`. On this branch: `37 passed (37)`.
- Red, self-review hardening: a file name the TREE chose, carrying an ANSI escape, reached the
  `execError` text raw — `expected 'file probe \'the-probe\': \'docs/bad\…' to contain '\u001b'`
  (escaped form). Tree-derived names are now escaped on the way out; green after.
- Red, codex review finding (P2, fixed): ``const s = `${`http://x`}`; forbidden();`` counted 0
  occurrences of `forbidden()` under `ignoreComments` — the inner backtick ended the outer template and
  the rest of the line was blanked as a comment. Now an interpolation stack; unterminated templates are
  refused. Three tests added, seen red first.
- Red, supervisor review of PR #89 (fixed): the E6-09 census counted only the double-quoted
  `"__gitnebula"`, so a single-quoted or template-literal spelling in code passed. With the new
  `file-surface-verdicts` E1-06 still using that census, the corpus went red:
  ```
  FAIL  fixture 'file-surface-verdicts' > produces exactly the outcome its hand-written expected.json pins
      - criterion E1-06: expected 'fail', observed 'pass'
      - stderr: expected it to contain "actual:   2"
  Tests  1 failed | 36 passed (37)
  ```
  Switched every census (fixtures and template) to the bare name; green after.
- Red, codex review of e424f39 (P1, fixed): a parent directory swapped for a symlink between
  resolution and `open` was followed — the test read the outside file and resolved instead of
  rejecting. Reads now re-validate the opened descriptor (containment + device/inode).
- My first fixture draft named `https://example.test` inside a `.ts` source and the corpus refused to run
  ANY fixture ("names a host that is not loopback"). Replaced with a `//` inside a string.

### Completion Notes List

1. **The surface** (`src/surfaces/file.ts`): six reads — `exists`, `fileCount` (existence reads: absence
   is a value) and `content`, `jsonPath`, `occurrences`, `filesContaining` (content reads: absence is
   unsatisfied for every comparison). Literal matching only, no regex anywhere a provider can reach.
   `ignoreCase` and `ignoreComments: c-like` sit on the target (D13).
2. **Confinement** is checked before any byte is read: the pattern by `domain/tree-path.ts` (one grammar
   shared with the schema), then segment-by-segment `lstat` resolution from `realpath(root)`, every symlink
   resolved and required to land inside the root (a dangling one judged lexically), reads opened with
   `O_NOFOLLOW | O_NONBLOCK`. Exact directory-entry name matching, so macOS's case-insensitive volume
   cannot pass what Linux fails.
3. **AD-13 unchanged**: same interface, one evaluation per assertion, no status; routed by
   `createProbeDispatcher` with no runner; joins the story 4.7 conformance suite on all four situations.
   Evidence reuses the `observation` member (`observationId: file:<probe>`) rather than widening the closed
   evidence union (D4).
4. **The three gitnebula defects** each have a unit test and a corpus-fixture criterion: `**` recursion
   (the report two levels deeper), case-folded literal stems required to co-occur in ONE file (the
   different wording), and a string/regex-aware comment stripper plus exclusions for the defining module
   and the enforcing test (the census that counted comments).
5. **The prompt** names the surface and its limit (D12). The refusal paragraph is pinned word for word.
6. **The template** gains a commented plan snippet with four worked probes; a new test extracts them as a
   reader would and validates each with the real plan schema.
7. **Scope deviations, each forced and stated.** `domain/criterion-result.ts` gains `'file'` in
   `PROBE_SURFACES` (AC1 requires it; nothing else there changed). `config/schema.ts` gains `retries.file`
   (forced by `Record<ProbeSurface>`). `surfaces/observation.ts` EXPORTS `readPath`, `compare`,
   `slugify`, `discriminator` with no behaviour change, so the file surface shares one jsonPath dialect
   rather than inventing a second. `domain/plan.ts` gains the `FileProbe` types, which the union needs.
8. **Self-review hardening.** A glob reports file names the branch chose, and those names reached
   `execError` messages raw, so a control character or an ANSI escape in a file name could reach the
   operator's terminal. `execError` message and hint are now passed through `printable`. Plan paths
   were already refused if they carried one. The codex review then found that the comment stripper
   did not track `${}` nesting, so a nested template could hide code from a count — the passing
   direction. Fixed with an interpolation stack; an unterminated template literal is now refused.
9. **Census narrowing, found by the supervisor.** My E6-09 probe counted only the double-quoted
   spelling while the script it replaces counted all three quote styles — a strictly weaker,
   undisclosed replacement. Now the bare name with comments stripped and the definer and enforcing
   test excluded; `file-surface-verdicts` E1-06/E1-07 pin both directions; the template example
   (which promised more than it checked) teaches the bare name, with a test that fails on a quoted one.
10. **Race on open, found by codex (P1).** A declared service runs beside verification and could swap
    a checked parent directory for a symlink before `open`, which `O_NOFOLLOW` does not cover. Every
    read now re-validates the opened descriptor; a directory listing taken mid-swap may still NAME
    outside files (Node has no `openat`), but none of their content is read.
11. **Not built, by design:** a before/after comparison (ADR-009's open question 2). The one real
   criterion that needs it — gitnebula's E6-36, "byte-for-byte unchanged" — needs a BASE-vs-HEAD read,
   which is not what `mechanics.around` does; it is a separate design.
12. **Full suite:** 219 test files, 4178 passed, 3 skipped (4181) on macOS; lint, typecheck and build clean.

### File List

NEW
- `src/domain/tree-path.ts` — the one path grammar both refusals use
- `src/surfaces/file.ts` — the executor
- `tests/unit/schemas/plan-file-probe.test.ts`
- `tests/unit/surfaces/file.test.ts`
- `tests/unit/surfaces/file-spawns-nothing.test.ts`
- `tests/unit/authoring/plan-prompt-file-surface.test.ts`
- `tests/unit/template-file-probes.test.ts`
- `tests/integration/surfaces/file-dispatch.test.ts`
- `fixtures/corpus/file-surface-replaces-probe-scripts/**` (AC5, PASS)
- `fixtures/corpus/file-surface-verdicts/**` (AC4/AC7, FAIL)
- `fixtures/corpus/file-surface-path-escape/**` (AC2/AC7, infra)

UPDATE
- `src/domain/criterion-result.ts` — `'file'` in `PROBE_SURFACES`
- `src/domain/plan.ts` — `FileProbe`, `FileAssertionTarget`, `FILE_COMMENT_SYNTAXES`, `FILE_PROBE_SOURCES`
- `src/schemas/plan.ts` — `FileProbeSchema`, union entry, serializer arm, draft-id arm
- `src/config/schema.ts` — `retries.file`
- `src/surfaces/observation.ts` — four exports, no behaviour change
- `src/surfaces/index.ts` — barrel line
- `src/cli/verify/probe-dispatch.ts` — the `file` arm
- `src/authoring/plan-prompt.ts` — the surface and its limit
- `templates/config.yaml` — `retries.file`, the file-probe section
- `fixtures/corpus/README.md` — evidence kind of the file surface
- `tests/helpers/plan.ts` — `FILE_PROBE`
- `tests/unit/schemas/plan-surfaces.test.ts` — five surfaces
- `tests/unit/config/retries.test.ts` — `file`
- `tests/integration/surfaces/conformance.test.ts` — the fifth row
- `tests/unit/authoring/__snapshots__/prompt-characterisation.test.ts.snap` — +38 lines, deliberate
- `docs/implementation-artifacts/sprint-status.yaml` — row to `review`
