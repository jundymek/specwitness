# Story 7.4: a project can actually declare its own redaction patterns

Status: ready-for-dev

## Provenance — a supervisor wrote this, and why it is a story rather than a correction

**Written by the supervisor of `epic/7-dogfooding-repairs` on 2026-09-07**, from evidence
gathered while reviewing story 7.0's follow-up, with the owner's agreement obtained in that
session. It does not appear in `docs/planning-artifacts/epics.md`.

It is a NEW story and not a correction to anyone's work because **nobody owns it**: no story
in the 7.0 cohort touches it, it is explicitly outside 7.0's Exact scope, and the defect
predates that story by four epics. Story 7.0's agent found it while fixing something adjacent,
recorded it as finding 17 in her Dev Agent Record, and correctly did not patch it — a config
key, edge wiring and a corpus fixture are not a line to add inside a repair.

**Action item e6.11-A applies to this file.** Every citation below was read on
`epic/7-dogfooding-repairs` at `54be81e` in the session that wrote this spec. **Verify them
yourself before building on them** (Task 0), and if one is wrong, say so in your PR body
rather than working around it. The supervisor who wrote this had three of his own premises
corrected by the 7.0 agent in one afternoon, each time by her reading a file he had only
asserted about. Do the same to this document.

## The defect, in one paragraph

AD-10 makes "config-declared extra patterns" part of the redaction rule. `RedactionOptions.extraPatterns`
is threaded through the domain, the surfaces and the authoring path, and three surface
files carry comments explaining what a project that "declares its own secrets" gets.

**UPDATED 2026-09-10, AFTER STORY 7.8 MERGED.** This spec was written against four surfaces.
There are now **five**: `src/surfaces/file.ts` threads `extraPatterns` too, correctly and from
its first commit — which means the dead wiring GREW while this story sat unassigned. Verify the
current count yourself in Task 0 and cover whatever you find; the citation table below stops at
`shell` because that is what existed when it was written, not because `file` is out of scope. **No
project can declare any.** There is no configuration key, and nothing at the edge ever
constructs a `RedactionOptions`, so every one of those paths receives `undefined` in
production and has done since Epic 3.

## Verified facts — cite these, do not re-derive them from memory

| # | fact | citation |
| --- | --- | --- |
| 1 | AD-10 names config-declared extra patterns as part of the rule | `ARCHITECTURE-SPINE.md:104-108` (AD-10) |
| 2 | The option exists and is documented as unwired | `src/domain/evidence.ts:134-142` |
| 3 | `redactText` takes them as an optional second argument | `src/domain/evidence.ts:464` |
| 4 | They are applied when present | `src/domain/evidence.ts:594` |
| 5 | The Project Config schema has NO key for them | `grep -in "redact\|extraPattern" src/config/schema.ts` → no output |
| 6 | `verify` binds no redaction anywhere | `grep -n "redaction" src/cli/commands/verify.ts` → no output |
| 7 | The browser surface threads and documents them | `src/surfaces/browser.ts:337`, `:993-994`, `:1148` |
| 8 | So does `http` | `src/surfaces/http.ts:993` |
| 9 | So does `shell` | `src/surfaces/shell.ts:618` |
| 10 | The only `extraPatterns:` producer in `src/` is an internal pass-through | `src/domain/evidence.ts:863` |

**Fact 2 in its own words**, which is the whole story:

> Epic 3 wires none — the parameter exists now so that when 4.x needs it there is nowhere new
> to put it, and no second redaction entry point gets invented.

Epics 4, 5, 6 and 7 did not wire it.

## The precedent that makes this urgent rather than tidy

`src/authoring/contract.ts:100-112` records the **same defect, already met once, in a narrower
place**. Story 6.8 gave the prompt builder a `redaction` parameter and left the entry point
without one, so — in that file's own words —

> the run's config-declared `extraPatterns` could not reach the builder from production at
> all — the parameter was reachable only from a direct builder test, which is the weakest
> possible form of "the behaviour is implemented".

A codex P1 caught it and it was fixed for the authoring path. Nobody then asked whether the
*same* was true one level up, and it was, and still is.

This is the fourth instance of one shape in this product: **D-5** (`provisionPlaywright`,
written in 5.1 and called from nowhere for two epics), **6.11** (`setup.install`, a config
surface the diagnostics validated and the pipeline never invoked), **6.8's builder parameter**
above, and this. Each survived because the only tests exercising the capability called it
themselves. That is precisely the defect class this product exists to catch in other people's
code.

## Acceptance criteria

**AC1 — a project can declare patterns, and the key has exactly one shape.**

```yaml
redaction:
  extraPatterns:
    - 'acme-[a-z0-9]{32}'
    - 'INTERNAL_[A-Z0-9_]+'
```

A top-level `redaction:` block with an `extraPatterns:` list of **strings**. camelCase, per
`src/config/schema.ts:27` (*"Key names are camelCase per the spine, EXCEPT the three `ai.roles`
keys"*) — so `redaction-patterns` and `redactionPatterns` are both wrong. The field name
mirrors `RedactionOptions.extraPatterns` deliberately, so there is no translation layer between
config and domain. Each string is compiled to a `RegExp` by the loader and **applied globally
regardless of whether the author wrote the `g` flag**, which is the rule
`src/domain/evidence.ts:134-142` already states for this option. Empty list and absent key both
mean "no extra patterns" and neither is an error.

⚠️ **An invalid regex is a `ConfigError` and the run does not start. It is never skipped.**
`src/cli/exit.ts:45-53` maps `ConfigError` to exit 3. Skipping a pattern the project supplied
precisely because the built-ins do not recognise its secrets means the run proceeds while that
secret goes to providers and into persisted artifacts — a fail-open on a security control, and
a warning does not repair it on a prompt-free path where nobody is reading stderr. **An earlier
draft of this spec said "rejected or skipped; either is defensible". That was wrong** and the
codex review of the spec caught it: only fail-closed is defensible here. This is the house rule
in `CLAUDE.md` — fail closed, then explain.

⚠️ **NOT exit 64.** `exitCodeForError` returns `EXIT.USAGE` only for `UsageError`, which is
reserved for a malformed command invocation; every other SpecWitness error, `ConfigError`
included, returns `EXIT.INFRA`. Schema failures already raise `ConfigError`
(`src/config/errors.ts`, `src/config/load.ts`). An earlier draft of this spec required exit 64
here and was wrong — that would have forced either a special case for this one field or a
change to every configuration failure in the product. Caught by the codex review of the spec
itself.

**AC2 — a declared pattern reaches every sink that redacts.**
A declared pattern is applied wherever the built-ins are: **every** surface — five as of
story 7.8, `file` included — evidence constructors before persistence, and the authoring path. Pin each with a test in which the
built-ins **cannot** match the planted secret, so the test fails if only the built-ins run.

**AC3 — the secret is absent, not merely marked.**
Every assertion checks the secret string is **absent** from the artifact, not that a redaction
marker is present (Epic 3 retro §7). A test that only looks for the marker passes against code
that prints both.

**AC4 — it reaches a persisted artifact.**
A declared pattern applies to what lands in `result.json`, not only to what reaches a terminal.
The durable artifact is the one that matters, and story 7.0 found that its two newest sinks had
been the least protected.

**AC5 — no second redaction entry point.**
`redactText` and the `domain/evidence.ts` constructors stay the only place redaction happens.
`evidence.ts:134-142` says the parameter exists precisely so no second entry point gets
invented; wiring it must not invent one.

**AC6 — a corpus fixture proves it end to end.**
A fixture whose project declares a pattern, whose run emits a matching secret, and whose
hand-written `expected.json` pins the secret **absent** from the persisted document. Seen red
before the wiring and green after — revert the edge binding, watch it fail, restore it. Quote
the red output in the PR body.

**AC7 — the comments become true.**
`browser.ts:337`, `http.ts:993`, `shell.ts:618` and `evidence.ts:134-142` describe a feature
that could not be switched on. After this story they describe what the product does. Do not
leave a comment promising what the code does not do — that is half of what kept this invisible
for four epics.

## Exact scope

**Yours:** `src/config/schema.ts` (the key); the edge wiring in **all three commands that
reach a provider or a surface** — `src/cli/commands/verify.ts`, `src/cli/commands/contract.ts`
and `src/cli/commands/plan.ts`; whatever threading is needed to reach the four surfaces and the
authoring path; the comments in AC7; the tests for all of it; and one new corpus fixture.

⚠️ **`contract` and `plan` are standalone edges and must not be forgotten.**
`src/cli/commands/contract.ts:418` calls `generateDraft` and `src/cli/commands/plan.ts:179`
calls `compilePlan`, each independently of `verify` — and **neither file contains the string
`redaction` at all** (`grep -c redaction` returns 0 for both, read this session). A project
using `specwitness contract` or `specwitness plan` would therefore still send a
custom-pattern secret to a provider, and AC2 would be satisfied on paper by wiring `verify`
alone. An earlier draft of this spec scoped only `verify.ts`; the codex review of the spec
caught it.

**Not yours:** the redaction algorithm itself, the built-in pattern set, the evidence union,
`docs/planning-artifacts/`. If the built-ins look wrong to you, report it; do not tune them
inside this story.

**If you find a defect outside this scope, REPORT IT — do not patch it.** Two of the most
valuable things in this epic so far were findings reported and deliberately left unpatched.

## Task 0 — before you write anything

Verify each row of the table above yourself, in the tree, this session. A supervisor wrote it
and supervisors get things wrong; three premises in this epic were corrected by an implementer
who read the file. If a citation here is wrong, that is a finding for your PR body.

## Testing notes

- **TDD.** Every test recorded red first; quote the failure text in the PR body.
- Plant a secret the built-ins **cannot** match, or the test proves nothing. Story 7.0's agent
  used `/wombat-[a-z0-9]+/g` in a path segment for exactly this reason.
- Corpus fixtures carry **hand-written** `expected.json` and never one generated by the code
  under test.
- Tests never spawn the real `claude`/`codex` CLIs and never read `~/.claude/` or `~/.codex/`.

## Gates you cannot satisfy — report as pending-owner, never as passed

Nothing in this story needs the owner's subscriptions or the dogfooding procedure. If you find
an AC only the owner can satisfy, that is a defect in this spec — name it in your PR body
rather than ticking it.

## Dev Agent Record

_(to be filled by the implementing agent)_

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
