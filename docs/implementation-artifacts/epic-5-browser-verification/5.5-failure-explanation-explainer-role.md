# Story 5.5: Failure explanation (explainer role) — optional

Status: ready-for-dev

## Cohort & Dependencies (harness self-configuration)

**You are in WAVE 3 of THREE waves, and wave 3 is the CLOSING run of the epic.** Epic 5 runs as three supervised waves against one long-lived epic branch, following the roadmap's own wave structure (5.1 → B: 5.2+5.3+5.4 → C: 5.5+5.6) — the owner's decision on 2026-09-03, answering Epic 4 retro action item **e4-A**. Epic 4's three-cohort split eliminated subscription freezes but lost ~7h43m of critical path to four ready PRs waiting overnight for a merge click, so Epic 5 pairs waves with **Stage 4 — a supervisor that may merge story PRs into the epic branch.**

```
master
  └── epic/5-playwright-integration-environment-resolution
        ├── wave 1 (--epic-continues):  5.1                  ← ALREADY MERGED before you start
        ├── wave 2 (--epic-continues):  5.2, 5.3, 5.4        ← ALREADY MERGED before you start
        ├── wave 3 (closing):           5.5, 5.6             ← you are here
        └──                             + retrospective + integration PR
                                                          │
        epic branch ──── one PR ──────────────────────────┴──→ master  (OWNER'S CLICK)
```

**This story and 5.6 are the two OPTIONAL stories in the project** — marked optional in `docs/planning-artifacts/epics.md`, and wave C of the roadmap is exactly the pair. They are scheduled because three waves lift the cohort-size pressure that would otherwise have forced a choice between them.

**Your wave has two agents: you (5.5), 5.6.** Expected mapping from launch order **within this wave**: **alice=5.5, bob=5.6.** Mapping is positional **per wave**, so wave 3's `alice` is 5.5, **not** 5.1 and **not** 5.2. An agent assuming an earlier wave's mapping will depend on the wrong peer. If `tmux-overview.sh` disagrees, depend on whoever holds the story id. (Epic 4 retro §2 observation 10: `events.log`'s agent field is unreliable — recycled names, plus phase events attributed to the invoking session — so join on timestamps or PR numbers, never on the name alone.)

Run after reading this spec:

1. `~/.terminal-agents/scripts/set-cohort.sh bob` — **only your own wave's peer.**
2. Complete wave **intent-sync** (write `intent.md`, read your peer's, ack).
3. **You set NO `set-depends-on` marker.** See below.

> **⚠️ EVERY PREDECESSOR MERGED BEFORE YOU STARTED, AND YOU DEPEND ON NONE OF THEM ANYWAY.** You stand on Epic 4's merged provider boundary — `src/providers/invoke.ts`, the `AgentProvider` port, the shipped `fake` adapter — all on `master` since 2026-09-02. **You do not depend on 5.2's browser surface**: your explainer summarises whatever evidence a run holds, and `gate`, `command`, `http` and `observation` evidence have all been merged since Epic 4. **You can start immediately and nothing blocks you.** Set no marker for 5.1, 5.2, 5.3 or 5.4; verify rather than assume:
> ```
> gh pr list --base epic/5-playwright-integration-environment-resolution --state merged
> ```
> If wave 2's three PRs are not all merged, stop and report it.

> **You do not depend on 5.6 either**, and it does not depend on you. ⚠️ **You and 5.6 are the two provider-facing stories in the epic and the confusion between you is the thing to settle at intent-sync**: an explainer produces **text a human reads**; an adaptation produces **a change to an executable artifact**. Different authority, different roles, different fields. **Do not share a role name, do not share a payload schema, and make sure the report does not present one as the other.**

> The step ordering is not cosmetic even though you skip step 3: the intent gate blocks `set-depends-on` and `agent-wait` until intent-sync completes, and Epic 2 lost five agents to block loops in their first minutes over exactly this (**H-10**).

### ⚠️ Your wave CLOSES the epic — the closure bookkeeping is your wave's business

Wave 3 is the run launched **without** `--epic-continues`, which is what tells the supervisor this run is the whole remainder of the epic. Consequences you should expect rather than be surprised by:

- **Your wave's supervisor reads the epic journal across all three waves** and writes `docs/findings/epic-5-retro-<date>.md` for the **whole** epic, following the shape of the **five** existing documents in `docs/findings/`. ⚠️ **The supervisor brief's §10 still wrongly claims `docs/findings/` does not exist and instructs the supervisor to establish the precedent** — the directory has held files since 2026-08-31, and **both wave 1 and wave 2 reported this** (action item **e3-I**/**e4-G**, open since Epic 3, now a fourth sighting). **Follow the existing retros, not the brief.** The journal entries from waves 1 and 2 are detailed and are the primary source for two thirds of the epic; the supervisor of this wave saw none of it first-hand.
- **It opens the one integration PR `epic → master`**, with a pending-owner list assembled from **all three** waves' journal entries. **That click is the owner's**, at every stage, and no supervisor may make it.
- **Your own PR still targets the epic branch** — **`epic/5-playwright-integration-environment-resolution`**, never `master`.

**After any unblock or `epic-updated` nudge:** `git fetch origin`, then — this project **squash-merges** — `git rebase --onto origin/epic/5-playwright-integration-environment-resolution <sha-of-your-last-merged-commit>` (**H-4**). **If your own PR is already merged, ignore a rebase nudge** (**H-3**).

### ⚠️ Stage 4: your own sign-offs may be the last thing between your code and the epic branch

Until 2026-09-03 the harness documented *"Stage 4 (merge) deliberately does not exist — merging is always the owner's click."* It exists now, and this project carries the key (`SUPERVISOR_MERGE=1`, `~/.terminal-agents/config/projects/specwitness/project.env:213`).

The gate is **three factual conditions**, because `SECURITY_GATE=1` is also set here:

1. `codex_review` — `clean` or `addressed`, **and fresh**;
2. `security_check` — `clean` or `addressed`, **and fresh**;
3. the **rebase verdict** — `clean` (or absent).

The supervisor's judgement is **not among them**. So under Stage 4 your codex-review and security sign-offs are **no longer paperwork ahead of a human's reading — they may be the last thing between your code and the epic branch.** Sign off from the actual review report, never from a status line (Epic 3 action item e3-A's mitigation).

**"Fresh" is a CONTENT rule here, not a clock rule — and the harness's older docs describe the clock rule, which will mislead you.** Because this project declares `REVIEW_IGNORE_PATHS` (`project.env:189`), `verdict_is_stale()` (`~/.terminal-agents/lib/freshness.py:234-256`) takes its content branch: a verdict is fresh **iff the diff from the recorded `codex_review_commit`/`security_check_commit` to `HEAD`, restricted to the paths your branch touches, contains zero reviewable paths.** The clock is never consulted on this path. Committing only this spec, a `docs/findings/*.md` or `sprint-status.yaml` after signing off **keeps you fresh**; touching one line under `src/` or `tests/` **makes you stale**. The patterns are **fnmatch globs whose `*` crosses `/`** (`freshness.py:160-190`), so your spec **is** covered. **`CLAUDE.md` is deliberately NOT in that set** and always re-stales you.

**The rebase verdict** (`hooks/lib-pr-poll.sh:451-513`, invoked at 669-671, written at 690) is computed **only when an agent was told to rebase and did**; an agent that never owed one has **no key at all, and that passes** — absence is not `unknown`. When present: **`clean` merges; `touched` refuses; `unknown` refuses** (`hooks/pre-tool-use.sh:1686-1712`). `touched` means the rebase moved code your sign-offs no longer cover, and **the fix is yours: re-run codex review and the security check over the rebased branch and re-record both verdicts.**

**`gh pr merge --repo` and `gh pr edit --base` are blocked outright, at every stage, for every agent.**

### Two merged waves are your contract — there is nobody to negotiate with

Waves 1 and 2 are gone. **Read their merged code and their PR bodies**, which Epic 4 proved are reliable **about their own diffs** and unreliable about anyone else's. Carry Epic 4's lesson 5 into your own PR body verbatim and obey it: ***describe your own diff; point at, but do not characterise, anyone else's.***

**Browser evidence exists by the time you run** (5.2 merged in wave 2), so you may summarise it like any other kind — but **you do not depend on it**, and your tests should not need it.

**⚠️ Read 5.3's merged work before you touch `src/schemas/result.ts`.** It added the reviewer-guidance fields to `DerivedCriterionResult` and the persisted criterion schema in wave 2, and its PR body states the final shape and whether `jsonReport`'s `schemaVersion` moved. **Build on that rather than around it.** 5.4's PR body likewise states the flake vocabulary and the per-attempt record shape.

**The renderer collision in your wave is small and it is with 5.6 alone:** `src/report/terminal.ts` and `src/schemas/result.ts` (which `src/report/json.ts` delegates to, one line). **Your field is `explanation`; 5.6's is the run's `adapted: true` marker and its adaptation record.** Settle it in writing at intent-sync — and settle the vocabulary too, so a reader can tell a hypothesis from an applied change.


### ⚠️ Wave 2's pattern, and why it is aimed at your wave

Wave 2's supervisor named the shape that fits nearly every defect the wave found — six guards in one story alone looked right and were not:

> **Two situations flattened into one code path, so testing one proves nothing about the other.**
>
> - the value is absent · vs · the read exploded — *one `catch` made them one path*
> - the artifact was produced · vs · the disk refused — *one `catch` made them one path*
> - the trace was written · vs · the trace can be read back — *a flag stood in for the artifact*
> - still on the declared host · vs · never left it — *a check after the fact*
> - the criterion is `needs_human` · vs · the probes stage ran at all — *a path ADR-003 skips*

**In every case the author had tested one half of a distinction thoroughly and the other half not at all.** Two of the wave's most serious defects were found by somebody re-reading their own work *after* the review that was supposed to be final.

And the warning that names your wave directly: **the one finding that lived between two stories** — `routeWebSocket` arriving in Playwright 1.48 while 5.1 honours whatever version a project pinned — was *"the first defect in the epic that no single story's tests could have caught."* The supervisor's conclusion: **"Wave 3 should expect more of this class, not less. 5.5 and 5.6 build on four merged stories rather than one."** Budget review rounds for seams you do not own, and when a guard passes, **break the behaviour it defends and check that it goes red** — wave 2's 5.2 agent found a vacuous guard exactly that way, where a passing test proved the probe would not *report* on an undeclared host and proved nothing about whether it would keep *driving* one.

## Story

As a repair-loop accelerator,
I want an optional, clearly non-authoritative root-cause hypothesis for failed criteria from the explainer provider,
So that repair agents get a head start without AI touching verdicts (FR-11 explainer; brief §19.3; AD-2).

## Acceptance Criteria

From `docs/planning-artifacts/epics.md` § "Story 5.5" (authoritative — expand, never weaken):

1. **Given** a run with failures and an explainer role configured
   **When** `verify --explain` (or `report --explain`) runs
   **Then** the provider receives criterion statement, expected/actual, and evidence summaries, and its hypothesis is stored/rendered in a clearly labeled non-authoritative `explanation` field — verdict and results are byte-identical with or without it.
2. **Given** no explainer configured or provider failure
   **When** `--explain` is requested
   **Then** verification results are unaffected; the explanation is simply absent with a note.

Clarifications (detail only, no weakening):

- **⚠️ AC1'S REAL REQUIREMENT IS "BYTE-IDENTICAL", AND IT MUST BE ASSERTED MECHANICALLY, NOT ARGUED.** This is the whole story. **Run the same fixture twice — once with `--explain`, once without — and compare the persisted `RunResult` with the explanation field removed. The bytes must match.** Anything that differs (a timestamp, a duration, a provider-usage entry, an ordering) either has to be excluded from the comparison **with a stated reason** or is a defect. `serializeRunResult` (`src/schemas/result.ts`) is the one function that turns a `RunResult` into bytes and is therefore the right thing to diff. **Do not settle for "the verdict was the same in both runs"** — that is the weaker claim and it would pass over a real leak of the explainer into results.
- **⚠️ THE `explainer` ROLE IS ALREADY DECLARED. You do not invent it.** `src/config/schema.ts:137` — `export const AI_ROLES = ['contract-author', 'plan-author', 'explainer'] as const;` — and `aiSchema.roles` already carries an optional `explainer` key. The config half exists; **nothing invokes it.** Verify that yourself before designing anything.
- **⚠️ AD-2 IS ABSOLUTE HERE: AN EXPLAINER NEVER TOUCHES A VERDICT.** The spine: *"Providers may author drafts (contract draft, plan draft, explanation text) — never CriterionResults, never Verdicts, never mutations of frozen artifacts. Verdict aggregation is a pure function in `domain/` with no provider access."* And Q50–Q52: the explainer is **non-authoritative**. The product's first non-negotiable rule is *never ask an LLM whether it passes.* Your hypothesis is **text a human or a repair agent reads**, and nothing else consumes it. **Make it structurally impossible for it to be consumed as anything else**, and say how in your module header.
- **The explanation goes in the ONE place the closed evidence union reserves for free-form provider text.** AD-10: *"Free-form provider text may appear only in the optional `explanation` field, clearly labeled non-authoritative."* Q47 lists it beside the six evidence kinds. **Check what already exists before adding anything**: `src/schemas/result.ts:161` shows `explanation: z.string().optional()` on `evidenceCommon`, and `src/domain/evidence.ts` has a `redactedExplanation` helper used by every evidence constructor. **Decide, state and justify** whether your criterion-level (or run-level) explanation reuses that concept or needs its own additive field — and if it needs its own, **do not widen `EVIDENCE_KINDS`** (that is an ADR).
- **⚠️ `--no-ai` AND THE ZERO-PROVIDER-CALL RERUN MUST SURVIVE THIS STORY.** FR-18 and Q66: *a frozen contract plus a compiled plan execute with zero provider calls; `--no-ai` guarantees it.* 4.7 proved it end to end and `tests/integration/verify-no-ai.test.ts` exercises all four cells of `plan present × --no-ai`. **You are adding a provider call to the verify path**, so:
  - `--explain` is **opt-in**. A default invocation attempts nothing.
  - **`--no-ai` and `--explain` together must be refused or must make `--explain` a no-op — decide, state, test.** Recommendation: **refuse with `ERROR:`+`HINT:` and exit 64 (usage)**, because a flag pair whose combination silently drops one of them is a flag pair people misread. `src/cli/commands/verify.ts:123-131` shows how `--no-ai` is wired (commander turns it into `options.ai === false`) and its header explains what the flag actually guarantees.
  - **Prove it: inject a provider that throws, run without `--explain`, and assert nothing called it.** That assertion is the one that catches an explainer that quietly runs by default.
- **AC1 names two entry points and they are genuinely different.** `verify --explain` explains a run it just produced; `report --explain` explains a **stored** run. **Q52 is binding on the second: `report` re-renders persisted `result.json` and NEVER re-executes.** So `report --explain` may make a provider call and may *add* an explanation, but **it must not re-run a single probe.** Decide whether `report --explain` writes the explanation back into the stored run or renders it transiently, **state your choice, and honour AD-8: `RunStore` is the sole writer under `.specwitness/runs/`.** If you implement only one entry point, **say which and why** — the AC's "(or `report --explain`)" reads as permitting one.
- **The prompt's inputs are fixed by AC1:** criterion statement, expected/actual, and **evidence summaries**. Three constraints on assembling them:
  - **Everything you send is already redacted, and you must not un-redact it.** `expected`/`actual` are redacted at derivation (`src/domain/criterion-result.ts:268-287`); evidence members are redacted at capture through the constructors (AD-10). **Send the redacted, bounded values — never re-read a raw file to give the model "more context."** That would be the one path by which a captured credential leaves the machine, and it is exactly the kind of well-meant helpfulness this boundary exists to stop.
  - **Bound it.** A provider prompt is a subscription cost and a context window. Summaries, not dumps.
  - **The contract's `statement` is a human's own words**, carried verbatim from the frozen contract; send it as-is.
- **Every provider invocation goes through the ONE merged gate.** AD-2 names `src/providers/invoke.ts` by path: *"if validation lived in each adapter, every future adapter would re-invent it with a slightly different failure mode."* It parses, validates against a versioned zod schema, feeds errors back, counts attempts and classifies the failure — with a bounded retry budget (default 2 ⇒ at most 3 attempts) clamped to `[0, 5]`. **Register your response schema in `src/schemas/versions.ts`** (`plan` was registered by 4.2; the file's own comment says a new artifact is a one-line addition). **Adapters return raw text and never validate** — do not add a second gate and do not bypass this one.
- **AC2's failure path is a hard requirement, not a nicety.** No explainer configured, provider binary missing, provider error, schema-gate rejection, timeout: **in every case the verification results are unaffected and the explanation is simply absent with a note.** Concretely: the explainer **never** changes an exit code, **never** turns a run into an infra error, and **never** fails the run. This is the inverse of every other provider call in the product — a plan-author failure legitimately stops a run; **an explainer failure must not.** State that asymmetry in your module header and test every route.
- **Provider usage is recorded.** `RunResult.providerUsage` (`src/domain/run-result.ts:40-57`) records role, provider, duration and attempts; Q65 requires every provider invocation to be recorded. ⚠️ **This is in direct tension with "byte-identical", and you must resolve it explicitly:** an explained run genuinely made a provider call, so its `providerUsage` genuinely differs. **Recommendation: exclude `providerUsage` from the byte-identical comparison along with the explanation field, state the exclusion and its reasoning, and assert separately that the explainer's usage entry is recorded and that a non-explained run has none.** Do not silently drop the recording to make a test pass — that would hide a subscription cost, which FR-15 and Q65 exist to make visible.
- **The label is part of the requirement.** *"Clearly labeled non-authoritative"* is in the AC. In the terminal report the explanation must be visibly marked as a hypothesis, not a finding; in JSON the field name and its documentation must carry the same. **A reader must not be able to mistake it for evidence.** Keep it bounded (FR-29).
- **The hypothesis is untrusted provider text.** Redact it at capture and bound it, exactly as every other captured text is. **Assert the secret is ABSENT, never that `[REDACTED]` is present** (Epic 3 retro §7).

## Tasks / Subtasks

- [ ] Task 0 (first, before any code): read the merged contracts (AC: all)
  - [ ] `src/providers/invoke.ts` **in full** — the ONE schema gate and bounded retry loop; the retry-budget semantics; what an adapter may and may not do.
  - [ ] `src/domain/agent-provider.ts` — `AgentProvider`, `AgentRequest`, `AgentResponse`, `AgentRole`, `ProviderDescriptor`; `src/providers/index.ts`; `src/providers/fake.ts` (the **shipped, config-selectable** fake and its fixture format).
  - [ ] `src/config/schema.ts:137` — **`AI_ROLES` already contains `explainer`** — plus `aiSchema` and the `superRefine`'s role/provider cross-check.
  - [ ] `src/schemas/versions.ts` — where a new payload schema is registered, one line.
  - [ ] `src/domain/criterion-result.ts:138-148, 268-287` — `DerivedCriterionResult`, and **why `expected`/`actual` are redacted at derivation**.
  - [ ] `src/domain/evidence.ts` — `redactText`, `boundedText`, `redactedExplanation`, and **the existing `explanation` concept on evidence members**.
  - [ ] `src/domain/run-result.ts:40-57` — `ProviderUsage`; `src/schemas/result.ts` (~150-330, `serializeRunResult`); `src/report/terminal.ts`; `src/report/json.ts` (**one line — keep it**).
  - [ ] `src/cli/commands/verify.ts` (the `--no-ai` wiring at 123-131 and its header at 45-51) and `src/cli/commands/report.ts`; `src/cli/exit.ts`.
  - [ ] `tests/integration/verify-no-ai.test.ts` — the four cells of `plan present × --no-ai` you must not break.
- [ ] Task 1 (TDD): the inertness proof (AC: 1) — **write this test FIRST; it is the story**
  - [ ] Run one fixture twice — with and without `--explain` — and assert the persisted `RunResult` is **byte-identical** modulo the explanation field and `providerUsage`, **with both exclusions stated and justified in the test's own comments**.
  - [ ] **Verify red:** make the explainer touch a criterion's status (or its `expected`) and watch this test fail. **Record what you planted** — Epic 4 retro §2 observation 7 and §3 lesson 7: *a guard is only a guard once you have seen it fail.*
  - [ ] Assert the **verdict and exit code** are identical in both runs, as a second, weaker guard.
- [ ] Task 2 (TDD): `--no-ai` and the zero-provider-call default (AC: 1)
  - [ ] `--explain` is opt-in; **a default invocation attempts nothing.**
  - [ ] `--no-ai` + `--explain`: refuse (recommended, exit 64 with `ERROR:`+`HINT:`) or no-op. **Decide, state, test.**
  - [ ] **Inject a provider that throws and prove nothing called it** on a default run — the assertion that catches an explainer running by default.
  - [ ] `tests/integration/verify-no-ai.test.ts`'s four cells stay green, unchanged.
- [ ] Task 3 (TDD): the explainer invocation (AC: 1)
  - [ ] Assemble the prompt from **redacted, bounded** criterion statement, expected/actual and evidence summaries. **Never re-read a raw evidence file.**
  - [ ] Register a response schema in `src/schemas/versions.ts` and invoke through the merged `providers/invoke.ts` gate. **No second gate; no bypass.**
  - [ ] Tests: the prompt carries the three required inputs; **a seeded secret in evidence, in `actual`, and in the criterion's own text never reaches the prompt** (assert absence); the prompt is bounded; a malformed provider response is rejected by the merged gate and produces AC2's absent-with-a-note outcome rather than a partial artifact.
- [ ] Task 4 (TDD): storage and rendering, clearly non-authoritative (AC: 1)
  - [ ] Store the hypothesis in the labelled non-authoritative field; persist it additively in `src/schemas/result.ts`; **decide the `jsonReport` `schemaVersion` question and record the reasoning**; assert an old `result.json` still parses.
  - [ ] Render it in `src/report/terminal.ts`, **visibly marked as a hypothesis and bounded**. **`src/report/json.ts` stays one line.**
  - [ ] Tests: the label is present and unambiguous; the field is absent when nothing was explained; **the hypothesis is redacted and bounded**; a reader-facing string makes the non-authoritative status explicit.
- [ ] Task 5 (TDD): AC2 — every failure path leaves results untouched
  - [ ] No explainer configured; provider binary missing; provider error; schema-gate rejection; timeout. **Each ⇒ results unaffected, explanation absent, a note explaining why, and the SAME exit code as an unexplained run.**
  - [ ] **Verify red:** make one failure path escalate to an `InfraError` and watch the guard fail.
  - [ ] Use the shipped `fake` provider (config-selectable, fixture-scripted) for the malformed-response and error paths — that is what it is for.
- [ ] Task 6 (TDD): `report --explain` (AC: 1) — **only if you implement it**
  - [ ] **Q52 is binding: `report` re-renders a persisted run and NEVER re-executes.** Assert no probe runs.
  - [ ] **AD-8: `RunStore` is the sole writer under `.specwitness/runs/`** — if you write the explanation back, write it through `RunStore`; never construct a run-directory path.
  - [ ] If you implement only `verify --explain`, **say which and why in your PR body**, and do not leave a half-wired flag on `report`.
- [ ] Task 7: hygiene (AC: all)
  - [ ] **Verify `tests/unit/config/boundary-scan.test.ts` fires** for any new module by planting an `x as unknown as DeclaredCommand`, watching it go red, removing it. **Do not author a second scan.**
  - [ ] **Verify `tests/unit/doctor/credential-boundary.test.ts` stays green.** You are adding a provider call — **never read, copy or persist `~/.claude/`, `~/.codex/` or any credential store** (NFR-1, AD-4, Q59).
  - [ ] `pnpm depcruise`, `pnpm typecheck`, `pnpm lint`, `pnpm test` green. `pnpm lint` forwards to `depcruise` and rejects eslint flags — **H-7**, not yours.
  - [ ] **Do not name a test file `*.env.test.ts`** (**H-13**). **New tests must be concurrency-safe** (**H-8**): ephemeral ports, temp directories, nothing hardcoded. **No test may invoke a real `claude` or `codex`** — use PATH shims or the shipped fake (AD-12).
  - [ ] Every fixture self-limiting; every temp directory removed in a `finally`/`afterEach` — *when a test run is killed outright, no `afterEach` executes at all* (retro §2 observation 8).

> **⚠️ THE CODEX REVIEW SANDBOX CANNOT BIND `127.0.0.1` (EPERM), so it structurally cannot run this project's service-backed integration tier.** In wave 2 the four `buildProbeFixture` files failed in every review log while every other integration file — which binds nothing — passed. **They are not regressions and they are not yours.** Wave 2's 5.3 agent first attributed this to H-8 worktree contention and corrected himself when a second review named the real cause; do not repeat that detour. Check whether a failing file binds a socket before you debug it.
  - [ ] `docs/implementation-artifacts/sprint-status.yaml` — **your row only.**

> **⚠️ `scripts/sprint-status.sh` reads the file from the worktree you are standing in**, and after wave 2's merges that is not the epic branch. Wave 2's supervisor sat 3 commits back and the script reported three rows of drift that do not exist. **Check `git show <epic-branch>:docs/implementation-artifacts/sprint-status.yaml` before believing it.** Rows 5-1 through 5-4 are already `done` on the epic branch, each flipped by its own agent in its own commit.

## Dev Notes

### Goal & Context

**This is a story whose entire value proposition is that it changes nothing.**

Every other AI-touching flow in this product produces an artifact that matters: a contract draft a human freezes, a plan that becomes executable. The explainer produces **a sentence somebody reads and may ignore.** It exists because a repair agent handed a bare `expected: 200, actual: 500` starts from zero, and a hypothesis — *"the service returned 500 on every request after the data reset; the migration in the diff drops a column this endpoint reads"* — is a head start.

That is a convenience, and conveniences are where boundaries erode. AD-2 exists because *"AI becoming the source of truth"* is the failure this product is built against, and the first non-negotiable rule in `CLAUDE.md` is **never ask an LLM whether it passes.** An explainer sits closer to that line than anything else in V0: it reads the evidence, forms an opinion about the failure, and prints it beside the verdict. **The only thing keeping it honest is that it is provably inert.**

So the acceptance criterion that matters is not "the explanation is useful." It is **byte-identical with and without `--explain`**, asserted mechanically. Write that test first, and let it fail before it passes.

Two invariants carry the story:

1. **An explainer never touches a verdict.** Not the status, not the aggregation, not the exit code. AD-2, and Q50–Q52.
2. **`--no-ai` still means zero provider calls.** FR-18 and Q66 are the product's determinism guarantee; 4.7 proved them end to end. **You add a provider call to the verify path, so you must prove it is off by default.**

### Exact scope

- The explainer invocation module (recommendation: `src/authoring/explain.ts` or `src/report/`-adjacent — **decide by layer rules, not by convenience**; see Project Structure Notes)
- A response schema + its one-line registration in `src/schemas/versions.ts`
- `src/cli/commands/verify.ts` — the `--explain` flag and its interaction with `--no-ai`
- `src/cli/commands/report.ts` — `--explain`, **only if you implement it**
- `src/schemas/result.ts` — the `explanation` field, additively
- `src/report/terminal.ts` — the clearly non-authoritative rendering
- `tests/**` — above all the inertness proof
- `docs/implementation-artifacts/sprint-status.yaml` — your row only

### Out of scope (do NOT build)

- **Anything that lets an explanation influence a result.** No status change, no severity change, no re-derivation, no retry, no "the explainer thinks this is infrastructure." **AD-2. If you want that, it is an ADR, not a branch.**
- **`aggregate()`, `RunOutcome`, `src/cli/exit.ts`, `deriveCriterionResult`.** All merged; all untouched by this story. **An explainer failure never changes an exit code.**
- **A second schema gate or a retry loop.** `src/providers/invoke.ts` implements both, exactly once, by AD-2's explicit instruction.
- **Mechanics adaptation — 5.6.** ⚠️ **Adaptation and explanation are different flows with different authority**, and confusing them would be the worst mistake available here: an explainer produces **text**; an adaptation produces **a change to an executable artifact**. Your provider may never propose a change to anything. **Say so in your module header.**
- **Retries and flake — 5.4.** **Human-judgment delivery — 5.3.** **The browser surface — 5.2.** You depend on none of them; you summarise whatever evidence a run holds.
- **Widening `EVIDENCE_KINDS`** or any closed union. An ADR, not a branch.
- **Re-reading raw evidence files to enrich a prompt.** The redacted, bounded values are what you send. This is a security boundary, not a limitation to work around.
- **`src/report/json.ts`.** One line, deliberately — `--json` stdout and the persisted `result.json` are the same bytes (Q53/Q55, AD-11).
- **`src/pipeline/stages/setup.ts`** (empty placeholder, retro §5 item 6), **`fenceMask`**, **ADR-003's prose**, **PRD addendum §B**, **e4-E**, **e4-F** — carried debt, none yours.
- **Authoring a dependency-cruiser rule, a second boundary scan, or a second credential-boundary test.** All merged and already covering you. Verify red; never duplicate.

### Dependencies & upstream contracts (by ID)

All merged. **Read the source, not this table** — Epic 2's lesson 5, re-confirmed in Epics 3 and 4: *verify shapes against merged source, not against a summary, including this spec's summaries.*

| What | Where | Why |
| --- | --- | --- |
| **AD-2: the ONE schema gate** | `src/providers/invoke.ts` | Parse, validate, feed back, count, classify. **Never bypassed, never duplicated** |
| **The provider port** | `src/domain/agent-provider.ts`, `src/providers/index.ts` | `AgentProvider`, `AgentRequest`, `AgentResponse`, `AgentRole` |
| **The shipped fake** | `src/providers/fake.ts` | Config-selectable, fixture-scripted; **how you test malformed and failing responses** |
| **The role, already declared** | `src/config/schema.ts:137` (`AI_ROLES`), `aiSchema` | **`explainer` exists in config. You invoke it; you do not invent it** |
| **Schema registration** | `src/schemas/versions.ts` | One line for a new payload schema (`plan` was 4.2's) |
| **Redaction at capture** | `src/domain/evidence.ts`, `src/domain/criterion-result.ts:268-287` | Everything you send is already redacted. **Do not un-redact it** |
| **Provider usage recording** | `src/domain/run-result.ts:40-57` | Q65: every invocation recorded. **In tension with byte-identical — resolve explicitly** |
| **Persistence** | `src/schemas/result.ts` (`evidenceCommon.explanation` at ~161; `serializeRunResult`) | Additive; old runs stay readable |
| **Rendering** | `src/report/terminal.ts`, `src/report/json.ts` (**one line**) | AD-11; the label is part of the requirement |
| **The `--no-ai` guarantee** | `src/cli/commands/verify.ts:45-51, 123-131`; `tests/integration/verify-no-ai.test.ts` | FR-18/Q66. **Four cells you must not break** |
| **`report` never re-executes** | `src/cli/commands/report.ts`; Q52 | Binding on `report --explain` |
| Sole run-directory writer | `src/infra/run-store.ts` | AD-8 |
| Credential boundary | `tests/unit/doctor/credential-boundary.test.ts` | NFR-1/Q59. Keep green; never weaken |

### Architecture compliance

- **AD-2.** Providers author **drafts** — never `CriterionResult`s, never verdicts, never mutations of frozen artifacts. Verdict aggregation is a pure function with no provider access. The one schema gate lives in `providers/invoke.ts` and is shared. **This story is the sharpest test of AD-2 in the product, because the explainer's output sits right beside the verdict.**
- **AD-10.** Free-form provider text may appear **only** in the optional `explanation` field, **clearly labeled non-authoritative** (Q47). Redaction happens at capture; render layers never re-redact.
- **AD-4 / NFR-1 / Q58, Q59.** No direct LLM API usage; AI is delegated to local `claude`/`codex` CLIs as subprocesses. **Never read, copy or persist any credential store.** In `subscription`/`chatgpt` mode the child environment **withholds** billing-risk variables; the parent environment is never mutated.
- **FR-18 / Q66.** `--no-ai` guarantees a zero-provider-call rerun. **You must prove yours is off by default.**
- **AD-11 / Q52, Q53, Q55.** One result model, many renderers; `--json` and `result.json` are the same bytes; `report` re-renders and never re-executes.
- **AD-5.** Persisted artifacts carry `schemaVersion` and evolve **additively**.
- **AD-8.** `RunStore` is the sole writer under `.specwitness/runs/`.
- **AD-12.** Hermetic: **no test invokes a real `claude` or `codex`** — PATH shims or the shipped fake.
- **AD-1.** Layer rules decide where your module lives. `src/report/**` may not import `src/infra/**`; `src/providers/**` is an adapter and may not import `src/config/**` or an application layer.
- **Conventions.** ISO-8601 UTC; bounded output with a pointer to the full file; `ERROR:`+`HINT:` on stderr; agent-callable commands prompt-free.

### Testing requirements

- **TDD, red first, and every guard seen red.** Epic 4 retro §2 observation 7 and §3 lesson 7: **a guard is only a guard once you have seen it fail.** **Your Dev Agent Record must say what you planted and what went red** — above all for the inertness proof and for the "nothing called the provider by default" assertion, which are the two guards this story exists to provide.
- **The inertness proof is a byte comparison, not an argument.** Diff `serializeRunResult`'s output. State and justify every exclusion.
- **No test invokes a real `claude` or `codex`** (AD-12). Use PATH shims or the shipped `fake` adapter, whose fixture format (`<mode>/<role>.json`, an array of raw response strings, last entry repeating) is designed to script exactly the malformed-then-valid sequences you need.
- **Fixtures are inline and built by the test.** **The Golden Verification Corpus proper is Epic 6.** Say so in your PR body.
- **Unit tests spawn zero subprocesses.**
- **H-8:** the auto-review runs `pnpm test` concurrently in your worktree. Ephemeral ports, temp directories, nothing hardcoded. **H-13:** never name a test file `*.env.test.ts`.
- **Every fixture self-limiting**; *when a test run is killed outright, no `afterEach` executes at all* (retro §2 observation 8).
- **Test every AC2 route**, not a representative one. Five routes; five tests.

### Integration expectations

Your PR body must state:

1. **The inertness proof**: how you compared, what you excluded, and why each exclusion is legitimate rather than convenient.
2. **The `--no-ai` + `--explain` decision** (refuse vs no-op) and the test that pins it, plus confirmation that `verify-no-ai.test.ts`'s four cells are untouched and green.
3. **Which entry points you implemented** — `verify --explain`, `report --explain`, or both — and why.
4. **Where the explanation is stored and rendered**, its label, whether `jsonReport`'s `schemaVersion` moved, and the reasoning.
5. **The `providerUsage` tension and how you resolved it** — recorded, and excluded from the byte comparison, with both facts asserted separately.
6. **That an explainer failure never changes an exit code**, with the five routes named.
7. **That the explainer proposes nothing and mutates nothing** — the boundary against 5.6's flow.

Carry Epic 4's lesson 5 verbatim and obey it: ***describe your own diff; point at, but do not characterise, anyone else's.***

### Failure modes (what "wrong" looks like here)

- **⚠️ An explanation that influences a result.** The failure this story exists to make impossible, and the reason the byte-identical assertion is the headline AC rather than a nicety.
- **An explainer that runs by default.** Breaks FR-18/Q66, spends subscription quota nobody asked for, and makes `--no-ai` a lie.
- **`--no-ai --explain` silently dropping one flag.** People misread flag pairs; refuse or no-op, but say which.
- **An explainer failure failing the run.** The inverse of every other provider call in the product — and a convenience that can break a verification is not a convenience.
- **Re-reading raw evidence to enrich the prompt**, sending a credential off the machine that redaction had already caught. The most likely security defect in this story, and it arrives as helpfulness.
- **An unredacted or unbounded hypothesis** reaching `result.json` and a terminal.
- **A hypothesis that reads like a finding.** "Clearly labeled non-authoritative" is in the AC because an unlabelled hypothesis beside a verdict *becomes* a finding in the reader's mind.
- **Dropping the `providerUsage` record to make the byte test pass** — hiding a subscription cost that FR-15 and Q65 exist to make visible.
- **A second schema gate**, or an adapter that validates. AD-2 names one gate, by path.
- **`report --explain` re-executing anything.** Q52 forbids it.
- **A breaking schema change** that stops last week's `result.json` parsing.

### Security

- **Everything sent to the provider must already be redacted and bounded.** `expected`/`actual` are redacted at derivation; evidence members at capture. **Never re-read a raw file for context.** A prompt is data leaving the process.
- **The hypothesis coming back is untrusted text.** Redact and bound it at capture. **Assert secrets are ABSENT, never that `[REDACTED]` is present** (Epic 3 retro §7 — output containing the marker *with the secret still beside it* survives review in a way a raw leak does not).
- **Never read, copy or persist `~/.claude/`, `~/.codex/` or any credential store** (NFR-1, AD-4, Q59). `tests/unit/doctor/credential-boundary.test.ts` enforces it — keep it green.
- **Billing safety (FR-15, AD-4):** in `subscription`/`chatgpt` mode the child environment is built by **withholding** billing-risk variables; the parent environment is never mutated. You invoke through the merged adapters, which already do this — **do not construct a child environment yourself.**
- **Never mint a `DeclaredCommand`**; provider invocation goes through the merged adapters and `ProcessRunner`.
- **Bounded retries cost real quota.** The merged gate's budget is default 2 (at most 3 attempts), clamped to `[0, 5]`. Do not raise it, do not nest a second loop.
- **Your security sign-off may be the last gate before merge** (Stage 4). Sign off from the actual report.

### Project Structure Notes

Where the invocation module lives is a **layer** decision, not a convenience one, and you must justify it. Constraints: `src/domain/**` is pure (no I/O, no zod) and cannot invoke a provider; `src/providers/**` is an adapter that may not import `src/config/**` or an application layer; `src/report/**` may not import `src/infra/**`; `src/authoring/**` is the application layer that already composes providers through the gate for contract and plan authoring, which makes it the most natural home. `src/cli/**` composes. **Pick by the rules, state the reasoning, and verify `pnpm depcruise` agrees** — plant a violating import, watch the rule fire, remove it. **Do not author a new rule.**

### References

- Epic ACs: `docs/planning-artifacts/epics.md` § "Story 5.5" (**marked optional; scheduled in wave 3 alongside 5.6, which is the roadmap's own wave C**)
- Roadmap: `docs/planning-artifacts/roadmap.md` § "EPIC 5" — wave C (*"optional, independent"*)
- Spine: **AD-2** (LLM authority boundary — the one that governs this story), **AD-10** (free-form provider text only in a labelled non-authoritative `explanation`), AD-4, AD-5, AD-8, AD-11, AD-12, AD-1; Conventions
- Questions: **Q50–Q52** (explainer non-authoritative; run storage; **`report` re-renders and never re-executes**), **Q66** (`--no-ai` guarantees a zero-provider-call rerun), **Q47** (closed evidence union + labelled `explanation`), **Q61** (the provider envelope), **Q63/Q64** (the shared schema gate; bounded retries), **Q65** (provenance — every invocation recorded), Q53/Q55 (stable JSON), Q58/Q59 (credential safety)
- PRD: **FR-11** (AgentProvider roles incl. `explainer`), **FR-18** (AI-free re-execution; *"any optional AI assistance during a run… is opt-in, recorded, and cannot alter assertions or expected values"*), FR-14, FR-15, FR-28, FR-29, FR-30, FR-31; NFR-1; brief §19.3
- Epic 4 retro: `docs/findings/epic-4-retro-2026-09-02.md` — **§2 observation 7 / §3 lesson 7** (guards seen red), **§3 lesson 5** (describe your own diff), §5 (carried debt), **§9** (gates no agent could run — including that **no test has ever invoked a real `claude`/`codex`**)
- Harness defects: `docs/findings/epic-2-harness-defects-2026-08-31.md` — **H-3, H-4, H-7, H-8, H-10, H-13**

### Gates you cannot satisfy — report as pending-owner, never as passed

- **Any CI-executed step.** No workflow has executed in **five** epics; triggers removed by owner decision 2026-08-30, and no version has ever run a single step (retro §9; e3-E still open).
- **Linux**; **Node exactly 22.12.** Everything has run on macOS/Darwin and 22.20.
- **⚠️ Real `claude`/`codex` invocations — and this story is one of the two in Epic 5 where that gap actually bites.** Every provider test in five epics uses a PATH shim or the shipped fake, so **the provenance a real CLI reports, and the shape of a real explainer response, remain unverified** (retro §9). Report the explainer as proven **against the fake** and unproven against a live subscription.
- **The Golden Verification Corpus** (Epic 6). Your fixtures are inline.
- **Whether the hypotheses are actually useful.** You can prove inertness mechanically; you cannot prove usefulness without dogfooding, which is Epic 7. Say so rather than claiming it.

## Dev Agent Record

### Context Reference

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
