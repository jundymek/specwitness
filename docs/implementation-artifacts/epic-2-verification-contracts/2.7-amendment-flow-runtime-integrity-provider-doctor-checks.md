# Story 2.7: Amendment flow, runtime integrity & provider doctor checks

Status: ready-for-dev

## Cohort & Dependencies (harness self-configuration)

All seven Epic 2 stories launch at once; the harness serializes them via depends-on. PRs target the epic integration branch `epic/2-verification-contracts` (**never `master`**). Expected agent↔task mapping from launch order: alice=2.1, bob=2.2, pamela=2.3, arnold=2.4, rambo=2.5, chuck=2.6, dolph=2.7 — if `tmux-overview.sh` shows a different mapping, depend on whoever holds the story id named below, not the name.

Run immediately after reading this spec:

- `~/.terminal-agents/scripts/set-cohort.sh alice,bob,pamela,arnold,rambo,chuck`

**You are last, you have three predecessors, and there is only ONE `waiting_for` marker.** Successive `set-depends-on.sh` calls **REPLACE** the marker; they do not accumulate. So you wait **sequentially**, and before setting each marker you check whether that predecessor has already merged — otherwise you wait forever on an event that already fired:

```bash
# Repeat this pattern for each predecessor, in this order:
#   1) chuck   (2.6 — the contract command you extend with --amend)
#   2) arnold  (2.4 — claude adapter capability result)
#   3) rambo   (2.5 — codex adapter capability result + codex doctor probe)
gh pr list --base epic/2-verification-contracts --state merged --search "<task-id>"
# merged already?  -> skip this one, move to the next
# not merged yet?  -> ~/.terminal-agents/scripts/set-depends-on.sh <peer>:done  (then wait)
```

**Immediately after EACH unblock:** `git fetch origin && git rebase origin/epic/2-verification-contracts` — your worktree was created before any of these merges. Do this before setting the next marker, so you are always rebased on the latest epic tip.

**You touch three areas other people just built** (the contract command, the doctor registry, both adapters' capability results). Use intent-sync early: ask arnold and rambo for their capability-result shapes and ask chuck for the contract command's structure **while you are still blocked**, so that when you unblock you are extending a known surface rather than reverse-engineering one.

## Story

As the human authority over expected behavior,
I want contract changes to be possible only through an explicit, versioned, confirmed amendment — and provider readiness visible in doctor,
so that agents cannot silently redefine done (FR-9, FR-10, FR-3 completion; UJ-5; ADR-005).

## Acceptance Criteria

From `docs/planning-artifacts/epics.md` (authoritative — expand, never weaken):

1. **Given** a frozen contract
   **When** I run `specwitness contract epic-7 --amend` in an interactive terminal and confirm
   **Then** a new version is created referencing the superseded version, re-reviewed and re-frozen; the audit trail (who/when/what changed) is preserved in the contract file's version history.
2. **Given** a no-TTY context (agent invocation)
   **When** `--amend` is invoked
   **Then** it refuses with `ERROR:`/`HINT:` explaining amendments are an operator action (ADR-005 stance) — it does not fall back to a flag-only path.
3. **Given** doctor
   **When** run with providers configured
   **Then** per provider it reports: binary found, version, non-interactive capability, auth-appears-usable (probed only via the CLI's own public behavior — e.g. `codex doctor` / exit codes), configured mode, and ⚠ billing-risk env vars — without reading credential files (NFR-1).

Clarifications (detail only, no weakening):

- **AC1 the amend flow is: confirm → new version → re-freeze.** Concretely: verify the current contract's integrity first (a *tampered* contract is not amendable — restore it or the audit trail is meaningless); increment `meta.version` (integer, monotonic — spine Identifiers row); append a history entry recording the superseded version, its fingerprint, the timestamp, and the operator-supplied reason; clear the frozen state so the human can edit; and re-freeze via 2.2's pure `freeze`. Whether re-freezing is a second command invocation (`--freeze`) or part of the amend flow is your call — **state it in the module header and the PR body**, and make sure the file is never left in a state that is neither valid draft nor valid frozen contract.
- **AC1 "who": take the schema-compatible branch.** 2.2's history entry is fixed at `{version, fingerprint, timestamp, reason}` — **there is no author field**, and the bullet below forbids redesigning that shape unilaterally. V0 has no identity system and inventing one is out of scope, so record reason + timestamp and state in the schema comment that authorship is attested by **git history**, not by SpecWitness. If you believe an author field is genuinely required by AC1's "who", raise it with bob in the cohort thread **before** touching a merged schema — do not add it yourself, do not read environment variables looking for a username, and do not fabricate an author.
- **AC1 `meta.history`'s shape belongs to 2.2** and is in the schema from version 1. Populate it; do not redesign it. If bob's shape is genuinely missing a field you need, ask in the cohort thread before changing a merged schema — a shape change after freeze semantics exist is expensive.
- **AC2 no-TTY refusal is absolute** (ADR-005, PRD open question 3 resolved): detect a non-interactive stdin/stdout and refuse with `ERROR:` + `HINT:` explaining that amendment is an operator action. **There is deliberately no `--yes`, no `--confirm`, no `--force`, no environment-variable bypass — for anyone.** Do not add one "for tests"; test the TTY branch by injecting the TTY predicate, not by adding a flag to the product.
- **AC2 `--amend` is the second and last TTY-permitted command** (spine "Non-interactive first": only `init` confirm and `contract --amend` may block on input; every agent-callable command — `verify`, `plan`, `report`, `doctor`, `contract --status` — stays prompt-free). Note that story 1.4 ultimately made `init` prompt-free too, so **`--amend` is the only interactive path in the product**. Everything else you touch must stay prompt-free; adding a prompt to `--status` would break the harness.
- **AC1/AC2 the confirmation is a real confirmation:** it must show *what is changing* (at minimum the current version, its fingerprint, and the reason being recorded) before accepting. A blind "are you sure? [y/N]" is friction without information, and the friction is the point only when it carries the information the operator needs to say no.
- **AC3 provider checks plug into the merged registry via NEW check modules only.** `src/cli/doctor/registry.ts`, `context.ts`, `effects.ts` and the seven existing checks in `src/cli/doctor/checks/` are merged and **not yours to modify** — the seam was built for you and is already proven by `tests/unit/doctor/registry.test.ts`. Add new files under `src/cli/doctor/checks/` and register them in `checks/index.ts` (append; do not reorder the existing entries — order is part of that file's stated contract). If you need a new side effect, add it to `DoctorEffects` **additively**; if you need new context, add a field to `DoctorContext` **additively** — that single-object shape exists precisely so your addition breaks no existing check.
- **AC3 provider checks are OPTIONAL, never required.** A missing agent CLI must **warn**, never fail. UJ-4's edge case is explicit: with no agent CLI installed, contract *generation* is unavailable but execution of existing plans still works — so a missing provider must not make `doctor` exit 3. The `required: false` ⇒ warn ⇒ exit unchanged rule is already implemented in the merged registry and renderer; use it, and let the check's `detail` explain what is unavailable.
- **AC3 reuse the adapters' probes; do not write a third one.** 2.4 and 2.5 each expose a typed capability result and their auth probe (`codex doctor` for codex; exit codes of a trivial invocation for claude — Q58). Your checks call those. A second, doctor-only probe would drift from what an actual invocation does, which is the exact failure mode a diagnostic tool must not have.
- **AC3 billing-risk warnings name the variable** (FR-15, UJ-4): `⚠ OPENAI_API_KEY present in environment — provider calls could bill your API account; see provider modes`. This is a **warn**, not a fail. Note that the merged `tests/unit/doctor/credential-boundary.test.ts` asserts doctor reads **exactly one** environment variable, `PATH` — reading `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` will make that test fail, so you must **extend that assertion deliberately** to the explicit allow-list `['PATH', 'ANTHROPIC_API_KEY', 'OPENAI_API_KEY', ...configured equivalents]` and say in the test's comment why each is permitted. Widening it to "any variable" defeats the guard — keep it an exact list.
- **Runtime integrity (the story title's middle term):** wire 2.2's `verifyIntegrity` into the paths that exist today — `contract --status` reporting a mismatch honestly, and `--amend` refusing on a tampered contract. `verify` is Epic 3 and calls chuck's guard function; do not create it.

## Tasks / Subtasks

- [x] Task 1 (TDD): the amend service (AC: 1)
  - [x] Extend `src/authoring/contract.ts` (or add `src/authoring/amend.ts` — prefer a new file to keep chuck's module stable) with a pure `amend(...)`: integrity check → version increment → history entry → unfrozen new version. Takes the instant and the reason as arguments (AD-9: no `new Date()`)
  - [x] Tests: version monotonicity, history entry contents, superseded fingerprint recorded, a tampered input refused, the pure function never touching the filesystem
- [x] Task 2 (TDD): the `--amend` CLI path (AC: 1, 2)
  - [x] Extend `src/cli/commands/contract.ts` **additively** with `--amend` and a `--reason` (or an interactive prompt for it); inject the TTY predicate and the prompt reader so both branches are unit-testable
  - [x] No-TTY refusal with the ADR-005 wording; no bypass flag anywhere
  - [x] The confirmation shows current version + fingerprint + reason before accepting
  - [x] Tests: TTY-confirmed happy path (injected), TTY-declined leaves the file byte-identical, no-TTY refusal exits 3 with `ERROR:`/`HINT:`, tampered contract refused
- [x] Task 3 (TDD): runtime integrity wiring (AC: 1)
  - [x] `contract --status` reports an integrity mismatch honestly (and `--json` carries it as a field, not as a crash)
  - [x] Tests: hand-tampered fixture through both renderings
- [x] Task 4 (TDD): provider doctor checks (AC: 3)
  - [x] `src/cli/doctor/checks/providers.ts` (or one file per check — your call, say which) — per configured provider: binary found, version, non-interactive capability, auth-appears-usable, configured mode
  - [x] `src/cli/doctor/checks/billing-risk-env.ts` — warns naming each present billing-risk variable
  - [x] Append to `BUILTIN_CHECKS` in `checks/index.ts`; add `DoctorContext`/`DoctorEffects` fields **additively** if needed
  - [x] All new checks `required: false`
  - [x] Tests: each check against injected effects (no real binaries); a configured-but-absent provider warns and the exit code stays 0; no providers configured at all is not an error; the `--json` shape still parses and now includes the new check ids
- [x] Task 5: NFR-1 guard maintenance (AC: 3)
  - [x] Update the env-read allow-list in `tests/unit/doctor/credential-boundary.test.ts` to the exact new list, with a comment justifying each entry; confirm the credential-path and `homedir` assertions still pass unchanged over `src/cli/doctor/**` and `src/providers/**`
  - [x] Verify the guard still fires: plant a `~/.codex/auth.json` literal in one of your new checks, observe red, revert
- [x] Task 6: integration tests (AC: 1, 2, 3)
  - [x] Built-CLI integration (the merged `tests/integration/` pattern, `input: ''` = no TTY): `--amend` refuses with exit 3 and the ADR-005 wording; `doctor` with a fake/absent provider reports the new checks and still exits 0; `doctor --json` remains parseable with stdout carrying nothing else; a billing-risk variable set in the child environment produces the naming warning

## Dev Notes

### Goal & Context

This story closes the loop the product exists for. UJ-5: an agent that cannot pass a criterion tries to weaken it; the fingerprint catches the edit, and the *only* legitimate path forward is an explicit, versioned, human-confirmed amendment with an audit trail. ADR-005 is honest that V0 is tamper-**evident**, not tamper-**proof** — your job is to make illegitimate change visible and legitimate change explicit, not to build cryptographic enforcement V0 does not have.

The doctor half completes FR-3, which story 1.5 deliberately left open: it built the registry and shipped seven checks, wrote a test proving an external check can plug in, and refused to stub provider checks because "a placeholder is a contract nobody agreed to". You are the consumer that seam was built for.

### Exact scope

- `src/authoring/amend.ts` (NEW) — or an additive extension of `src/authoring/contract.ts`
- `src/cli/commands/contract.ts` — **additive** extension only (`--amend`)
- `src/cli/doctor/checks/providers.ts`, `src/cli/doctor/checks/billing-risk-env.ts` (NEW) + appended registration in `src/cli/doctor/checks/index.ts`
- `src/cli/doctor/context.ts` / `effects.ts` — **additive** fields only, if genuinely needed
- `tests/unit/doctor/credential-boundary.test.ts` — the env allow-list update
- `tests/integration/doctor.test.ts` and `tests/unit/doctor/render.test.ts` — the expected check-id lists, extended (see Testing requirements)
- `tests/unit/authoring/**`, `tests/unit/doctor/**`, `tests/integration/**`
- `docs/implementation-artifacts/sprint-status.yaml` — your row only

### Out of scope (do NOT build)

- **`specwitness verify`** — Epic 3. Chuck's guard function in `src/authoring/` is the AC4 deliverable of story 2.6 and Epic 3 story 3.7 wires it. Do not create a verify command or a pipeline stage.
- **Modifying any of the seven existing doctor checks, the registry, or the renderer.** Add files; append registration. The seam's whole value is that existing checks do not change.
- **Rewriting chuck's contract command.** Extend it.
- **A third capability/auth probe.** Call the adapters'.
- **Any non-interactive amend escape hatch** — explicitly forbidden by ADR-005, in any form, including an env var, a config key, or a hidden flag.
- **Cryptographic signing, key storage, or anything implying tamper-proofness** — ADR-005's hardening path is deferred to v2+ and is out of scope; do not half-build it.
- **Reading `~/.claude/`, `~/.codex/`, `SPECWITNESS_ROLE`-style harness markers, or any credential store.** (The addendum §F floats a `SPECWITNESS_ROLE=agent` marker as an idea; ADR-005 settled on the TTY rule instead. Implement the TTY rule; do not add the marker.)
- **A `--prune`/retention feature, scorecard, or anything from Epic 6/7.**

### Dependencies & upstream contracts (by ID)

**Requires merged: 2.6 (chuck), 2.4 (arnold), 2.5 (rambo)** — in that waiting order. Plus 2.2, 2.3 and Epic 1:

| What | Where | Notes |
| --- | --- | --- |
| `src/cli/commands/contract.ts` | 2.6 | You extend it additively with `--amend`. |
| `src/authoring/**` (`generateDraft`, the verify guard, persistence) | 2.6 | Reuse; do not duplicate. |
| Claude capability result + auth probe | `src/providers/claude-code-cli.ts` (2.4) | Auth via exit codes of a trivial invocation (Q58). |
| Codex capability result + `codex doctor` probe | `src/providers/codex-cli.ts` (2.5) | `codex doctor` is the public auth surface. |
| `freeze`, `verifyIntegrity`, `fingerprint`, `meta.history` shape, `parseContract`/`serializeContract` | `src/schemas/contract.ts`, `src/schemas/canonical.ts` (2.2) | Pure. Freeze takes the instant as an argument. |
| `ProcessRunner` | `src/domain/process-runner.ts`, `src/infra/process-runner.ts` (2.3) | Probes spawn through it — never `child_process` directly. |
| `DoctorCheck` / `CheckResult` / `createRegistry` / `DoctorContext` / `DoctorEffects` / `BUILTIN_CHECKS` | `src/cli/doctor/**` (Epic 1, story 1.5) | The seam. `required: false` ⇒ warn ⇒ exit unchanged. Registration order is contract; append. |
| `DOCTOR_SCHEMA_VERSION`, `renderJson`/`renderHuman`/`hasRequiredFailure` | `src/cli/doctor/render.ts` (Epic 1) | Additive evolution only; the `--json` shape is snapshot-tested. |
| NFR-1 AST guard | `tests/unit/doctor/credential-boundary.test.ts` (Epic 1, extended by 2.3) | Asserts doctor reads exactly `PATH` — you extend that list deliberately. |
| `resolveRoleProvider`, `ProviderConfig` | `src/config/index.ts` (Epic 1) | Providers and their modes. |
| `IntegrityError`, `ProviderError`, `InfraError`, `UsageError` | `src/domain/errors.ts` (Epic 1) | Closed AD-7 set; no seventh class. |
| `EXIT` / `exitCodeForError` | `src/cli/exit.ts` (Epic 1) | The only module allowed to define an exit code — the scan test enforces it. |

Binding: FR-3 (the provider half: binary, version, non-interactive, auth-usable-without-reading-credentials, billing-risk warning, pass/warn/fail, non-zero only on a required failure), FR-9, FR-10, FR-15, NFR-1, UJ-4 edge case, UJ-5, ADR-005, AD-4, AD-5, architecture questions Q9, Q56–Q60, Q65.

### Architecture compliance

- **AD-1.** `src/cli/doctor/**` is edge code and may use `execa`, `node:fs`, `node:net` — but route provider spawning through 2.3's `ProcessRunner` so probes and invocations cannot diverge. `src/authoring/**` is application layer and must not import `cli/`.
- **AD-4.** Capabilities probed and cached per session; auth readiness only via the CLIs' own public commands and exit codes; billing-risk variables reported and withheld from children; parent env never mutated; **no credential store is ever read**.
- **AD-5.** Contract versions integer monotonic; the amendment audit trail lives in `meta` (never fingerprinted, so amending does not perturb the `spec` hash of a version); provenance preserved.
- **AD-9.** No `new Date()` in `src/authoring/**`; the instant is injected. (Doctor itself takes the wall clock directly — the merged `doctor.ts` explains why: it produces no run id.)
- **ADR-002.** Exit codes only via `cli/exit.ts`; you throw, the edge classifies. A no-TTY `--amend` refusal is exit 3 (environment/policy class), not 64 — the invocation is well-formed, the context is wrong. State this reasoning in the module header.
- **Conventions.** ISO-8601 UTC; `ERROR:`/`HINT:` via the global handler; every agent-callable command stays prompt-free.

### Testing requirements

- **TDD.**
- **The TTY branch must be tested without a TTY.** Inject the "is interactive" predicate and the prompt reader; do not shell out to a pty and do not add a product flag to make testing easier.
- **Unit:** amend purity and version/history correctness; the three refusal paths (no TTY, declined, tampered); each new doctor check against injected effects; the billing-risk check with and without variables present.
- **Integration (built CLI, `input: ''`):** `--amend` refuses with exit 3 and the ADR-005 wording; `doctor` still exits 0 with providers absent; `doctor --json` still parses and stdout carries nothing else; the new check ids appear in registration order.
- **No real `claude`/`codex` needed.** Use PATH shims (2.4/2.5 established the fixtures) or injected effects. Any real-CLI test is tagged and skipped by default (AD-12).
- **NFR-1 guard:** allow-list updated to an exact list with justification, and the guard verified red-then-green.
- **Doctor's `--json` shape is pinned by HAND-WRITTEN assertions, not snapshots** — there are no `.snap` files and no `toMatchSnapshot` anywhere in this repository. Appending checks **will** fail `tests/integration/doctor.test.ts` (~line 267 asserts the exact seven-element check-id list with `toEqual`) and `tests/unit/doctor/render.test.ts` (line ~103, same pattern). Extend both expected lists deliberately, in registration order, and mention the change in the PR body — silently loosening an exact-list assertion to a `toContain` would delete the guarantee that the `--json` order is stable.

### Integration expectations

- **Epic 3** validates contract integrity before every verify run (FR-9) using 2.2's primitives and chuck's guard; your `--amend` is the only sanctioned way a contract legitimately changes afterwards.
- **Epic 7 (dogfooding)** is where a real `claude`/`codex` finally runs. Your doctor checks are what tells the operator whether that will work — so their `detail` text should be actionable ("codex found, v0.144.4, exec available, auth appears usable" / "claude not found on PATH — contract generation unavailable; existing plans still run").
- Epic exit criteria served: "No code path reads credential stores"; the FR-3 provider half; UJ-4's edge case; UJ-5's amendment path.
- **You close Epic 2.** Your PR is the last one before the epic's integration merge — leave the tree coherent: full suite green, `depcruise` clean, `--json` snapshots updated, and the epic's exit criteria addressable one by one in your PR body.

### Failure modes to consider

- **The no-TTY refusal accidentally being bypassable** — e.g. a `--reason` flag that skips the confirmation, or a check on `stdout.isTTY` only while the prompt reads stdin. Check the stream you actually read from, and add a test that the flag combination does *not* create a bypass.
- **An amend that leaves the file in a half state** (version bumped, history written, but not re-frozen, and the fingerprint now stale). Write atomically, and decide explicitly whether the post-amend file is a valid *draft* (recommended: yes, with `frozen: false` and the history intact) — never something `parseContract` rejects.
- **Amending a tampered contract**, which would launder the tampering into the audit trail as legitimate. Refuse: integrity first, always.
- **A doctor check throwing and taking the report down** — the merged registry already isolates checks (a throwing check reports `fail` with its detail), but a *provider* check reporting `fail` would change the exit code if you marked it required. Mark them all optional.
- **A provider probe hanging** — put a timeout on every spawn; the merged git checks use 5s and `checks/git.ts` holds the shared vocabulary for "missing / hung / said no". Follow that shape.
- **Doctor becoming slow** because each provider spawns two or three probes sequentially. Cache within the run; the registry is deliberately sequential, so keep individual probes cheap.
- **The credential-boundary test failing for a legitimate reason** and being "fixed" by loosening it to allow any env read. That would delete the guard. Extend it to an exact list.
- **A configured provider whose adapter is `fake`** — do not report it as a missing binary; report it as what it is.
- **No `ai` block at all in config** — no provider checks, no warnings, exit 0. That is a normal project state, not a diagnosis.

### Security

- **NFR-1 is the headline property of this story.** Auth readiness is probed exclusively through the CLIs' own public behavior (`codex doctor`, exit codes of a trivial invocation). No path reads `~/.claude/`, `~/.codex/`, `CLAUDE_CONFIG_DIR`, `CODEX_HOME`, `.netrc`, or any credential file. The AST guard enforces it; you keep the guard sharp by extending the env allow-list precisely rather than broadly.
- **ADR-005 is a security control, not a UX preference.** The no-TTY refusal with no escape hatch is what stops a coding agent from scripting a contract amendment. Do not add a bypass, and do not make the refusal conditional on anything an agent controls.
- **Billing safety:** report the variables, never their values. A warning that printed a key would be a credential leak into terminal scrollback and PR bodies.
- **No shell anywhere**; all probes go through `ProcessRunner` with argv arrays.
- `SECURITY_GATE=1`: this is the most security-relevant story in the epic — write a substantive `set-security-status.sh` note covering the no-TTY refusal's completeness, the credential-boundary allow-list change, and the absence of any bypass.

### Project Structure Notes

- New doctor checks live in `src/cli/doctor/checks/` beside the seven existing ones; `checks/index.ts` states that its **order is part of the contract** — append, never reorder.
- `DoctorContext` and `DoctorEffects` are single extensible objects specifically so you can add to them without touching a signature; the merged headers say so explicitly. Add fields; change none.
- Prefer `src/authoring/amend.ts` (new file) over editing chuck's `contract.ts` where the logic is separable — smaller diffs on freshly merged files are easier to review and easier to rebase.
- Test fakes belong in `tests/`, never `src/` (Epic 1 decision) — except 2.3's config-selectable `src/providers/fake.ts`.
- Story branch `story/2.7-amendment-flow-runtime-integrity-provider-doctor-checks`; PR targets `epic/2-verification-contracts`. Conventional Commits (e.g. `feat(cli): add contract amend flow and provider doctor checks`); subject ≤72 chars.
- **CI is disabled repo-wide** (retrospective §2.1/A1). Run `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm depcruise`, `pnpm test`, `pnpm build`, `./scripts/pack-smoke.sh` locally, paste the output into the PR body, and name Linux, Node-22.12 and the real-CLI paths as unverified.

### References

- [Source: docs/planning-artifacts/epics.md#Story 2.7] — acceptance criteria (verbatim above)
- [Source: docs/planning-artifacts/prds/prd-specwitness-2026-08-30/prd.md#FR-3] — the provider half of doctor: binary, non-interactive, auth without credential reads, billing warning
- [Source: docs/planning-artifacts/prds/prd-specwitness-2026-08-30/prd.md#FR-9] — integrity validated before every run
- [Source: docs/planning-artifacts/prds/prd-specwitness-2026-08-30/prd.md#FR-10] — explicit amendment, preserved history, no in-place mutation of a frozen contract
- [Source: docs/planning-artifacts/prds/prd-specwitness-2026-08-30/prd.md#FR-15] — billing safety warnings
- [Source: docs/planning-artifacts/prds/prd-specwitness-2026-08-30/prd.md#UJ-4] — doctor journey incl. the no-agent-CLI edge case
- [Source: docs/planning-artifacts/prds/prd-specwitness-2026-08-30/prd.md#UJ-5] — tampering caught, correction stays possible
- [Source: docs/adr/ADR-005-freeze-enforcement-limits.md] — amend is operator-only: TTY required, no-TTY refusal, no escape hatch
- [Source: docs/planning-artifacts/architecture/architecture-specwitness-2026-08-30/ARCHITECTURE-SPINE.md#AD-4] — auth probing via public CLI behavior only
- [Source: docs/planning-artifacts/architecture/architecture-specwitness-2026-08-30/ARCHITECTURE-SPINE.md#Consistency Conventions] — `contract --amend` is the TTY-permitted exception; everything agent-callable stays prompt-free
- [Source: docs/planning-artifacts/architecture/architecture-specwitness-2026-08-30/architecture-questions.md#Q9] — amendment semantics; #Q58 auth probing; #Q65 provenance
- [Source: docs/planning-artifacts/roadmap.md#EPIC 2] — 2.7 runs last, touching the contract command, doctor and both adapters
- [Source: src/cli/doctor/registry.ts] — the seam, with its three safety properties stated in the header
- [Source: src/cli/doctor/checks/index.ts] — registration order is contract; this story is named in its header
- [Source: tests/unit/doctor/credential-boundary.test.ts] — the NFR-1 guard whose env allow-list you extend
- [Source: docs/findings/epic-1-retro-2026-08-31.md] — §5.2 fakes in tests/; §2.3 dismissal-backed-by-a-test; "reproduce, do not trust"

## Dev Agent Record

### Agent Model Used

claude-opus-5 (Claude Code, autonomous supervised-cohort mode).

### Debug Log References

Four investigations worth keeping, each reproduced before it was acted on
("reproduce, do not trust" — epic-1 retrospective).

1. **`runGit` never reported `notFound`.** Reported by 2.3 (pamela) after her
   own runner test went red against the same pattern. Reproduced directly:
   under the pinned execa 10 with `reject: false`, a missing binary **resolves**
   with `code: 'ENOENT'` and `exitCode: undefined` rather than throwing, so the
   ENOENT test inside the `catch` never ran. `git-present` therefore reported
   *"git --version exited without a code"* on a machine with no git — the one
   diagnosis doctor most exists to give, given wrong.

2. **An invalid `cwd` raises the same ENOENT as a missing binary.** Pamela's
   follow-up. My first fix made this *worse*: a vague wrong answer became a
   confident one ("git not found on PATH; install git") for an operator whose
   git is fine and whose directory is not. Reproduced with
   `execa('git', ['--version'], {cwd: '/no/such/dir'})` → `code: 'ENOENT'` with
   git installed. Now the filesystem is asked before ENOENT is allowed to mean
   "no such binary".

3. **The `--amend` TOCTOU gap.** Integrity was verified against bytes read
   *before* the confirmation prompt — a window as long as a human takes to type.
   A tamper landing in that window would have been written over with a fresh
   audit entry, laundering it as an operator-confirmed change. Demonstrated by
   editing the file from inside the prompt callback, which is the only place the
   race can be opened.

4. **My AD-9 purity guard failed on its own documentation.** The module header
   explains *why* there is no wall-clock read, so the forbidden string appeared
   in prose. Fixed by stripping comments before scanning rather than softening
   the assertion — a guard that cannot tell code from a comment about code
   teaches people to delete the comment.

**Guards verified red-then-green, never assumed:** the NFR-1 credential scan
(planted `~/.codex/auth.json` + `CODEX_HOME` in a new check), the AD-9 purity
scan (planted a real `new Date()`), the billing-adapter drift guard, and the
`ANTHROPIC_AUTH_TOKEN` behavioural test. One of these taught me something: the
drift guard I wrote did **not** fire when I removed a name from `SPENDABLE_BY`,
because it only proves doctor *knows* a name, not that it treats it as
spendable. Its comment now states that limit and the behavioural test covers the
gap.

### Completion Notes List

**All six tasks complete. All three ACs met.**

**AC1 — amendment.** `src/authoring/amend.ts` is pure: integrity check → version
increment → history entry recording the *superseded* version and *its*
fingerprint → unfrozen draft. Instant and reason are arguments (AD-9).

**AC2 — the no-TTY refusal is absolute.** No `--yes`, `--confirm`, `--force`,
environment variable or config key, and none added "for tests" — the TTY
predicate and prompt reader are injected. Exit **3**, not 64: the invocation is
well-formed, the context is wrong. Asserted through the built binary with
`input: ''`, including that `--help` offers no escape hatch to find.

**AC3 — provider readiness + billing.** `ai-providers` and `billing-risk-env`
appended to `BUILTIN_CHECKS`; the seven existing checks untouched. Both
`required: false`, so nothing here can move doctor's exit code (UJ-4).

**Deviations from the spec, each deliberate:**

- **Re-freeze is a SECOND invocation** (`--freeze`), not part of the amend flow.
  The spec delegated this choice. I first chose the opposite and reversed it:
  AC1 asks for a version that is "re-reviewed and re-frozen" and the
  clarification says to clear the frozen state *so the human can edit*. Freezing
  in-call closes that window and yields version N+1 whose criteria are identical
  to version N — an audit trail recording a change that never happened. The
  spec's own recommendation ("the post-amend file is a valid draft") supports
  this; I had read the warning and skipped the recommendation attached to it.
  Reversal announced to 2.6 rather than left in a diff. DECISIONS.md D1.

- **ONE `ai-providers` check over the collection**, not one per provider —
  the house pattern of `commands-resolvable` and `ports-free`, and what keeps
  the `--json` check-id list identical on every project. D11.

- **`effects.ts` changed non-additively.** The spec scopes this file to additive
  fields; the two ENOENT fixes are behaviour changes. Taken because it is my
  directory this wave, my AC3 depends on that exact classification, and I close
  the epic. Declared rather than slipped in. D13.

- **No `author` field** in the history entry. Raised with 2.2 before touching a
  merged schema, as instructed, and confirmed. A self-reported identity in a
  file the agent can already edit is an attestation with no attester behind it;
  authorship is attested by git history. D2.

- **`--status` integrity is 2.6's, verified by me.** One job with two owners is
  one too many; agreed in intent-sync that chuck owns the `state`/`integrity`
  fields and I assert on them. I added the human-rendering half his tests did
  not cover. D9.

**One Codex finding dismissed with reasoning** (D17): a later round asked me to
warn about every present billing variable regardless of reachability. It
contradicts two earlier rounds from the same reviewer that I *did* implement,
and the spec settles it twice — AC3 frames billing reporting as *per provider*,
and the failure-mode list says a config with no `ai` block gets "no provider
checks, no warnings, exit 0". The concern underneath it was fair, so the
unreachable case now names what would make the key billable.

**One residual limitation, stated rather than hidden** (D15): a microsecond
window remains between the pre-write re-read and the rename. Closing it needs a
lock or CAS; ADR-005 scopes V0 as tamper-*evident*, and an adversary able to
write at that instant can rewrite the file a moment after the amendment lands
anyway. Named in the module header rather than half-built.

**The most instructive defect** (found by Codex): I insisted on *one probe* so
doctor could not drift from a real invocation — then judged `capable` with my
own predicate rather than the adapters'. A codex missing `--output-last-message`
was reported READY and would have refused at the point of use. Reusing a probe
is not enough; the *judgement over* the probe has to be shared too, or
duplicated with a test proving the copies agree.

**Epic 2 exit criteria served:** no code path reads a credential store (AST
guard, four-name allow-list, each justified); FR-3's provider half; UJ-4's
edge case; UJ-5's amendment path.

**Verification (CI is disabled repo-wide):** `pnpm install --frozen-lockfile`,
`typecheck`, `lint`, `depcruise` (85 modules), `test` (1332 passed / 3 skipped,
67 files), `build`, `pack-smoke` — all green locally on macOS / Node 22.20.
**Unverified:** Linux, Node 22.12 exactly, and every real-CLI path (no `claude`
or `codex` is ever spawned by this suite; AD-12 real-CLI tests stay skipped).

### File List

**NEW**
- `src/authoring/amend.ts` — the pure amendment service
- `src/cli/commands/contract-amend.ts` — the TTY-gated flow and its real IO
- `src/cli/doctor/provider-probe.ts` — normalises 2.4's and 2.5's probe shapes
- `src/cli/doctor/checks/providers.ts` — `ai-providers`
- `src/cli/doctor/checks/billing-risk-env.ts` — `billing-risk-env`
- `tests/unit/authoring/amend.test.ts`
- `tests/unit/cli/contract-amend.test.ts`
- `tests/unit/doctor/provider-probe.test.ts`
- `tests/unit/doctor/providers.test.ts`
- `tests/unit/doctor/billing-risk-env.test.ts`
- `tests/unit/doctor/effects.test.ts`

**UPDATED**
- `src/cli/commands/contract.ts` — `--amend`/`--reason`, amend dispatched before
  the project check, `--force` barred from a draft carrying amendment history
- `src/cli/doctor/effects.ts` — `probeProvider` added; ENOENT classification fixed
- `src/cli/doctor/context.ts` — `billingRiskEnv`, read by name at the edge
- `src/cli/doctor/checks/index.ts` — two appended registrations, order preserved
- `tests/unit/doctor/credential-boundary.test.ts` — four-name allow-list + drift guard
- `tests/unit/doctor/checks.test.ts` — check-id list; the 1.5 placeholder replaced
- `tests/unit/doctor/helpers.ts` — `providerProbes`, `billingRiskEnv` fakes
- `tests/integration/doctor.test.ts` — billing, absent provider, check-id list
- `tests/integration/contract.test.ts` — `--amend` refusals, human integrity rendering
- `docs/implementation-artifacts/sprint-status.yaml` — my row only
