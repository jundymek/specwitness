# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Test output
coverage/
# vitest's JSON report, written by `pnpm test:ci` and read by the skipped-suite report.
.vitest-report.json
# The same report for the browser-only run (`pnpm test:browser`, story 6.9's CI job), which
# is what proves those suites RAN rather than skipped.
.vitest-browser-report.json

# Environment / secrets
.env
.env.*
!.env.example

# SpecWitness runtime artifacts — local-only (contracts/ and plans/ ARE committed)
.specwitness/runs/
.specwitness/scorecard.jsonl

# Packed tarballs from the pack smoke test
*.tgz

# OS / editor noise
.DS_Store
*.log

# Local-only agent settings
.claude/settings.local.json
